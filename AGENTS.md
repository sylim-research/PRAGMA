# PRAGMA Codex 작업 가이드

## PRAGMA 상위 작업 원칙 — 완성 우선 모드

이 절은 이 저장소의 모든 개발·검증·문서화 판단에 우선하는 상위 원칙이다.

지금부터 PRAGMA의 최우선 목표는 **웹앱을 최대한 빠르게 실제 사용 가능한 통합 상태로 완성하고,
박사학위논문 집필에 집중할 수 있는 상태를 만드는 것**이다.

아직 개발은 끝나지 않았다. 수업 자료, 기존 기능 간 연결, 콘텐츠 생성·검수·편성·학습 수행 등
필요한 기능은 계속 구현한다. 다만 앞으로 모든 개발 판단은 다음 원칙을 따른다.

1. 연구문제와 실제 수업 운영에 필요한 핵심 기능을 우선한다.
2. 이미 만든 기능은 서로 연결하여 실제 end-to-end 흐름을 완성하는 것을 새 기능 추가보다 우선한다.
3. 웹앱 완성에 필요한 기능은 구현하되, 과설계·과도한 추상화·미래 확장용 구조는 피한다.
4. 좋은 아이디어라도 지금 학위논문 버전에 필수적이지 않으면 후순위로 둔다.
5. 구현 가능성과 실제 동작을 구분하고, 중요한 흐름은 실제 데이터로 검증한다.
6. 현재 확정된 연구·학습설계를 임의로 다시 확대하거나 재설계하지 않는다.
7. 논문에 기술할 내용과 실제 구현 상태가 어긋나지 않도록 한다.
8. 작업 중 발견한 문제는 `지금 반드시 해결` / `완성 전 해결 권장` / `후속 개선`으로 구분하고,
   자동으로 작업 범위를 확장하지 않는다.

모든 작업에서 먼저 다음을 묻는다.

> 이 작업이 PRAGMA를 실제로 완성하고 박사논문을 완성하는 최단 경로에 필요한가?

Yes라면 구현한다. No라면 지금은 하지 않는다.

속도 때문에 핵심 완성도를 희생해서도 안 되고, 완성도를 높인다는 이유로 불필요하게 개발 범위를
넓혀서도 안 된다. 목표는 최소 기능도 최대 기능도 아니다.

**논문에서 방어 가능하고 실제 수업에 사용할 수 있는 완결된 PRAGMA를 가장 짧은 경로로 만든다.**

## 동결 전 개발 규칙 · 2026-09-08

1. 저장 문항의 설계 계약으로 검사·표시한다. 최신 프롬프트와 다르다는 이유로 공통 장면·후보 대역을 다른 설계로 해석하거나 기존 승인 콘텐츠를 일괄 재생성하지 않는다.
2. 후보를 바꾸면 정답·대역의 정합성을 보존하고 해설·참고 표현·인용을 최종 후보와 함께 검사한다. 후보 국소 통과만으로 수정 미션의 품질 통과를 선언하지 않는다.
3. 모델·프롬프트·문항 계약 고정 → 최종 콘텐츠 승인·편성 → 실제 학습 수행·저장 복구 확인 뒤 동결한다. 일치하는 기존 검증은 재사용하고 추가 유료 전수 검토·모델 실험·구조 확장은 필수 결함이 없으면 하지 않는다. 상세 기준은 `docs/operations/CONTENT_REFRESH_RUNBOOK.md`를 따른다.

## 소규모 수정의 기본 완료 범위 · 2026-09-08 사용자 지시

- 소소한 UI·문구 수정은 localhost에서 확인하고 로컬 커밋까지만 한다. 푸시·PR 생성·main 병합·운영 배포는 사용자가 별도로 요청할 때 수행한다.
- 이전 작업의 배포 승인을 이후 소규모 수정에 계속 적용하지 않는다. 여러 변경을 모아 배포하라는 명시적 요청이 있으면 그 범위 안에서 진행한다.

## 운영 배포 정본 원칙

운영 웹앱의 유일한 코드 정본은 GitHub `origin/main`이다. 기능 브랜치를 Railway production에
직접 올려 일시적으로 확인하는 방식은 금지한다. preview·staging이 필요하면 production과 분리된
환경을 사용한다.

1. 기능 브랜치의 커밋이 `origin/main`의 조상이 되기 전에는 구현·배포 완료로 보고하지 않는다.
2. 운영 배포는 GitHub `main` 트리거만 사용한다. `railway up` 등 소스 직접 업로드는 production에
   사용하지 않는다. production build는 `scripts/verify-production-source.mjs`가 GitHub 저장소·브랜치·
   커밋 메타데이터를 검증하며, CLI 업로드나 비-main 소스를 거부해야 한다.
3. 병렬 작업 여러 개를 한 번에 출시할 때는 포함돼야 할 기능 커밋 SHA를 모두 명시하고, 최신
   원격 ref를 받은 뒤 `npm run release:lineage -- <sha> [sha ...]`로 각 커밋이 `origin/main`에
   포함됐는지 확인한다. 하나라도 누락되면 배포하지 않는다.
4. 통합은 최신 `origin/main`에서 만든 clean worktree에서 수행한다. dirty 기본 작업공간이나 upstream이
   사라진 브랜치를 통합·배포 기준으로 사용하지 않는다.
5. PR/통합 브랜치에서 typecheck·전체 테스트·production build를 통과시킨 뒤 main에 반영한다.
   GitHub ruleset `Protect main production`을 active로 유지해 main 변경에는 PR과 필수 status check
   `Typecheck, tests, and production build`를 요구하고, main 삭제·force push를 차단하며 bypass actor를
   두지 않는다. 1인 운영을 위해 approving review 수는 0으로 두되 PR·CI는 생략하지 않는다.
   Railway의 **Wait for CI**를 켜서 GitHub Actions 성공 전 운영 배포가 시작되지 않게 유지한다.
6. 배포 완료 보고에는 `요청 기능 커밋 → main 포함 → CI 성공 → Railway 배포 SHA → 운영 smoke`를
   구분해 기록한다. Railway에서 잠시 보였다는 사실만으로 main 통합 완료를 주장하지 않는다.
7. Supabase Edge 함수도 같은 원칙을 따른다. `origin/main`에 병합된 커밋에서만 `npm run edge:deploy -- <함수>`로
   배포하며, 이 스크립트는 미커밋 변경이나 main lineage 밖 커밋을 거부한다. `--allow-unmerged`는 사유를
   dev-log에 남기는 예외 경로일 뿐 기본 절차가 아니다. (2026-09-11 연구자 승인)
7. 운영에 이미 포함된 핵심 화면·라우트·메뉴는 회귀 테스트를 유지한다. 의도적으로 제거할 때는
   해당 테스트와 정본 문서, decision log를 같은 변경에서 갱신한다.

## 현재 작업 인수인계 · Claude Code와 Codex 공통

- 기존 작업을 이어갈 때 가장 먼저 `docs/CANONICAL.md`, 최근 관련 `docs/dev-log/`,
  `docs/research-trail/04_evidence_index.md`를 읽는다. 공개범위 정리로 삭제된
  `docs/handoff/ACTIVE_HANDOFF.md`를 현재 정본으로 가정하거나 복원하지 않는다.
- 사용자 인수인계 또는 최근 기록에 적힌 작업공간·branch·HEAD를 실제 Git 상태와 대조한 뒤 수정한다.
- 다른 worktree에 있다면 그곳을 최신 상태라고 가정하거나 사용자 변경을 정리하지 않는다.
- Claude Code와 Codex가 같은 worktree를 동시에 편집하지 않는다.

## 제품 정본 발견 규칙 · Claude Code와 Codex 공통

- 앱 작업은 먼저 `docs/CANONICAL.md`를 열고 정확한 현행 경로를 확인한다.
- 정본은 검색 결과, 수정 시각, 파일 크기, 날짜의 최신값 또는 자동 요약으로 고르지 않는다.
- 생성·평가·저장, 학습자 구조, 관리자 운영의 현재 정본은 다음 세 파일뿐이다.
  - `docs/contracts/PRAGMA_생성계약_정본.md`
  - `docs/product/PRAGMA_학습자구조_정본.md`
  - `docs/product/PRAGMA_관리자구조_정본.md`
- `docs/contracts/history/`와 `docs/product/history/`는 역사 자료다. 현재 상태를 판단하거나
  구현 근거로 사용하지 않는다. 충돌하면 파일 경로와 실제 코드·테스트 근거를 함께 보고한다.
- **최신 승인 학습설계는 MPJ5 + DCT1**이다. 다섯 MPJ의 기능은
  `첫인상 판단 → 맥락 대비 판단 → 판단하고 고쳐보기 → 이유 찾기 → 여러 초안 비교`다.
  localhost Mission V4의 다섯 MPJ를 최신 설계 근거로 보며, 그중 하나를 제거해 MPJ4로
  되돌리지 않는다.
- 현행 `mission_v5` 스키마·생성기·러너와 운영 DB·Edge·Railway는 **MPJ5 + DCT1**에
  동기화됐다. 신규 생성 순서는 `scale4 → judge3 → fix_choice → reason → multi_judge`이며
  `mpj_response_v2`로 저장한다. 과거 `mission_v5` MPJ4 행은 legacy 읽기 호환으로만 유지하고,
  최신 제품설계나 신규 생성 계약으로 해석하지 않는다. 네이티브 MPJ5의 실생성·저장·교수자 검수·
  인증 학습자 종단은 별도 운영 확인 대상이다.
- 논문 작업공간을 함께 다룰 수 있는 세션이면
  `C:\PRAGMA_THESIS_LOCAL\01_정본\00_정본목록.md`와 위 세 경로의 일치 여부도 대조한다.
  불일치하면 검색이나 날짜로 한쪽을 선택하지 않고 보고한다.

## 모델별 작업 배분 및 실행 가이드

아래 내용은 절대 규칙이 아니라 기본 권장 방식이다. 사용자 지시와 현재 작업의 성격을 우선한다.

### Codex

- 터미널을 활용해 구현·실행·검증을 빠르게 반복하는 강점을 활용한다.
- 수정 전에 필요한 범위에서 관련 코드와 영향을 확인한다.
- 단순하고 범위가 명확한 작업에는 과도한 조사나 승인 절차를 만들지 않는다.
- Claude Code가 작성한 코드를 검토할 때 구현 의도를 추측하지 않는다.
- 실제 코드·타입·테스트를 기준으로 회귀 위험, 누락, 불일치와 검증 공백을 찾는다.

### 공통 역할 분담 (2026-08-10 사용자 재확정 · 변경 지시 전까지)

- **기본 원칙**: Codex를 메인 실행 책임자로 두고, Codex가 필요성을 flag한 경우에만 Claude Code를 독립 검토자로 투입한다. 두 모델이 같은 작업 전체를 반복하거나 동시에 구현하지 않는다.
- **Codex = 메인 실행 책임자**: 조사, 코드 수정, 테스트, 브라우저 E2E, 문서 동기화, Git·배포 준비와 유지보수를 담당한다.
- **Claude Code = 선택적 독립 검토자**: Codex가 제시한 commit·diff·증거와 질문 범위 안에서 반대 가설, 누락, 회귀 위험, 분모 오류, provenance와 연구 주장 과장을 읽기 전용으로 검토한다. 원칙적으로 앱 코드를 수정하지 않는다.
- Codex는 작업마다 `[교차검증 필수]`, `[교차검증 권고]`, `[단독 진행 적합]` 중 하나로 판단한다. 세부 기준과 Claude 전달 프롬프트는 `docs/operations/AI_CROSS_REVIEW_PROTOCOL.md`를 따른다.
- **운영 순서**: ①Codex가 구현하고 검증된 커밋을 만든다 ②결정적 검토가 필요하면 Codex 작업을 멈춘다 ③Claude에는 clean commit과 명확한 질문 범위만 전달한다 ④Claude는 검토 보고서를 작성한다 ⑤사용자 판단 후 Codex가 필요한 제안만 선별 구현한다 ⑥같은 worktree·같은 기능을 두 모델이 동시에 편집하지 않는다.
- Claude에게 검토를 요청할 때는 범위를 명시한다: "관련 구조 전체를 재설계하지 말고, 제시된 결정의 타당성과 위험만 검토하라".
- **Claude의 커밋 범위**: research-trail·계약·인수인계 등 **기록 문서**는 검증 후 자율 커밋할 수 있다. 앱 코드·DB 스키마·프롬프트는 커밋하지 않는다(사용자가 개별 건으로 지정한 경우만 예외).
- **커밋 서명**: 커밋 메시지에 `Co-Authored-By:` 트레일러를 붙이지 않는다. 이 저장소의 커밋 명의는 연구자 1인으로 유지한다(AI 협업 기록은 dev-log·research-trail에 남긴다).
- 이 분담은 **누가 구현하는가만 정한다.** push·merge·PR·배포·DB 변경의 사전 승인 게이트와 user-visible UI 변경의 브리프 게이트는 그대로 유효하며, 역할 분담이 이를 대체하지 않는다.
- 예외(빌드 중단급의 명백한 1줄 수정 등)는 그때 사용자가 지정한다.

## 연구·개발 기록 원칙

이 프로젝트는 한중 통번역 학습 워크플로우 웹앱 개발과 DDR 박사학위논문 작성을 함께 진행한다. 따라서 코드 변경뿐 아니라 중요한 설계 결정, 시행착오와 검증 근거를 추적 가능하게 남겨야 한다.

이 원칙은 개발을 방해하는 절대 규칙이 아니라, 논문에 필요한 기록이 유실되지 않도록 하기 위한 기본 운영 지침이다.

### 기록 문서

다음 문서를 연구 기록의 정본으로 사용한다.

```text
docs/research-trail/
├── 01_design_traceability.md
├── 02_decision_log.md
├── 03_iteration_log.md
└── 04_evidence_index.md
```

문서가 없다면 기존 저장소 구조와 문서 형식을 확인한 뒤 생성한다. 이미 존재한다면 새 문서를 중복 생성하지 말고 기존 문서를 갱신한다.

### 작업 종료 시 기록

의미 있는 개발 작업이 끝나면 다음을 수행한다.

1. 기존 `docs/dev-log` 형식을 확인하고, 실제로 조사·수정·검증한 사실만 간결하게 기록한다.
2. 이번 변경이 논문에 남길 만큼 중요한지 판단한다.
3. 연구적으로 중요한 변경인 경우에만 관련 `docs/research-trail` 문서를 갱신한다.
4. 네 문서를 매번 모두 수정하지 말고, 변경 성격과 관련된 문서만 갱신한다.
5. 하루 마지막에는 별도의 개발 일기를 새로 쓰기보다, 당일 중요 작업의 기록 누락 여부만 확인한다.

### Research trail 갱신 기준

다음 중 하나에 해당할 때 연구 기록을 남긴다.

* 학습설계 또는 연구 구성개념이 변경된 경우
* 핵심 학습 흐름이나 사용자 경험이 변경된 경우
* 생성계약, 핵심 데이터 구조 또는 평가 방식이 변경된 경우
* 중요한 설계 대안을 검토하고 채택·기각한 경우
* 구현 실패, 테스트 결과 또는 사용자 피드백으로 설계를 수정한 경우
* 논문에서 설계 근거 또는 반복 개발 과정의 증거로 활용할 수 있는 경우

단순 UI 간격, 색상, 오탈자, import 정리, 경미한 타입 오류나 동작 변화가 없는 리팩터링은 원칙적으로 `research-trail`에 기록하지 않는다. 필요한 경우 `dev-log`에만 기록한다.

### 문서별 역할

* `01_design_traceability.md`: 연구 문제·이론·설계 원리·웹앱 기능·생성 데이터·검증 방법 사이의 연결이 바뀔 때 갱신한다.
* `02_decision_log.md`: 중요한 설계 결정을 내리거나 유력한 대안을 채택·기각했을 때 문제, 대안, 결정, 근거와 관련 커밋을 기록한다.
* `03_iteration_log.md`: 의미 있는 반복 개발 단위별로 시작 문제, 변경 내용, 검증 결과, 예상과 달랐던 점과 다음 설계에 반영할 교훈을 기록한다.
* `04_evidence_index.md`: Git 커밋·태그·릴리스, 테스트 결과, 스크린샷, 영상, 정본 문서 등 논문 근거로 활용할 증거가 새로 생겼을 때 위치를 기록한다.

### 기록 원칙

1. 중요한 결정은 가능한 한 해당 작업이 끝난 직후 기록하고, 나중에 기억에 의존해 복원하지 않는다.
2. 단순히 “무엇을 완료했다”보다 “왜 변경했는지, 어떤 대안을 검토했는지, 무엇으로 검증했는지”를 우선 기록한다.
3. 사소한 변경까지 연구 기록으로 만들지 않는다.
4. 구현 내용, 테스트 결과와 파일 경로는 실제 확인된 사실만 기록한다.
5. 설계 이유, 대안 또는 연구적 의미가 명확하지 않으면 추측하여 만들어내지 말고 `확인 필요`로 표시한다.
6. 기존 문서의 결정 번호, 반복 번호, 표 형식과 작성 방식을 우선 따른다.
7. 동일한 내용을 여러 문서에 반복하지 말고, 필요한 경우 관련 decision ID, iteration ID, 커밋 또는 파일 경로로 연결한다.

### 작업 종료 보고

작업 종료 시 다음을 간결하게 보고한다.

* 수행한 변경
* 실행한 검증
* 갱신한 `dev-log`
* 갱신한 `research-trail` 문서
* 연구 기록을 갱신하지 않았다면 그 이유
* 사용자의 판단이 필요한 `확인 필요` 항목

웹앱 세션을 마칠 때는 종료 보고에 다음 형식의 **[논문 영향 3줄]**을 포함한다.

1. 바뀐 수치: 테스트 수·버전·배포 상태
2. 바뀐 화면: 논문 캡처 목록에 영향을 주는 UI
3. 바뀐 프롬프트·계약: 동결본 재발행 필요 여부

각 항목에 해당 사항이 없으면 `없음`이라고 쓴다.

기록 작업 자체가 개발보다 커지지 않도록 한다. 핵심 목표는 모든 변경을 기록하는 것이 아니라, 논문에서 중요한 설계 논리와 시행착오가 유실되지 않게 하는 것이다.
