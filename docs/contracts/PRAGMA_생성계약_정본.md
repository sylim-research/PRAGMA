# PRAGMA 생성·평가·저장 계약 정본

> 상태: 현재 정본. **이 문서는 계약(구조·불변조건·게이트)만 담는다.**
> 식별 규칙: 날짜 없는 이 경로만 현행이며, 날짜 판본은 `history/`의 역사 자료다.
> 대외 과업명은 **MJT(Metapragmatic Judgement Task)**·**DCT형 통번역 산출 과제**다(DCT: Discourse Completion Task).
> 코드·DB·JSON의 기존 `mpj_*` 식별자는 내부 호환 이름으로 유지하며 대외 표기 변경을 이유로 rename하지 않는다.
> 최신 승인 학습설계와 현행 소스 계약: **네이티브 MJT5 + DCT1**. 과거 `mission_v5`
> MJT4 행은 legacy 읽기 호환 범위이며 신규 생성 계약이 아니다.
> 테스트 수·Edge 버전·해시 값·배포 상태 같은 날짜 종속 사실은
> `docs/research-trail/04_evidence_index.md`와 관련 dev-log가 정본이며, 여기에 적지 않는다.
> **2026-08-25 구현 동기화: `21afafd`** — 현행 native 장면 2문장·선행 발화 null,
> FixChoice 3지선다·권장안 1개, MultiJudge 4후보, one-point recap을 소스·운영과 대조했다.
> **이전 전범위 코드 대조 기준선: 2026-08-07 · main `9d57612`** (모델 라우트·미션 규칙·스키마 버전 대조)
> 델타 재대조 `fcadb5b`→`9d57612`: 관할 영역 2커밋 모두 계약 무영향 —
> `canonicalRouting.test.ts` 신설, `coreQualityAudit.ts`에 기존 15축의 한국어 라벨 상수
> 추가(축·판정 로직 불변). 본문 수정 없음
> (`08_작업관리\reviews\2026-08-07_관리자구조_델타재대조_변경안.md`, 박사님 승인)
> 델타 재대조 `a0053fe`→`fcadb5b`: 통역 역할 계약 3건 반영
> (`08_작업관리\reviews\2026-08-06_정본3종_델타재대조_변경안.md`, 박사님 승인)
> 2026-08-07 개정: §5.4 AI 모델 간 독립 검토(교차 벤더 방식) 신설 + §8·§10 각 1항
> (`08_작업관리\reviews\2026-08-07_4-6-3_교차벤더검토_계약개정_변경안.md`, 박사님 승인).
> 문서 개정만이며 코드·프롬프트 변경 없음 — 위 「마지막 코드 대조」는 그대로 유효하다.
> 2026-08-07(2차) 개정: §5.4 실행 형태를 실행/노출 분리로 재규정(박사님 지시·fable 판정).
> 관리자 화면에 읽기 전용 노출을 허용·요구하되 실행 트리거·DB 반입은 기존 게이트 유지.
> 이 개정에 따라 관리자 열람 화면(skeleton)이 앱 코드로 추가된다.
> 2026-08-09 로컬 개정: main `7fc2c97` 기준 HSK 3.0 참고 데이터 provenance와
> 비차단 사후감사 계약을 §8.1에 추가했다. migration·seed·Edge·Railway는 미적용 상태다.
> 적용 범위: 코어 생성, Full Mission 승격, 자동검사, 사람 검수, 저장, provenance, 학습자 수행 로그
> 이전 문서: `history/legacy/PRAGMA_생성계약_v1_2026-07-23.md`는 결정 이력 보존용이며, 현재 동작과 충돌하면 이 문서가 우선한다.

### 2026-08-02 원문 분량 정책 파일럿 보정

`DEC-20260802-05`에 따라 `scenario_core_v3` 원문의 분량 통제를 다음처럼 시험 적용한다.
이 절은 분량 판정에 한해 아래의 이전 문장 수 규칙보다 우선하며, TTS 실측 전 최종 동결은
아니다.

- 정책 버전: `effective_chars_v1`
- 유효 글자: NFC 정규화 뒤 공백·문장부호를 제외한 Unicode 문자·숫자의 수
- R29 저장 차단: 수준·모드별 유효 글자 범위를 벗어난 경우
- R29 경고: 종결부호 기준 2~4문장을 벗어난 경우
- 생성층: 2~4문장 담화 형태와 유효 글자 범위를 함께 지시하고, 둘 중 하나가 어긋나면
  기존 사실을 보존하는 원문 보정을 최대 한 번 수행한다.
- 길이 정책은 클라이언트 입력을 신뢰하지 않고 서버가 `level`·`mode`에서 계산한다.
- 생성물에는 `length_policy={version, unit, min, max, actual}`을 저장한다. 생성 meta에는
  `length_policy_version`과 보정 적용 여부를 저장한다.
- `length_policy`는 정책 provenance이므로 내용 중복 판정용 `content_hash`에서는 제외하지만,
  생성 계열을 식별하는 `prompt_snapshot_hash`에는 정책 버전·범위 전체를 포함한다.

| 수준 | 번역 | 통역 |
|---|---:|---:|
| 입문·중초급 | 45~65자 | 30~45자 |
| 중급 | 60~85자 | 40~60자 |
| 고급 | 80~110자 | 55~85자 |

위 범위는 방향별 별도 규칙을 만들지 않는 파일럿 시작값이다. 통역 본배치 전 방향·수준별
표본의 TTS `provider`·`model`·`voice`·재생 시간을 함께 기록해 범위의 적절성을 확인한다.
값을 바꿀 때는 같은 버전을 덮어쓰지 않고 새 정책 버전과 새 생성 계열을 발급한다.

### 2026-09-10 사건 근거 확인과 코어 의미 검토의 진행 조건

- P/D/R 선지정은 유지하되, 원문 생성 전에 화행의 계기·행위자·책임과 접촉 이력·실제 권한·
  부담 근거를 확인한다. 일반 시드는 구체화할 수 있지만 이미 지정된 사건을 바꾸거나, 조건을
  맞추기 위해 조장·관리소장 등에 특수 권한을 발명하지 않는다. 충돌·근거 누락이면 보류한다.
- P는 해당 관계·과업에서 행사할 수 있는 실제 권한을 확인한다. 직함·처리 능력·자신의 참여
  선택권만으로 우위를 부여하지 않는다. D는 실제 접촉 이력으로 판단한다.
- R은 기존 §3.1의 화행별 준거를 적용한다. 요청의 노력·시간·자원, 사과의 피해 심각도,
  제안의 의향 충돌·수용 난이도·사안 중대성을 구별한다. 나머지 화행은 구체적 이해관계·민감성을
  근거로 검토한다. 이를 새로 타당화한 아홉 화행 공통 척도라고 주장하지 않는다.
- 관계관리 관점의 권리·의무는 사건을 해석하는 보조 지시다. 화행별 문구를 실제 잘못·피해·
  의무의 존재 증거로 사용하지 않는다. 해당 이론과 원고의 절은 삭제하지 않는다.
- 사전 확인한 상황·관계로 원문을 만든 뒤 코어의 기존 16축 의미 검사를 실행한다. 모든 축의
  근거가 있는 pass만 다음 단계로 넘긴다. fail·warning·근거 누락·호출 실패는 통과가 아니다.
  보류 초안은 검토 증거이며 새 수업 콘텐츠로 자동 저장·승인하지 않는다.
- 개별·배치 생성과 기존 코어의 미션 확장에 같은 조건을 적용한다. 현행 검사 버전과 실제
  상황·관계·원문·PDR·맥락 및 요청 화행·수준·도메인·모드·주제가 일치하는 결과만 재사용한다.
- 한국어 명사를 추측해 인물 표기를 삭제하고 조사를 재조립하는 후처리를 폐지한다. 구자료의
  A/B 표기는 문법을 훼손하며 지우지 않고 교체 전까지 보존한다. 신규 학생용 문장은 생성 때
  자연스럽게 작성하고 의미 검사에서 확인한다.
- 코어 통과는 전체 미션 승인과 다르다. MJT 각 상황·후보·권장 판단·해설, DCT 원문·참고 표현과
  피드백 연결을 함께 검토한 후보만 교과목 교체 대상으로 삼는다. 과거 콘텐츠·승인·수행 기록은
  보존하고, 새로운 release/run을 사용한다. 이번 후보 갱신은 최종 논문 콘텐츠 LOCK이 아니다.

코드 정본은 `sceneGrounding.ts`, `learnerScene.ts`, `generate-scenario/index.ts`,
`coreBatchRun.ts`, `promoteMission.ts`다. 현행 후보·버전은 `contentRelease.ts`를 따른다.
의미 검사 통과는 모델 판단의 결과이며 상황 타당성이나 학습효과의 보증으로 보고하지 않는다.

### 2026-09-07 통역 장면 간소화와 현실성·개연성 기준

DEC-20260907-02에 따라 2026-08-02·08-05·08-06의 **학생용 A/B/C 역할 소개와
통역 상황문 1인칭 금지**를 폐지한다. 당시 결정·원본은 Git 이력에서 보존한다.
통역은 주어진 발화를 듣고 목표어로 옮기는 훈련이다. 상황문은 번역처럼 나/저는의 훈련
시점 또는 구체적인 인물 역할로 쓸 수 있다. 별도 통역사가 현장에 있을 이유를 만들지 않는다.

- 학생용 상황·관계에는 실제 인물 관계, 용건, 접촉 이력, 핵심 부담만 쓴다.
  “학습자 통역사인 당신”, “한국인 원발화자”, “중국인 청자”와 A/B/C 소개를 넣지 않는다.
- R16은 번역/통역의 source modality와 명시적인 서면/구두 모순을 검사한다.
  언어명·통역사·C의 부재나 1인칭 서술을 이유로 저장을 차단하지 않는다.
- 원문 제약과 P/D/R의 준거는 유지한다. 훈련 시점이 1인칭이어도 원문의 의미·의도·화용적
  힘을 임의로 바꾸거나 자유 발화로 대체할 수 없다. R30의 답안 평가 기준 비노출도 유지한다.
- 새 생성의 인물은 실제 장면 시드와 용건을 따라 정한다. P×D 범용 예시의 이웃·상사 등을
  모든 주제에 강제로 주입하지 않는다. 지정 P/D/R과 시드의 충돌은 억지 설정으로 해결하지 않는다.
- **현실성·개연성**은 부담 R과 별개이며, 요청·거절·사과·감사·제안·초대·반대·칭찬·불만
  모든 화행의 번역·통역 미션, 양방향·모든 수준·도메인에 공통 적용한다. 코어·MJT 각 장면·DCT의
  실제 계기와 인물의 행위·결정·책임·수혜·평가 관계, 민감한 일의 신뢰·동의,
  장소·채널·행동의 연결을 확인한다. 요청·반대·거절 등의 특정 화행에만 한정하지 않는다.
  화면 밖 사정을 지어내서 정당화하지 않는다. 이웃에게 병원 예약 변경을 맡기는 장면은
  이유·위임 배경이 없으면 부적절하나 이웃·병원이라는 단어 자체를 금지하지 않는다.
- 코어 AI 검토에 16번째 축 scene_plausibility, 미션 품질검토에 implausible_scene을
  추가한다. 심한 부조화는 fail, 근거가 불확실하면 warning과 구체적 근거를 남긴다.
  이는 의미 검토 기준이며 결정론 R규칙이 상식을 완전 판정한다는 주장은 하지 않는다.
  교수자 채택 검수도 같은 기준을 사용한다.
- 이전 기준의 생성 품질결과를 새 기준의 검사로 재사용하지 않는다. 기준은 검수 snapshot과
  hash에 포함하고 이전 검수·승인·수행 이력은 보존한다.

새 후보는 pragma_natural_scene_candidate_20260907_01, 코어 core_v16,
미션 v16_natural_scene, 코어 품질 core_quality_v9_scene_plausibility,
미션 품질 quality_v19_scene_plausibility로 식별한다. 정확한 버전은
supabase/functions/_shared/contentRelease.ts가 단일 원천이다. DB schema와 MJT5+DCT1은 유지한다.

음성은 교수자가 선택한 ElevenLabs Voice ID 하나를 두 언어에 공통 적용한다.
운영 비밀 설정 ELEVENLABS_API_KEY와 ELEVENLABS_VOICE_ID로 연결하며 브라우저의
기존 기본 음성보다 서버 선택을 우선한다. 선택이 설정된 뒤에는 오류가 나도 다른 목소리나
제공자로 몰래 대체하지 않는다. 설정 전 기존 OpenAI 재생은 유지하며 음성 선택 완료로 보지 않는다.

### 2026-09-04 중→한 번역·통역 방향 실현 계약

논문 버전의 정식 중→한 경로는 **번역과 통역을 모두** 지원한다. 번역에서 학습자는 제3자
번역자가 아니라 위 표의 자기 발신 상황 화자이며, 통역에서는 주어진 발화를 듣고 목표어로 옮긴다. 두 모드 모두 자연스러운 훈련 시점을
사용하며 기존 한→중 번역·통역 경로와 원문 제약을 유지한다.

- 코어·MJT5·DCT1의 모든 원문은 요청된 화행을 유지한다. 다른 화행의 사건으로 교체하거나
  보조 화행을 중심 목적으로 승격하지 않는다.
- 모든 한국어 판단 후보·수정안·참고안은 중국어 원문의 명제, 화행 목적, 태도와 화용적 힘을
  보존한다. 등가는 축자역이 아니므로 한국어의 관계·채널·장르에 맞춘 형식 조정은 허용한다.
- 자연성은 실제 한국어 담화에서 판단한다. 중국어 어순·불필요한 주어 반복·명사화·직역 결합이
  남은 번역투는 `unnatural_language`, 화행·의도·힘의 변경은 `gate1_violation` 또는
  `internal_inconsistency`, 대역 오판은 `band_mismatch`로 기존 코드 안에서 처리한다.
- 일반 한국어 문법 교정, 존대·사과·감사 표현의 누적, 문장의 길이는 화용적 우수성의 가산점이
  아니다. 더 길거나 더 공손한 산출을 자동으로 더 좋은 답으로 판정하지 않는다.
- 네이티브 구조 `scale4 → judge3 → fix_choice → reason → multi_judge → DCT`와
  `X→A→A→A→Y→C` topology는 방향 때문에 바꾸지 않는다.
- 중→한 통역 DCT는 중국어 원발화 텍스트를 화면에 노출하지 않고 TTS로 들려준다. 학습자는
  한국어로 통역해 녹음하고, STT가 만든 한국어 전사를 직접 확인·수정한 뒤 제출한다.
- 원발화 재생 횟수는 미션의 `replay_limit`을 따르고 기본값은 2회다. 통역에는 내용 어휘 힌트를
  제공하지 않는다. 녹음 음성은 STT 처리와 로컬 확인에만 사용하고 학습 기록에는 저장하지 않으며,
  학습자가 확정한 전사만 산출·피드백·완료 기록으로 사용한다.
- 통역의 한국어 산출도 중국어 원발화의 명제·화행 목적·태도·화용적 힘을 기능적으로 등가 재현한다.
  P·D·R은 주어진 원문의 발화자와 상대 사이의 관계를 따른다.

**내부 참여자 표기의 호환성:** context_spec.interpreter_role_contract의
source_speaker:A / target_addressee:B / learner_interpreter:C / pdr_relation:A_to_B는
기존 데이터와 P/D/R 귀속을 읽기 위한 내부 값으로 유지한다. R25는 신규 코어의 구조값을
확인하지만, 이 값을 학생용 장면에 그대로 쓰거나 물리적인 세 사람이 반드시 등장해야 한다고
해석하지 않는다. 핵심 검토 대상은 누가 무엇을 누구에게 말하는지와 원문의 충실한 재현이다.

PRAGMA는 교육 우선형 학습 플랫폼이며, 수업 과정에서 연구 가능한 자료를 남긴다. 생성·평가·저장 계약은 교육 경험을 검사 도구처럼 축소하기 위한 장치가 아니라 다음을 보장하기 위한 기술 계약이다.

- AI 생성물이 사람 검수 없이 학습자에게 공개되지 않는다.
- 상황 적절성, 의미 충실성, 문법·형식 문제를 서로 섞지 않는다.
- 생성물과 학습자 수행의 버전·출처·변경 이력을 식별할 수 있다.
- 현재 자료만으로 교육 효과나 실제 상호작용 능력을 인과적으로 주장하지 않는다.

허용되는 제품·논문 설명은 다음 범위다.

> 한·중 통번역 상황에서 맥락 단서를 해석하고, 화행별 핵심 특징을 판단·조절하여 표현으로 산출하는 능력을 지원하고 기록한다.

핵심 구성개념은 `상황 기반 화용적 의사결정 및 표현 조절 능력`이다. `전반적 화용 능력`이나 실제 상호작용 능력 전체를 측정한다고 주장하지 않는다.

**정체성 문장 (2026-08-06 확정 — 계약·학습자 구조·논문 공통 기준):**

> 1. PRAGMA는 학습자가 상황에 맞는 외국어 화행을 자유롭게 만들어 내도록 하는 도구가
>    아니라, 주어진 원발화의 의미와 의도를 보존하면서 특정 수신자와 상황에 적절한
>    목표어 표현을 판단·산출·수정하도록 지원하는 통번역 학습 워크플로우이다.
> 2. 번역 모드에서 학습자는 발신자 관점에서 주어진 원문을 읽고 목표어 텍스트로
>    옮기고(자기 발신 상황의 원문 제약형 번역), 통역 모드에서는 주어진 발화를
>    듣고 목표어 발화로 옮긴다. 두 모드 모두 원문 제약과 상황 제약을 동시에 만족해야 한다.

일반 L2 산출 교육과의 경계는 **원문 제약**이다 — "이 상황에서 무엇을 말할까"가 아니라
"이 원발화를 어떻게 옮길까"를 묻는다.

세부 정체성은 `docs/research/PRAGMA_PRODUCT_RESEARCH_IDENTITY_2026-07-28.md`를 따른다.

## 2. 데이터 층위

```text
scenario_core_v3
  └─ 선택한 코어만 Full Mission으로 승격
       ├─ 현행 소스 계약: mission_v5 네이티브 MJT5 + 미니 담화 DCT1
       └─ legacy 읽기 호환: 과거 mission_v5 MJT4 + 미니 담화 DCT1
            └─ 사람 검수 후 reviewed
                 └─ 학습자 수행과 feedback/revision 기록
```

### 2.1 코어

- JSON 스키마: `scenario_core_v3` (v2는 읽기 호환 — 분량 정책 `effective_chars_v1` 도입으로 v3)
- DB 외곽 `content_format='scenario_core_v1'`은 기존 행·RPC 호환을 위해 유지되는 이름이다.
- 코어는 상황, 관계, 출발문, 선행 발화와 요약 메타를 가진다.
- 코어에는 MJT 문항과 DCT 참고안이 없다.
- 495 본 배치는 코어만 생성한다. 495개를 Full Mission으로 일괄 승격하지 않는다.

### 2.2 Full Mission

- 현행 소스의 `mission_v5`는 네이티브 MJT5+DCT1이다. 다섯 MJT의 기능은 `첫인상 판단 →
  맥락 대비 판단 → 판단하고 고쳐보기 → 이유 찾기 → 여러 초안 비교`이며 정확한 유형 순서는
  `scale4 → judge3 → fix_choice → reason → multi_judge`다.
- 네이티브 `judge3`는 첫인상 장면과 다른 사건을 DCT 앵커 P·D·R에서 판단하게 해 맥락 대비를
  독립 수행한다. 과거 `mission_v5` MJT4 행은 삭제·백필하지 않고 읽기 호환한다.
- 런타임 피드백 schema는 `feedback_v1`을 유지한다.
- 현행 미션·피드백 `prompt_version` 문자열은 promptSnapshot·Prompt Harness가 정본이다.
  이 문서에 버전 문자열을 복사해 두지 않는다.
- 코어를 선택해 개별 승격하며, 생성 직후 상태는 `generated`다.
- `mission_v1/v2`는 legacy MJT5, `mission_v3`(2026-07-28 MJT4 기준선)·`mission_v4`는 열람·회귀 호환용으로 보존한다.
- 기존 v1~v4를 v5로 백필하거나 신규 v5와 같은 생성 계열로 취급하지 않는다.
- 생성·자동 품질 점검·교수자 검수의 단위는 완전한 MJT5+DCT1 미션 한 건이다. 화행 학습 주차에는
  같은 `speech_act`·수준·언어방향·수행모드의 승인 미션 두 건을 독립적으로 편성한다.
- 주차와 각 미션의 학습목표는 특정 `target_feature` 하나가 아니라 해당 화행의 핵심 화용 판단
  묶음이다. 단일 `target_feature` 필드는 현행·legacy 호환과 문항 진단에 사용할 수 있지만,
  그 값 하나로 미션의 교육 범위를 대표하거나 완료를 판정하지 않는다.
- 두 미션 사이 A/B 역할, 변화축, 합산 진단차원 coverage를 생성계약으로 만들지 않으며 pair-first
  생성이나 pair lifecycle을 사용하지 않는다. 기존 `speech_act_ab_v1` 배정 필드는 역사 데이터
  호환용으로 남길 수 있지만 표준 생성·검수·편성은 이를 작성하거나 요구하지 않는다.

### 2.3 수행 로그

- 학습자 수행은 기존 `learner_mission_logs` 계열을 사용한다.
- 최초 산출, 제공된 피드백, 수정 산출, 완료 상태와 미션 식별자를 연결한다.
- MJT 응답은 기존 `context_judgment` JSONB에 저장한다. 네이티브 MJT5는 `mpj_response_v2`,
  legacy 4문항 미션은 `mpj_response_v1` 봉투를 사용하며 별도 DB 컬럼은 추가하지 않는다.
- 봉투에는 `mission_schema_version`, 문항별 선택 trace, 선택적 `production_support`와 `learner_dissent`를 넣는다.
- Scale4는 4점 원응답을 남기되 적절/부적절 방향만 정오 판단에 사용한다. 독립 Judge3는 선택한
  대역을 별도 trace로 남기고, FixChoice는 최초 대역과 선택한 교정안 위치를 함께 남긴다.
  Reason은 주원인 선택만 남기며 v4 이후 확신도는 생성·노출·저장하지 않는다.
- MultiJudge는 후보별 Judge3를 반복하지 않고 `best_candidate_index`와 `worst_candidate_index`만 남긴다. 번역의 내용 어휘 힌트는 열람 여부와 최초 열람 시각을 `production_support`에 비채점 trace로 남기며, 통역에는 힌트 trace를 만들지 않는다.
- 이 trace는 응답 재구성과 문항 품질 점검을 위한 비채점 기록이다. AI 참고 판정과의 일치 여부를 학습자 점수로 자동 환산하지 않는다.
- 로그가 존재한다는 이유만으로 연구 이용 동의가 성립하지 않는다.
- 이후 새 로그 필드나 분석 의미 변경은 DB 스키마와 연구 주장 범위를 먼저 보고한다.

## 3. 생성 조건과 표본 층위

### 3.1 연구 구인행렬

```text
speech_act 9 × P 3 × D 3 × R 3 = 243셀
```

- `P`: 상대적 권력
- `D`: 사회적 거리
- `R`: 해당 화행의 상황 부담

중국어 화행 연구에서 직접 근거가 확인된 범위에 한해 R의 조작적 의미를 다음처럼 구체화한다.

- 요청: 상대에게 요구되는 노력·시간·자원과 요청 수행의 부담
- 사과: 잘못이 초래한 침해·피해의 심각도
- 제안: 상대 의향과의 충돌, 제안 수용의 난이도와 사안의 중대성

세 정의를 다른 여섯 화행에 기계적으로 일반화하지 않는다. R 필드·3단 수준·학습자 UI는 유지한다.

P·D·R은 상황 적절성 판단의 조건이다. 직접성과 공손성을 하나의 축으로 합치지 않는다.

### 3.2 전달 커버리지

```text
speech_act 9 × learner_level 3 × task_mode 2 = 54셀
```

- `learner_level`: 입문·중급·고급의 교육 지원 수준
- `task_mode`: `translation | stt_interpreting`

54셀은 교육용 전달 커버리지이며 243셀과 같은 연구 구인행렬이 아니다. PRAGMA 수준을 CEFR·ACTFL 등급에 직접 대응시키지 않는다.

### 3.3 편성·사건 메타

- `domain`: 일상·학업·직장
- `theme`: 강좌 편성 영역
- `topic`: 구체적 사건 시드
- `industry`: 직장 장면의 분야 메타
- `business_function`: 직장 장면에서 수행하는 실제 업무 기능 메타
- `context_spec`: P·D·R과 사건 시드의 내부 호환성 해결층

이 값들은 필요하지만 P·D·R과 같은 독립 화용 구인으로 과장하지 않는다.
도메인은 일상·학업·직장 3분류를 유지하며 `전문·학술`을 네 번째 도메인으로 만들지 않는다.
전문성은 수준·산업·직무·theme/topic의 조합으로 표현한다. 별도 근거가 생기기 전 배치 계획의
연구 구인축을 추가하지 않는다. 2026-09-10 자유 수량 제작은 수준별 총량·과업 수량을 우선하며
일상·학업·직장은 회전 배분하고 실제 분포를 사전에 표시한다. 임의의 총량·비율에 1:1:1을 강제하지 않는다.
현행 theme 목록에서 여행·이동과 관련 세 시드는 제외한다. 기존 저장 자료는 다른 테마로 바꾸거나
삭제하지 않으며, 원래 theme/topic의 구조 검사에 필요한 읽기 호환은 보존한다.

## 4. 화행 목표와 진단용 target feature

교육용 화행은 9종이다.

- 요청
- 거절
- 감사
- 사과
- 제안
- 초대
- 반대
- 칭찬
- 직접 불만

9화행 모두 사람이 작성한 진단용 `target_feature` 카탈로그를 가진다. 칭찬하기와 칭찬 대응은
분석상 구분된 feature로 보존하되, 현재 `compliment` 코어의 자동 승격 기본값은 칭찬하기
feature다. 이 기본값은 주차·미션의 학습목표가 아니라 생성·판정 호환을 위한 내부 기본값이다.

계약 원칙:

- `targetFeatures.ts`의 코드·version·band schema가 실행 정본이다.
- AI가 target feature, band code, learner label을 임의로 만들지 않는다.
- 모든 band는 인간 캘리브레이션 전 `proposed` 지위다.
- 표현 자원은 가능한 실현 수단이지 고정 정답 목록이 아니다.
- 한 화행에 복수의 기능적 head와 supportive move가 함께 나타날 수 있다.
- 상황 적절성, 직접성·공손성, 화행별 target feature를 분리해 해석한다.
- `请`·`谢谢`·`不好意思`·호칭·완화어의 개수나 중첩 자체를 적절성의 긍정 증거로 쓰지
  않는다. 관계·부담·화행 목적에 비해 과도한 누적은 더 좋은 답이 아니라 과잉 관계조정일 수
  있으며, 기존 AI 품질점검의 대역 정합 항목과 교수자 최종 검수에서 확인한다. 새 점수나 finding
  code를 만들지 않는다.
- 교육 목표의 우선 키는 `speech_act`다. `target_feature`는 문항별 주원인·대역·피드백과
  근거 귀속을 재현하기 위한 내부 진단 태그이며, 학습자에게 주차·미션 목표로 제시하지 않는다.
- 거절에서는 표면적 거절형만으로 상호작용 기능을 확정하지 않는다. 실제 최종 불수락과 상대의
  호의·비용을 인정하며 후속 수락 가능성을 남기는 의례적 1차 사양은 완충 대역의 강약이 아니라
  장면 전체의 순차 결과로 구분한다. 이 구분을 새 band·필드·자동 점수로 만들지 않고 교수자 검수와
  수업자료의 경계 질문으로만 사용한다.
- 요청에서는 문장 내부 완화, 외부 보조행위, 요청 본체의 담화상 앞뒤 배치를 서로 다른 실현
  층위로 확인한다. 특정 장치의 유무·개수나 직접성만으로 적절성을 자동 판정하지 않으며, 이 구분도
  새 점수·필드를 만들지 않고 교수자 수업자료의 점검 질문으로만 사용한다.
- 하나의 미션은 해당 화행에 관련된 명료성·관계 조정·부담·선택권·책임 범위 등 복수의 화용
  차원을 MJT와 DCT에 걸쳐 다룬다. 정확한 다차원 coverage 저장 형식은 구현 전에 별도 version으로
  확정하며, 기존 단일 필드에 복수 의미를 억지로 덮어쓰지 않는다.

### 4.1 `usable_facts` 폐쇄 목록

명제적 supportive move는 코어의 `usable_facts` 또는 출발문에 있는 사실만 사용할 수 있다.

- 최대 8개
- 목록 밖의 이유, 대안, 수리, 보상, 재발 방지 약속, 새 일정 추가 금지
- 사실의 유무를 MJT 정답 단서로 사용 금지
- 관습화된 사과·인사, 완화어, 호칭 같은 비명제적 자원은 폐쇄 목록 대상이 아니다.

## 5. 코어 생성 계약

### 5.1 모델과 응답 형식

- 모델: `gpt-4.1-mini`
- **연구 콘텐츠(코어·미션·비평)에는 모델 교체 폴백이 없다** — 실패를 숨기고 다른 모델로
  강등하지 않는다(2026-08-04 제거). 학습자 런타임 피드백만 가용성 폴백(`gpt-4o-mini`)을
  유지하며, 폴백 발생은 호출 원장에 구분 기록된다.
- API: Chat Completions
- 코어 출력: strict Structured Outputs
- `strict: true`
- `additionalProperties: false`

필수 모델 출력 5필드:

```text
situation_ko
relation_ko
source_text
preceding_turn
brief_note_ko
```

출력 키와 DB 저장 구조는 strict 적용 전과 같다. strict schema 적용은 출력 형식 강화이며, 내용 타당성을 자동 보장하지 않는다.

### 5.2 코어 생성 계열

현재 코어 표면 해시 값은 **Prompt Harness와 `docs/research-trail/04_evidence_index.md`가 정본**이다. 이 문서에
해시 값을 복사해 두지 않는다. 해시에는 다음이 포함된다.

- system/user 프롬프트 템플릿
- 서버 주입 카탈로그와 조립 규칙
- 모델과 폴백 모델
- temperature
- response format
- strict JSON Schema 전문

셀별 화행·수준·도메인·P/D/R 값은 각 행의 구조화 필드로 저장하며 표면 해시에 넣지 않는다.

해시가 바뀌면 이전 계열 생성물과 같은 동결 집합으로 섞지 않는다.

### 5.3 코어 자동검사

- 결정론 검사: 스키마·축·모드·인접쌍·topic·산업 등 저장 전 규칙
- 보조 AI 비평: `core_quality_v5`, 15축
- 사람 눈검사: 최종 콘텐츠 판단

AI 비평의 `pass`는 사람 검수나 내용 타당화를 대체하지 않는다.

### 5.4 현재 콘텐츠 버전의 집중 검수와 선택적 독립 검토


기본 절차는 **규칙 검사 → 저장된 품질점검 연결 → 미션 최종 검수 자료 준비 → 교수자 최종 확정**이다. 승인 정책은
`focused_v1`이며, 의미·화용 검수 기준과 선택적 모델 프롬프트는 `content_review_v2`를 유지한다.

- 현재 미션의 생성 품질점검과 provenance의 콘텐츠 hash가 일치하고 결과가 완전하면 재사용한다.
  재사용 증거는 `generation_quality`에 저장하며 새 OpenAI 응답이나 독립 검토로 꾸미지 않는다.
  재사용 가능한 기본 결과가 없을 때만 OpenAI 품질점검을 한 번 호출한다.
- 추가 모델 검토는 교수자가 선택하는 선택 절차다. 선택하면 Claude가 동일 원본·기준을 독립 검토하고,
  지적이 있을 때 OpenAI가 그 지적을 판정한다. 기존 결과·기각된 지적은 삭제하지 않는다.
- 교수자는 중대 지적(`fail`)과 `needs_professor` 쟁점에 수정 필요·수정 없이 사용 가능·보류 및 근거를
  저장한다. 이미 교수자 판단이 저장된 항목도 계속 확인한다. 일반 경고는 전문을 보존하고 전체
  수업 사용 판단에 포함하되 모든 경고에 별도 결정을 요구하지 않는다.
- 자동 규칙의 warning은 `needs_professor=true`인 확인 신호로 남긴다. 저장 코어의 경고도 현재
  코어에서 재계산해 포함하며, 화면과 DB가 같은 필수 판단 목록을 사용한다.
- 미션의 문항별 귀속·HSK·해시는 교수자 판단 전에 최종 검수 자료로 한 번 준비하고 구조 검사를
  다시 수행한다. 미귀속 20% 초과는 R32 확인 신호이며 비율만으로 차단하지 않는다. 승인 시
  새 모델 결과로 바꾸지 않고 교수자가 확인한 자료를 그대로 사용한다. 원본 변경 시 새 검수가 필요하다.
- 수정 필요·보류는 승인을 막는다. 전체 사용 근거와 명시적 교수자 확인이 필요하며 AI는 승인하거나
  학생에게 공개하지 않는다. 생성 critic의 중대 지적을 수정 없이 채택할 때 필요한 기존 개별 override는 유지한다.
- 미션은 편성 전, 주차 자료는 편성 후 검수한다. 주차 승인에는 현재 편성 미션의 승인이 필요하다.
  원본·기준이 바뀌면 새 버전을 검사하고, 그 버전의 생성 품질점검을 우선 연결한다. 추가 모델 전수 검토를 반복하지 않는다.
- 승인 경계의 현재 DB 원본 hash, 관리자 권한, 확정된 증거의 불변성, 미션 최종화의 원자적 저장을 유지한다.
  생성 provenance hash는 품질점검 재사용의 근거이며 DB 승인 경계 hash를 대체하지 않는다.
- 체험 감수는 교수자가 필요한 부분을 확인하는 도구다. 기본 승인에 전 문항 체험 기록을 필수화하지 않는다.
  이미 저장된 체험 기록의 미확인·수정 요청·보류는 계속 승인을 막는다.
- 기존 승인 행은 `multimodel_v2` 정책과 증거를 보존한다. 미승인 행은 현재 버전에서 경량 정책으로
  연결할 수 있고, 기존 모델 결과와 교수자 판단을 지우지 않는다. 모델 일치를 내용 타당도나 학습효과로 해석하지 않는다.
- 규칙 오류·호출 실패·실행 중 버전 변경은 보류한다. 성공한 호출의 반복, 자동 재시도, 자동 승인,
  학습자 미편성 콘텐츠의 자동 공개를 하지 않는다. 실제 운영 상태는
  `docs/dev-log/2026-09-06-focused-content-review.md`에 기록한다.

## 6. Full Mission 생성 계약

### 6.1 현행 mission_v5 네이티브 MJT5 계약

승인된 목표 순서는 `첫인상 판단 → 맥락 대비 판단 → 판단하고 고쳐보기 → 이유 찾기 →
여러 초안 비교 → DCT`다. 맥락 대비 판단은 소박한 규칙의 반례를 별도 MJT로 다루며,
현행 소스는 이 순서를 독립 5문항으로 생성·검사·실행한다. 각 새 미션의 최초 진입에는
한 화면의 미션 안내가 선행하고, 장면문에는 관찰 가능한 사실만 사용하며 정답 방향을 누출하지 않는다.

MJT5라는 문항 형식의 다양성만으로 화행의 다차원 학습 범위나 학습효과가 자동으로 충족됐다고
간주하지 않는다. 각 미션의 문항 품질과 진단 근거는 독립적으로 검수한다. 개별 Reason 문항의
주원인을 하나로 제한하는 것은 문항 변별을 위한 규칙이며, 두 미션 사이 coverage 계약을 뜻하지 않는다.

아래 블록은 **현행 `mission_v5` 네이티브 MJT5 구조**다. 과거 4문항 `mission_v5`는
삭제·백필하지 않고 별도 legacy 스키마와 `mpj_response_v1`으로만 읽는다.

```text
1. Scale4
   매우 적절 / 다소 적절 / 다소 부적절 / 매우 부적절
   → 적절/부적절 방향이 맞으면 인정
   → reference_scale_code와의 정도 차이는 참고·수업 토론 자료

2. Judge3
   첫 장면과 다른 사건의 과소·적정·과잉 판단 1회
   → DCT 앵커와 같은 P·D·R에서 맥락 의존성 확인

3. FixChoice 3
   별도 사건의 과소·적정·과잉 판단 1회
   → 수정안 3개 중 이 장면의 권장안 1개 단일 선택
   → 권장안 1개 + 일부 조정이 필요한 경계안 2개

4. Reason 3
   이유 3개 공개 → 가장 큰 이유 1개 선택
   → 확신도 없음

5. MultiJudge 4
   적정 후보 2개 + 조정이 필요한 후보 2개
   → 한 화면에서 적절한 표현 1개와 조정할 표현 1개 선택
   → 제출 뒤 각 초안의 대역 범주와 개별 근거 공개
   → 적정 후보 사이에 유일한 1등을 부여하지 않음

6. DCT
   번역 또는 통역 산출 → 피드백
   → 세 층 모두 통과하면 현재 표현으로 완료 또는 선택적 재표현
   → 수정이 필요하면 원문과 다른 수정안을 작성한 뒤 완료
```

- `scale4`는 독립 첫인상 문항이다. `accepted_scale_codes`는 같은 방향의 두 코드이고, `reference_scale_code`는 그 안의 대표 정도 하나다.
- Scale4는 각 target feature 카탈로그의 `counter_rule_ko`가 경계하는 소박한 규칙에 대한 반례를 우선 배치한다. 특정 PDR에서는 직접성·강도·명료성·책임 범위 등 해당 자원의 다른 대역이 적절할 수 있음을 보여 주어, 더 간접적·길거나 강한 표현을 자동 상향하는 편향을 막는다.
- `judge3`가 DCT 앵커와 같은 P·D·R의 Anchor A를 독립 판단한다. `fix_choice`와 `reason`은
  같은 Anchor A 상황을 공유한다. `fix_choice`는 대역 판단을 잠근 뒤 권장안 1개와 경계안 2개를 공개하고 가장 알맞은 수정안 하나를
  고르게 한다. 복수 선택 배열형 trace는 역사 응답 호환을 위해 유지한다.
- `reason`은 `problem_band_code`로 부적절 대역을 검증한 표현에 대해 이유 3개와 단일
  `accepted_reason_id` 중 하나를 바로 고른다. 학습자 응답 trace에는 선택 이유를 남기며,
  형식적인 최초 적절성 판단은 새로 만들지 않는다. 기존 최초 판단 필드는 과거 응답 읽기 호환용이다.
- `contrast_plan_v1`의 `multi_judge`는 모든 수준에서 적정 2·조정 필요 2의 후보 4개다.
  BEST/WORST 선택은 각 범주에서 하나씩 고르는 방식이며, 같은 범주 안의 숨은 순위를 요구하지 않는다.
  과거 `comparison_role` 기반 문항은 당시 계약으로 읽는다. 프롬프트의 최신 여부만으로
  저장 문항의 후보 의미나 `X→A→A→A→Y→C` 공통 장면을 다른 설계로 재검사하지 않는다.
- 후보 문장을 교체하면 변경 문항의 해설·참고 표현을 최종 후보로 함께 갱신한다. 정답·대역
  메타데이터는 보존하며 참고 표현과 인용의 실제 존재를 검사한다. 갱신 누락은 완료로 처리하지 않는다.
  국소 후보 검사 뒤 완성된 미션 전체를 품질점검하며, 남은 중대 지적은 계속 승인 대상 쟁점으로 남긴다.
- 선택지 수를 수준 난이도로 사용하지 않는다. 난이도는 장면과 표현의 미묘함으로 조절한다.
- DCT는 MJT와 같은 `mission_id`, attempt와 `speech_act`에 속한다. 현행 호환용
  `target_feature`를 함께 보존할 수 있지만 이것을 미션 전체의 단일 학습목표로 해석하지 않는다.
  MJT 완료만으로 미션 완료 처리하지 않는다.
- 현행 v5의 `production_task.mode`는 번역 또는 통역 하나다. 번역·통역 연계는 과목 설정, 주 수행/보조 수행 구분, companion 로그 계약을 먼저 설계한 뒤 별도 버전에서 추가한다.

### 6.2 Reason·맥락·전이 게이트

Reason 단일 선택은 다음 조건에서만 허용한다.

- 문항마다 하나의 주된 화용 판단 원인이 명확하고, 내부 진단 feature와 연결할 수 있다.
- 이유 3개는 `primary` 1, `pragmatic_misconception` 1, `meaning_grammar_context` 1이다.
- 정답은 `primary` 하나뿐이다.
- 오답도 target에 실제로 보이는 표현이나 인접한 화용 쟁점을 근거로 한 그럴듯한 설명이어야 하며, 황당한 문법 주장이나 상황과 무관한 절대 규칙으로 쉽게 제거할 수 있어서는 안 된다.
- primary의 위치와 ID를 고정하지 않고 선택지 순서를 섞는다.
- 두 이유가 동등하게 방어 가능하면 생성 실패 또는 AI 품질검사 fail이다.
- 복수 head와 supportive move 자체를 금지하지 않는다. 다만 이 문항의 가장 큰 판정 원인은 하나여야 한다.

P·D·R 원시 저장 코드는 학생에게 보여 주지 않는다. 현행 학습자 화면은 `speaker_lower` 같은
원시값 대신 `상대 높음·아는 사이·부담 높음`처럼 풀어 쓴 P/D/R 칩을 판단 전에도 명시적 교수
단서로 보여 준다. 상황문 자체에서도 권한·접촉 이력·실제 부담의 구체적 근거를 확인할 수 있어야 한다.

- 현행 native MJT5와 DCT의 `situation_ko`는 140자 이내의 정확히 두 문장이다. 역할 메타데이터를
  나열하지 않고 첫 문장에서 원문 화행의 주체가 누구에게 무엇을 하려는지, 다음 문장에서 접촉
  이력과 상대가 감수할 실제 부담을 자연스럽게 잇는다. 번역·통역 모두 1인칭 훈련 시점 또는
  구체적인 인물 역할로 서술하며 별도의 통역사 역할 소개를 붙이지 않는다.
  기록 목적·즉시 반응 여부 같은 매체 속성을 연구 설명처럼 풀어 쓰거나, 상대의 권리·선택권·의무와
  답안에 포함할 표현 자원 같은 평가 기준을 학생용 상황문에 노출하지 않는다. 역사 장면은 저장값을
  바꾸지 않고 학습자 런타임에서 메타 문장을 제외한 최대 두 문장·140자로 투영한다.
- AI 품질검사의 `scene_underspecified`도 같은 경계를 사용한다. 상대·용건이 없거나 관계·접촉 이력과 실제 부담이 모두 빠져 P·D·R 판단이 실제로 갈릴 때만 보고하며, 문장이 짧거나 기록 목적·즉시 반응 여부가 설명되지 않았다는 이유만으로 결함을 만들지 않는다.
- `relation_ko`의 학습자 표시값은 상대 역할과 학습자와의 관계를 자연스럽게 한 줄로 합친다.
  두 역할의 병렬 나열, 화자 역할, `A → B`, P·D·R 원시 코드·라벨을 이 값에 섞지 않는다.
  별도의 학습자용 P/D/R 칩은 `pdr` 저장값을 풀어 쓴 표시 projection이므로 허용한다.
- 현행 native MJT5의 다섯 MJT와 DCT는 `preceding_turn=null`이다. 거절·반대처럼 앞선 요청·제안·
  의견이 필요한 화행과 감사·칭찬·사과·직접 불만의 선행 사건은 `situation_ko` 안에 요약해 장면을
  self-contained하게 만든다. 역사 native 행의 값도 학습자 런타임에서는 숨기며, legacy MJT4의
  응답 화행만 저장값과 표시 호환을 유지한다.
- legacy 행에 화자와 상대가 함께 저장돼 있으면 러너 UI에서 오른쪽 상대 정보만 추출한다. 저장 원문과 관리자 검수 화면은 바꾸지 않는다.

각 미션 내부의 맥락 배치는 `앵커+대비`다.

- Scale4(MJT1): Anchor A에서 P/D/R 한 축만 바꾼 Contrast X
- Judge3·FixChoice·Reason(MJT2·3·4): DCT와 같은 앵커 PDR과 동일한 Anchor A 상황
- MultiJudge(MJT5): Anchor A에서 P/D/R 한 축만 바꾼 Contrast Y
- DCT: 같은 앵커 PDR의 New Event C로 근접 전이

서버는 MJT2가 생성한 Anchor A의 `situation_ko`·`relation_ko`·channel을 MJT3·4에 고정한다.
따라서 현행 상황 topology는 `X → A → A → A → Y → C`다. X/A/Y/C 사이의 완전 중복은
결정론적으로 차단하고, 의미적 차이·자연성·대비의 충분성은 AI critic과 교수자 검수가 판정한다.
장면 구성 응답은 Anchor의 코어 PDR과 X/Y의 한 축 대비 조합만 허용한다. 생성 후 장면은
그대로 두고 PDR 코드만 덮어쓰는 자동 보정은 하지 않는다. 실패 시 직전 장면과 오류를
함께 전달하여 기존 한 번의 재시도 안에서 다시 구성한다.
기존 두 번째 품질검사는 장면·이유 선택지·피드백·의미 보존을 구분하여 확인한다. 과거 검수
판정과 수리 이력은 새 critic의 콘텐츠 입력에서 제외하며, 후보를 교체한 뒤에는 전체 수정본을
다시 검수한다. 이유 문구의 문자상 중복은 R4로 차단하고 의미상 중복은 critic이 판정한다.

화행 학습 주차에는 같은 `speech_act`·수준·언어방향의 완전한 MJT5+DCT1 미션 두 건을
편성한다. 미션 2는 미션 1의 사실상 복제본이 아닌 새로운 상황이어야 한다. 생성과 검수는 각 미션을
독립적으로 처리하고, 편성은 동일한 상황문을 공백·문장부호만 바꾼 명백한 복제본을 제외한다.
그 밖의 의미상 복제 여부는 교수자가 두 상황을 읽고 확인하며 변화축·pair coverage를 계산하지 않는다.

강좌 수행 유형은 `translation`(번역), `mixed`(통번역 반반), `interpreting`(통역)로 구분한다.
2026-09-10 사용자 확정에 따라 교과목별로 선택하며, 9개 화행 학습 주차와 화행 선택 후의
13주에 번역형은 번역 2개, 통번역 반반은 번역 1개·통역 1개, 통역형은 통역 2개를 편성한다.
시나리오 생성의 수준별 총량·통역 비율은 재료 제작에만 적용하며 이 편성 계약을 변경하지 않는다.
기존 표준 프리셋의 통번역 설정·저장된 배정은 자동 변경하지 않는다. 각 미션은 기존 MJT5+DCT1 생성·
검수 계약을 독립적으로 충족한다. 번역의 입력·출력만 음성으로 바꾸어 통역 미션을 만들지 않으며,
문어 번역과 구어 통역 훈련에 맞는 자연스러운 상황·원문·수행 조건을 구성한다.
번역·통역 전용 강좌는 각 모드의 두 미션을 유지한다. 7·14주 메타화용 토론과 1·8·15주에는 새 미션을
편성하지 않는다. `target_interpreting_week_count`·legacy 비율은 저장 호환용으로 보존하며
현행 배분에 사용하지 않는다. 과거 Scope Lock v1 배치 manifest의 수량·인덱스는 변경하지 않으며,
그 역사적 생성 quota를 현행 강좌 편성 정책이나 새로운 1:1 콘텐츠 확보 근거로 해석하지 않는다.

13주 `선택 화행 집중 보완`은 교강사가 앞선 학급 수행을 참고해 기학습 화행 하나를 선정하는
주차다. 같은 화행·수준·방향의 새 MJT5+DCT1 미션 두 건을 편성한다. 통번역형은 번역·통역
각각 한 건이며, 한 화행 주차의
기존 완전 미션 단위를 유지하며, P·D·R과 상황을 다양하게 구성하고 고부담으로 한정하지 않는다.
화행 선정 전 빈 초안은 허용하되 새 미션 편성은 화행 선택 후 진행한다. 이전의 서로 다른 화행
배정은 자동 변환·삭제하지 않고 교강사가 선택·교체한다.

주차별 `중심 질문`은 수업 안내 전용이며 생성 프롬프트·검수 통과 조건·평가 준거로 전환하지 않는다.
교강사는 상황·P·D·R과 원문의 의도·태도에 따라 질문을 구체화하거나 교체할 수 있다. 특정
완화·선택권·이유·대안이 항상 필요하다고 전제하지 않으며, 원문의 의미와 화행 목적을 보존한다.
주차 표시 명칭은 아홉 화행 모두 두 글자로 통일하되 내부 enum·생성 스키마·진단 태그는 유지한다.

한 주차의 두 DCT만으로 광범위한 전이나 화행 숙달을 주장하지 않는다. 학기 전체 PDR 배치표에서
주차별 조건을 순환하고, 중간·기말 통합 과제에는 P·D·R 중 하나 이상이 달라지는 전이 문항을 둔다.
배치 자체는 전이 학습의 기회이며, 실제 전이와 학습효과는 수행 자료로 별도 검증한다.

### 6.3 미션 모델과 형식

- 미션 승격 모델: `gpt-4o`
- 현재 미션 출력: JSON mode
- 미션 strict JSON Schema: 미구현
- 생성 후 결정론 검사
- 최초 완전 미션은 한 응답에서 생성한다. 결정론 검사 실패 중 허용된 문항·산출 필드만 국소
  repair하며, 허용되지 않은 필드와 기존 정상 문항은 동결한다.
- MJT3 `fix_choice`는 검증된 within 후보 1개를 lower·upper의 공통 기준으로 사용한다. MJT5
  `multi_judge`는 within A·B를 먼저 생성·검증하고 lower는 A, upper는 B와 상대적 최소대조로
  생성한다. 서버가 후보별 `intended_band`·교육적 역할·보존할 의미/발화 의도/화행 기능·조절 축·
  극단화 금지를 먼저 고정하며, 특정 최종 표현은 하드코딩하지 않는다.
- within 후보가 실패하면 경계 후보를 만들지 않고 해당 within 후보만 한 번 재생성한다. 최초
  재생성에서도 같은 의미 결함이 반복되면 중단하며 무한 재시도하지 않는다.
- AI 품질점검이 정확한 MJT3·MJT5 후보 경로를 `band_mismatch` 또는
  `implausible_distractor`로 지목하면 기존 표현을 repair하지 않고 동일 blueprint와 검증된 within
  기준으로 해당 후보만 새로 생성한다. 정상 후보·후보 순서·대역 metadata는 immutable하다.
- 후보 교체 뒤에는 변경 후보 검사와 기존 결정론적 최종 문항 검사만 수행한다. 그 결과는 대상
  후보의 판정만 해소하며, 기존 warning과 비대상 critical fail을 제거하거나 전체 미션을 자동
  pass로 완화하지 않는다. 그 밖의 단순 형식·표현 결함만 candidate-level repair 대상이다.
- critic의 명시적 자기모순만 counter-rule로 보정한다. 경계 판단이 불확실한 경우 warning으로
  남기며 자동 pass로 전환하지 않는다.
- 구조적으로 저장된 `generated` 초안도 current LOCK release·동일 fingerprint·결정론 검사·AI critical
  fail 제외·고유 content hash를 모두 만족해야 500 후보 집계에 포함된다.
- 별도 outline 파이프라인과 mission_v5 구조 변경은 사용하지 않는다.

### 6.4 R5 길이 단서 판정 해석 (2026-08-25 교차검증 개정)

R5는 `multi_judge` 후보의 길이가 목표 화용 판단을 우회하는 응답 단서로 작동하는 것을 통제한다. 판정 수준은 다음과 같이 해석한다.

- **warning(독립 의미 검수 신호)**: 모든 non-within 후보의 길이가 within 후보 구간의 한쪽
  밖에만 분포하는 완전 분리, 과잉안 유일 최장, 과소안 최단 집중, 최장/최단 비율 3 초과.
  길이 패턴만으로 저장을 차단하지 않는다.
- **AI·교수자 판정**: 필수 AI 품질점검은 실제 BEST/WORST 선택을 화용 판단 없이 식별하거나
  현저히 좁힐 수 있는 `answer_cue`인지 문장 내용과 함께 독립 판정한다. AI fail은 저장을 막고,
  warning은 교수자 검수에 남긴다.

2026-07-31의 9화행 파일럿에서 B2 프롬프트 보강(대역과 길이를 별개 축으로 명시하고 탈상관 조합을 요구)은 미션 생성 성공률과 일부 길이 구간 중첩을 개선했다. 다만 화행별 1표본이므로 길이 단서 문제가 일반적으로 해소되었다고 보지 않는다.

길이가 일부 target feature와 자연스럽게 관련될 수 있다는 사실과, 길이가 응답 단서로 작동한다는 사실은 동시에 참일 수 있다. 채점상 within이 아닌 모든 후보가 유효한 WORST이므로(`mpjSummary`의 방향 일치 판정), 학습자가 목표 화용 판단 없이 길이만으로 유효한 BEST/WORST를 식별하거나 선택지를 현저히 좁힐 수 있다면 수정 대상으로 판정한다. 반복 표본에서도 동일 경향이 지속되면 축별 생성 전략 또는 응답 판정 방식의 분화를 재검토한다(현재는 미도입·유보).

### 6.5 결정론적 규칙 R1–R33 현행 인벤토리 (2026-08-25 재감사)

규칙 번호는 연구·운영 기록의 감사 키이므로 의미를 바꾸어 재사용하지 않는다. 실제 판정 정본은
`src/lib/pragma/missionRules.ts`이며, Zod 스키마가 먼저 거부하는 구조 결함은 개별 번호보다
R1로 먼저 보고될 수 있다. 의미 보존·자연성·대역의 실제 타당성은 이 규칙군이 아니라 분리된
AI 품질점검과 교수자 검수가 담당한다.

| ID | 현행 결정론 검사 | 판정·범위 |
|---|---|---|
| R1 / R1c | 미션·코어 스키마, 현행 MJT5 순서, 축·대역 코드, theme/topic 구조 | fail |
| R2 | native `judge3`의 비적정 대역 1개와 DCT 앵커 PDR 일치 | fail |
| R3 | native FixChoice 3안·권장안 1개(legacy 계약은 별도 호환) | fail |
| R4 | Reason의 참조 ID·이유 역할·주원인·앵커 PDR·중복 이유 문구 | fail(legacy 일부 warning) |
| R5 | MultiJudge 역할·대역·중복·한 축 대비 및 길이 완전 분리 | 구조 fail·길이 warning; §6.4 우선 |
| R6 | highlight가 실제 target 부분문자열인지 | fail |
| R7 | Scale4 응답 구간·극성·참고 판정 | fail/warning |
| R8 | native MJT5의 self-contained 장면(`preceding_turn=null`), legacy 응답형 인접쌍 | fail |
| R9 | 코어 상황·관계와 미션 해설·note의 국가 단위 일반화 의심 패턴 | warning; 부정·인용·장소 맥락의 실제 위반 여부는 교수자 판단 |
| R10 | direction·source/target/선행발화 언어 | 명백한 혼입 fail·불확실한 후보 warning |
| R11 | 참고 산출안 1–2개·문항별 권장안 존재 | fail; 현행 스키마가 대부분 먼저 R1로 거부 |
| R12 | 한 방향으로 예측 가능한 accepted 분포 | warning |
| R13 | target feature 코드·version | fail |
| R14 | 학습자 라벨·마무리 원칙의 카탈로그 복사 | fail |
| R15 | target feature의 화행과 요청 화행 일치 | fail; domain/theme은 R1c가 담당 |
| R16 | 요청·실제 payload의 mode/modality와 서면·구두 장면 신호 | 구조 불일치 fail; 장면 정규식은 warning |
| R17 | 산업 메타데이터는 work 도메인에서만 허용 | fail |
| R18 | 교정·이유 문제 문장이 비적정 대역인지 | fail |
| R19 | 한 미션 안의 MJT source·판정 후보 완전 중복 | warning; 배치 중복은 별도 hash/저장 계층 소관 |
| R20 | 미션 provenance 필수값 | fail; 행 바깥 상태 정합은 DB/RPC 소관 |
| R21 | 권장안이 부적절 target 또는 명시적 invalid 교정안과 동일한 기계적 모순 | fail |
| R22 | **retired** — 과거 수준·HSK 휴리스틱 | §8.1 비차단 `hsk_lexical_audit`로 대체, 번호 재사용 금지 |
| R23 | 코어의 source·PDR·modality·direction·`usable_facts` 계승 | fail; DCT 장면은 새 사건이며 channel은 계승축 아님 |
| R24 | 계획 target feature와 생성 feature 일치 | fail |
| R25 | 신규 코어 `context_spec`, 통역 A/B/C·PDR 역할 계약 | fail |
| R26 | work 산업 라벨을 뒷받침하는 제한된 어휘 단서 | warning; 배치·개별 코어 생성은 miss 시 industry AI 검토 1회, 의미 fail/호출 실패는 저장 중단 |
| R27 | 현행 `X→A→A→A→Y→C` 상황 topology, X/A/Y/C 완전 중복, 학습자 장면 2문장·140자 이하 | topology·MJT 형식 fail, DCT 형식 warning |
| R28 | 번역은 email/messenger, 통역은 face-to-face/phone | fail |
| R29 | DCT 유효 글자 범위·focal segment·담화 전체 참고안 경고 | fail/warning; 길이 범위는 §2026-08-02 파일럿 |
| R30 | 코어·MJT·DCT 상황문의 정형 평가 단서 신호 | warning; 공개 정보의 타당성은 교수자 판단, 단어 금지·자동 삭제 규칙은 아님 |
| R31 | 적용 범위 mission_v5의 item lineage 구조·scope·provenance | 구조 fail; covered pack 조건, 미귀속 비율 자체는 차단하지 않음 |
| R32 | `model_unattributed` claim과 20% 참조 상한 초과 | warning; 최종 귀속 결과를 교수자가 확인한 뒤 승인 |
| R33 | 현행 native MJT5의 복수 진단차원·근거 위치 구조 | fail; 선언의 의미 타당성은 AI/사람 검수 |

## 7. 평가와 피드백 경계

### 7.1 분리 원칙

피드백은 최소 다음 층을 구분한다.

1. 의미·의도 충실성
2. 명백한 문법·형식 문제
3. 해당 target feature의 상황 적절성

직접성, 공손성, 의미 충실성을 하나의 총점으로 합치지 않는다. 더 길거나 더 완곡한 표현을 자동으로 더 좋은 답으로 판정하지 않는다.

MJT 5개가 끝나면 직접 산출 전 recap에서 문항마다 실제로 본 목표어 표현 한 조각과 그 표현을
가르는 구체 근거 하나만 `표현 → 메모` 형식의 한 줄로 보여 준다. 새 목표어 모범답안·추상적 교훈·
정답/오답 성적표를 만들지 않으며, MultiJudge도 적정 표현의 핵심 근거 한 점만 남긴다. 런타임 projection은
표현 조각을 최대 18자, 근거 메모를 최대 26자로 제한하고 중국어 조각에만 `Noto Serif SC`를 적용한다.

학습자 피드백 화면은 `내 답 → 의미 전달·문법 정확성·상황 적절성 판정 → 다음 행동`만 우선 노출한다. 세 판정은 상태 아이콘과 `통과/수정 필요`로 결과를 분명히 하고, 통과한 층에는 구체적인 긍정 문구를 제공한다. 세 층이 모두 통과하면 수정을 강제하지 않고 `이 표현으로 완료`를 주 행동으로 제공하며, `다른 표현도 시도해보기`는 선택 행동으로 둔다. 수정이 필요한 경우에만 `한 번 다듬어보기`로 연결하고, 원문과 실질적으로 다른 수정안을 작성해야 완료할 수 있다. 앞뒤 공백만 다른 답은 수정으로 인정하지 않되 내부 공백은 언어별 의미 가능성을 고려해 자동 정규화하지 않는다. 여러 층이 동시에 실패한 한 번의 수정 과업은 `의미 전달 → 이번 미션의 목표 화용 축 → 문법 정확성` 순서로 최종 수정 목표를 정한다. 세 층의 독립 판정과 상세 피드백은 그대로 보존한다. 이는 점수나 절대 정답 선언이 아니며 AI 참고 판정 고지는 유지한다. 판정 근거 전문은 관리자 검수·로그에 남기며 학습자에게 반복해 읽히지 않는다. 공통 피드백 프롬프트는 요청 화행을 기본값으로 삼지 않고, 의미층에서는 사실·조건·핵심 화행 내용만 확인하며 강도·완화·선택권·명료성·범위 등 목표 화용 자원의 변화는 화용층에서 판정한다. 다듬기 화면은 판정 결과에서 도출한 한 줄 수정 행동을 제시하고, 완료 화면은 수정 전·후, 바뀐 부분, 해당 답안에 맞춘 한 줄 조언, 저장 상태와 다음 행동만 남긴다. 판정 결과가 없을 때만 일반 원칙 한 줄을 대체 문구로 사용한다.

DCT 산출 화면에서는 장면 설명과 실제 수행 도구를 한 카드에 붙이지 않는다. 번역은 `지금, 직접 번역할 장면`과 메신저 입력 화면을 분리하고, 통역은 장면 카드와 듣기·녹음 프레임을 분리한다. 두 장면 카드는 고정 최소 높이를 두지 않고 같은 패딩·행간·문장 간격을 사용해 내용 길이에 맞게 줄어든다. 번역에는 산출을 막는 비화용적 내용 어휘·고유명사·전문용어만 정확히 2개 제공하며, 기본 접힘 상태에서 학습자가 요청할 때 한 줄 패널로 펼친다. target feature의 완화 표지, 문장 구조와 완성 답안은 힌트로 제공하지 않는다. 통역에는 첫 수행과 재도전을 포함해 힌트를 제공하지 않는다.

### 7.2 사람 검수

상태 흐름:

```text
core-only
→ generated
→ AI/결정론 검사
→ 사람 검수
→ reviewed
```

- 일반 학습자 공개 최소 조건: `mission_status='reviewed'`
- 연구 분반에서 band를 측정 주장에 사용할 경우: `reviewed`와 별도의 인간 캘리브레이션 상태가 필요하다.
- `generated`는 생성 성공이지 교육적 승인 상태가 아니다.

## 8. provenance와 재현성

코어와 미션은 최소 다음을 식별할 수 있어야 한다.

- model
- provider
- prompt version
- prompt snapshot hash 또는 mission content hash
- generated_at
- generation attempt
- schema version
- language direction

Edge가 계산한 `prompt_snapshot_hash`를 클라이언트가 변형하거나 로컬 코드로 재계산하지 않는다. 프롬프트 정본 화면은 배포 Edge 소스에서 생성된 저장소 스냅샷과 저장 행의 지문을 대조한다.

### 8.1 HSK 3.0 참고 데이터와 lexical audit

HSK 3.0은 PRAGMA 학습 지원 수준의 숙달도 등가표나 인증 기준이 아니다. 생성된 중국어를
검토할 때 사용하는 **누적 어휘 참고 상한**이다.

| PRAGMA 수준 | 중국어 어휘 참고 상한 | 누적 항목 수 |
|---|---:|---:|
| 입문 | HSK 1–4급 | 2,000 |
| 중급 | HSK 1–5급 | 3,600 |
| 고급 | HSK 1–6급 | 5,400 |

공식 PDF→추출 CSV→결정론 파생→연구자 mapping→DB seed의 각 층은 source ID·공식 URL·
SHA-256·변환 version으로 식별한다. 공식 L1/L2/L3 주제와 연구자 코딩은 같은 진실값으로
취급하지 않는다. 원 코더·시점·지침이 확인되지 않은 과거 코딩은
`legacy_imported_unverified`이며 PRAGMA 장면 채택 여부로 자동 변환하지 않는다.

생성 계약은 다음을 강제한다.

- 무작위 HSK 어휘 목록을 prompt에 주입하지 않는다.
- 중→한은 중국어 source를, 한→중은 mission의 중국어 target·참고안을 감사한다.
- 감사 결과는 `hsk_lexical_audit`에 policy/source ID, 참고 상한, token 수, 참고 일치율,
  범위 밖 검토 후보와 상태를 기록한다.
- lookup 실패는 `unavailable`로 기록하고 생성·저장을 막지 않는다.
- 범위 밖 후보는 고유명사·전문용어·분절 단위·상위/사전 밖 어휘일 수 있다. 자동 삭제·
  재작성·승인 차단에 쓰지 않고 사람 검수의 참고 자료로만 사용한다.
- lexical audit는 중국어 자연성·화용 적절성·숙달도를 판정하지 않는다.

§5.4 AI 모델 간 독립 검토를 실행한 경우 검토 결과에 대해서도 최소 provider·model·prompt
version·실행 시각·대상 행의 식별자를 남긴다. 이 기록은 생성 행의 provenance를 덮어쓰지
않고 별도로 보존한다.

### 8.2 교수자 수업자료·메타화용 토론 생성

`teaching-materials`는 원자료 분석·코어·MJT5+DCT1 생성과 별도의 교수자 자료 생성 경로다.
`source_teaching_v2`는 교수자가 확인한 소스 1~6개와 저장된 주차 화행·목표를 입력받는다.
미션 편성 전에도 수업자료 또는 토론자료 초안을 만들 수 있으며, 승인된 편성 미션은 선택 입력이다.
7·14주는 각각 2~6·2~13주 계획의 화행을 가져온다. 다른 학습 주차는 그 주차 화행 범위로 제한한다.
기존 `weekly_teaching_v1` 초안과 미션 기반 생성은 읽기·편집·생성 호환을 유지한다.
1·8·15주는 생성 대상이 아니다. 학습자 개인 응답·가공 집계·학습효과를 생성하지 않는다.

- 생성 전 실제 요청의 프롬프트·원문을 확인한다. 전체 입력 100,000자 초과는 거절하며 절단하지 않는다.
  소스 본문 합계 60,000자, 소스별 제목 160자·출처 500자, 강조 내용 2,000자, 선택 미션 여섯 개까지다.
  기존 v1 추가 본문은 12,000자다. 전체 요청 한도는 두 경로에 공통 적용한다.
- PDF는 브라우저에서 텍스트를 추출한다. 영상·음성·캡처 자료는 확인한 본문과 출처를 텍스트로 입력한다.
  자동 OCR·음성 전사·영상 자막 연동은 포함하지 않는다. 추출 방식·분량·누락 안내와 교수자 확인 상태를 보존하며,
  파일 종류나 출처 입력 자체를 신뢰성 인증으로 설명하지 않는다. 최초 파일은 보관하지 않는다.
- 공용 네 절과 교수자 전용 진행 메모를 구조화하고 각 절의 근거 ID를 보존한다. 새로 변형한 사례는
  수업용 변형 예시로 표시하도록 지시하며, 원문의 사실·인용과 AI 설명의 적합성은 교수자가 확인한다.
  v2의 각 절에는 원문 근거 구절 1~3개를 포함한다. 공백을 정규화해 원문 존재 여부를 검사하지만,
  해당 인용이 설명 전체를 뒷받침한다는 의미 검증을 통과했다는 뜻은 아니다.
- 입력 지문·정확한 모델 요청·응답 ID·반환 모델·프롬프트 버전을 버전별 초안에 기록한다.
  프롬프트 확인 뒤 근거가 바뀌면 호출 전 차단하고, 호출 중 근거·초안 버전이 바뀌면 저장하지 않는다.
- 잘린 응답·거절·구조 오류는 저장하지 않고 자동 재호출·모델 대체를 하지 않는다. 교수자 수정은
  새 버전으로 저장하며, 과거 자료·생성 이력을 덮어쓰지 않는다.
- 공개는 기존 주차 콘텐츠 검수·교수자 승인으로 확정한다. 연결한 미션의 현재 승인과 원본의 일치도
  확인한다. 새 초안이나 근거 변경 후에는 이전 승인을 사용하지 않는다.
  소스만으로 초안을 만드는 기능이 기존 주차 공개 요건을 면제하지 않는다. 화행 학습 주차는 완전한 미션
  두 개의 편성·검토 등 기존 주차 승인 요건을 충족한 뒤 공개한다.
- 원자료 전문·모델 요청·교수자 진행 메모는 관리자 전용이며 학생 유인물·공용 HTML에서 제외한다.
  이 경로는 미션 스키마·정답·DCT 피드백 계약을 바꾸지 않는다.

## 9. 배포·검증 상태는 여기 적지 않는다

Edge 배포 버전, migration 적용 여부, 스모크 결과, 테스트 수, 프롬프트 스냅샷 수·해시는
날짜 종속 사실이다. 이 계약 문서에 적으면 반드시 썩는다.

- 현재 상태의 정본: `docs/research-trail/04_evidence_index.md`와 관련 `docs/dev-log/`
- 시점 고정 증거: `docs/research-trail/`
- 프롬프트·해시 대조: `/admin/prompt-harness`
- 현행 모든 코어·미션은 조건 LOCK 전 개발·테스트 콘텐츠이며, LOCK 뒤 전량 재생산한다
  (`docs/operations/CONTENT_REFRESH_RUNBOOK.md`).

## 10. 운영 금지와 승인 게이트

- 495/500 대량 배치를 임의 실행하지 않는다.
- core hash 변경 전 영향 범위와 새 생성 계열 분리 방식을 보고한다.
- 생성·평가·저장 계약, DB schema, RLS, prompt hash에 영향이 있으면 먼저 범위를 보고한다.
- migration 적용, Edge 배포, Railway 배포는 사용자 승인 없이 실행하지 않는다.
- `generated`를 `reviewed`로 자동 승격하지 않는다.
- AI 모델 간 독립 검토 배치를 임의 실행하지 않는다. 대상 표본·규모·모델을 먼저 보고한다.
- target feature의 학술적 지위를 코드 구현만으로 validated라고 부르지 않는다.
- 라운지 활동은 이 계약의 학습 수행·연구 로그에 포함하지 않는다.

## 11. 관련 정본

- 현재 작업 상태(날짜 종속 사실의 정본): `docs/research-trail/04_evidence_index.md`와 관련 `docs/dev-log/`
- 학습자 구조: `docs/product/PRAGMA_학습자구조_정본.md`
- 관리자 구조: `docs/product/PRAGMA_관리자구조_정본.md`
- 제품·연구 정체성: `docs/research/PRAGMA_PRODUCT_RESEARCH_IDENTITY_2026-07-28.md`
- Can-do 원칙: `docs/research/PRAGMA_CAN_DO_ALIGNMENT_PRINCIPLES_2026-07-28.md`
- 화용론 설계 원칙: `docs/research/PRAGMA_PRAGMATICS_DESIGN_PRINCIPLES_2026-07-27.md`

### 장시간 생성의 결과 보존

선택형 Astra 미션 생성은 제공자 응답 ID와 요청 버전을 보존하고 완료 결과를 비동기로 회수한다.
작업의 `completed`는 생성 본문의 확보만 뜻하며, 기존 규칙·AI 검사·초안 저장·교수자 승인 게이트를
대체하지 않는다. 화면 복귀 시 저장된 결과로 기존 점검을 이어간다. 접수 여부가 불명확한 요청을
자동 재전송하지 않으며, 재개에서도 기존 완료 응답과 호출 이력을 유지한다.
학습설계·미션 스키마·교수자 승인·학습자 공개 조건은 그대로다.
