# 교과목 20칸 미션 승격 결과 · 2026-09-10

run ID `scene_grounding_course_20260910`(18칸) + 앞서 만든 2칸(w2-0·w9-1). 원자료는 Git 미추적 로컬 `.tmp/scene-grounding/`.

**이 문서는 상태표다.** 문항·정답·해설의 내용 검수는 `docs/dev-log/2026-09-10-reason-item-diagnosis.md`의 결론에 따라 **계약 결정 전까지 착수하지 않았다.**

## 상태표

| key | 화행/모드 | 미션 | 품질 | finding |
|---|---|---|---|---|
| w2-0 | 요청/번역 | 있음 | fail | primary_reason_ambiguity |
| w2-1 | 요청/통역 | **없음** | — | 코어 수정 후 승격 대기 |
| w3-0 | 감사/번역 | **없음** | — | 코어 수정 후 승격 대기 |
| w3-1 | 감사/통역 | 있음 | pass | — |
| w4-0 | 칭찬/번역 | **없음** | — | 코어 수정 후 승격 대기 |
| w4-1 | 칭찬/통역 | 있음 | warning | critic_grounding_failure |
| w5-0 | 초대/번역 | 있음 | fail | primary_reason_ambiguity |
| w5-1 | 초대/통역 | **없음** | — | 🔴 코어 보류(연구자 판단) |
| w6-0 | 거절/번역 | 있음 | fail | primary_reason_ambiguity |
| w6-1 | 거절/통역 | **없음** | — | 🔴 코어 보류(연구자 판단) |
| w9-0 | 사과/번역 | 있음 | pass | — |
| w9-1 | 사과/통역 | 있음 | pass | — |
| w10-0 | 제안/번역 | 있음 | pass | — |
| w10-1 | 제안/통역 | 있음 | pass | — |
| w11-0 | 반대/번역 | 있음 | fail | primary_reason_ambiguity |
| w11-1 | 반대/통역 | 있음 | fail | primary_reason_ambiguity |
| w12-0 | 불만/번역 | 있음 | fail | primary_reason_ambiguity |
| w12-1 | 불만/통역 | 있음 | pass | — |
| w13-0 | 요청/번역 | **없음** | — | 🔴 생성 파이프라인 2회 실패 |
| w13-1 | 요청/통역 | 있음 | fail | primary_reason_ambiguity |

집계: 미션 있음 **14** (pass 6 · warning 1 · fail 7) / 없음 **6**.

fail 7건은 **전부 같은 코드**다. 그 코드의 진단과 계약 문제는 별도 문서에 있다. **오늘의 pass/fail을 수용 기준으로 쓸 수 없다**는 것이 그 문서의 결론이며, 통과한 미션에서도 같은 종류의 결함을 확인했다.

## 미션이 없는 6칸

- **w2-1·w3-0·w4-0** — 상황문을 고친 새 코어는 저장돼 있다(`docs/research-trail/evidence/2026-09-10-course-core-revisions/`). 수정 전 코어로 만든 미션 3건은 모두 fail이었고 `.tmp/scene-grounding/superseded-missions/`로 옮겼다. 이유 찾기 계약이 정해진 뒤 승격한다 — 지금 만들면 같은 결함이 반복될 가능성이 높다.
- **w5-1·w6-1** — 코어 자체가 보류. 원인·선택지·시드 문안 후보는 `docs/dev-log/2026-09-10-course-core-review.md`.
- **w13-0(요청·번역·교수 면담 요청)** — 🔴 **새로 확인된 세 번째 계통 문제.** 미션 생성 호출이 두 번 모두 같은 지점에서 실패했다: `최종 후보 해설 갱신 실패: 해설의 인용이 최종 문항의 원문·후보에 없습니다`. 후보 해설을 갱신하는 단계가 최종 문항에 없는 문자열을 인용해 자체 검증에 걸린다. 미션은 저장되지 않았다(`mission_status` 없음). 같은 계열의 경고가 w2-1 1차 승격에서도 `candidate_feedback_refresh_failed`로 나왔다.
  - 두 번 같은 자리에서 실패했으므로 일시적 오류로 보지 않는다. 세 번째 재시도는 하지 않았다.
  - 조사 범위 = `candidate_feedback_refresh` 경로가 어느 문자열을 인용 대상으로 삼는지, 문항 확정 전후로 대상이 바뀌는지. 다른 칸은 통과했으므로 이 칸의 내용이 조건을 건드리는 것으로 보인다.

## 남은 순서

1. 이유 찾기 문항의 오답 정의를 정한다(연구자). 이것이 나머지를 푼다.
2. 정해진 뒤 fail 7건과 미승격 3건을 처리한다.
3. w5-1·w6-1 시드를 정한다(연구자).
4. w13-0의 해설 갱신 실패를 조사한다.
5. 20칸이 갖춰진 뒤 교수자 최종 승인 → 편성 교체 → 교체 후 구 미션 추가 보관.

이 문서의 어떤 항목도 교수자 승인·편성·배포를 뜻하지 않는다.

## 갱신 (2026-09-11)

- w5-1: 승인된 시드로 코어 생성 통과(793a1576). 미션 승격은 이유 찾기 계약 배포 뒤.
- w6-1: 승인된 시드(두 차례 면담)로도 보류. 마지막 두 이유는 P 코드 방향 오독. 응답 화행에서 선행 발화자를 화자로 오인한다는 가설과 선택지는 재감사 문서 5절.
- w13-0: 원인 실측 완료(해설 갱신 모델의 줄임표 인용). microfix안은 재감사 문서 6절. 미구현.
- 미션 14건 Reason 문항 표적 재감사: 재생성 4 · 국소 수정 10 · 경계 2. 계약 배포 전 실행하지 않는다.
- PR #135: 문서 전용이 아니었음을 확인(배포된 엣지 수정 포함). CI 실패 = `scene-grounding-edge.test.mjs`의 옛 `formal` 기대 → `distant`로 정렬. 제목·본문을 실제 범위로 고쳤고 병합하지 않았다.
