# 연구 범위 집중과 문헌 기반 자료 저작 보존

- 사용자 승인: 라운지 제거와 자동 편성의 과도한 강조 축소. 소스 기반 저작 제거에는 이의를 제기했으며,
  AI만 신뢰할 수 없으므로 **문헌·소스 기반 수업자료를 만들고 싶다**는 요구를 명확히 했다.
- 분류: `[단독 진행 적합]`. 핵심 미션·승인·수행 기록·생성 계약을 바꾸지 않는 범위 정리다.
- 시작: `0088a62e`, `codex/admin-menu-label-2026-09-10`. 라운지와 자동 채우기는 별도 커밋으로 구분한다.

## 라운지 제거·소스 기반 저작 보존

- 학습자 메뉴를 수업·기록 두 개로 정리하고 `/learner/lounge/*`를 수업으로 연결했다.
- 라운지 전용 화면·셸·정적 자료·전용 테스트 11개 파일을 제거했다. 제거 전 자료는 Git 이력에 있다.
  수업·미션·수행 기록과 DB는 수정하지 않았다.
- 실제 자료 분석에 남은 `라운지 표현 후보` 문구를 `참고 표현 후보`로 정리했다.
  내부 `expression_resource`와 보관·생성기 전달 조건은 유지한다.
- PDF·텍스트 등 소스 수집, 근거 선택, 인용 대조, 생성·수정·교수자 승인과 기존 저장 자료의 읽기 호환은 보존한다.
  `AdminTeachingStudio`, `TeachingSourceCollection`, `teachingSourceApi`, 소스 생성 프롬프트·서버·DB는 변경하지 않았다.
- 관리자·학습자 정본의 과거 라운지 현행 설명을 정리하고, 소스 기반 저작을 남기는 사용자 보정을 기록했다.
  출처 표시·문자열 인용 검사가 내용 타당성을 보장한다는 주장은 하지 않는다.

## 자동 채우기 표시 정리

- `AdminComposer`의 AI 표기·핵심 자동화 배너·3단계 강조를 제거했다. 현재 조건 요약과
  `미션 자동 채우기` 보조 버튼을 남기고 프리셋·네 축 설정은 `편성 조건 조정`에 접었다.
- 교과목 미선택 시 채우기를 비활성화한다. 부족 미션 안내·주제 확대 선택은 접힌 영역 밖에 유지한다.
- `buildAutomaticAssignments`와 수준·방향·화행·모드 조건, 명시적 편성 저장은 변경하지 않았다.
- 사용자의 검증 최소화 지시에 따라 아래 검사 결과를 재사용하고 기록·커밋 단계에서 추가 검사를 하지 않았다.

## 검증

- 타입 검사·운영용 빌드 통과. 관련 기존 **8파일 49 tests 통과**: 경로·하단 메뉴·자동 편성 규칙·
  소스 기반 및 미션 기반 수업자료·실제 자료 분석. 전체 테스트는 반복하지 않았다.
- localhost에서 과거 라운지 주소 3개가 수업으로 연결되고 라운지 탭이 사라진 것을 확인했다.
  자동 채우기의 접이식 조건·교과목 미선택 시 비활성 버튼과 PC·390px 화면도 확인했다.
- 합성 개발 로그인으로 경로·화면만 확인했다. 실제 운영 로그인·생성·편성 저장을 새로 검증하지 않았다.
- 기존 빌드의 CSS·Browserslist·큰 청크 경고는 남았다. 빌드 메타데이터만 변한 생성 스냅샷 두 파일은 원복했다.
- 근거: `.tmp/research-scope-build.log`, `.tmp/composer-simplified-desktop.png`와 EVD-20260910-04.

## 기록·논문 영향

- DEC-20260910-03·EVD-20260910-04를 갱신했다. 핵심 학습 구인과 수행 계약이 같아 traceability는 유지한다.
- 이번 범위의 사용자 확인 필요 항목은 없다. 운영 반영은 별도 요청 전까지 로컬 커밋 범위다.

1. 수치: 관련 49개·타입·운영용 빌드 통과. 새 운영 배포 없음.
2. 화면: 학습자 메뉴 3→2, 라운지 제외. 자동 채우기 버튼과 접이식 조건으로 편성 화면 경량화.
3. 프롬프트·계약: 변경 없음. 문헌·소스 기반 저작과 교수자 검토를 보존하며 동결본 재발행은 불필요하다.

## 운영 반영 후속

- 사용자가 메뉴명·수업 운영 통합·라운지 제외·자동 채우기 표시 정리의 커밋·푸시·배포를 승인했다.
  대상 커밋은 `34622eca`, `0088a62e`, `287fd30c`, `beee3645`다.
- 최신 main은 기준 `71a9c854`와 같아 기존 로컬 검증을 재사용한다. GitHub 보호 규칙의 요금제 제한을
  고려해 PR CI 성공을 직접 확인한 뒤 병합한다. main·CI·Railway SHA·운영 확인 결과는 이 변경 PR 본문에 기록한다.
- 첨부한 `/admin/batch`의 72·495건 및 243·54셀 표시는 별도 배치 생성 화면의 정리 후보다.
  이번 배포에 배치 생성 동작·수량·프롬프트 변경이나 실제 생성 실행은 포함하지 않는다.
