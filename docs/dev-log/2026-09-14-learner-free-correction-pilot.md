# 대표 1건 자유교정 learner UX 체험

- 범위: 사용자가 승인한 localhost 대표 1건. production-ready 기능·생성기 전면 수정으로 확대하지 않는다. [단독 진행 적합].
- 기준: 원격 main으로 확인했던 `54a868d10ca9fb6cf967c875f4aa3bcddccd2961`. 기존 D checkout `4b1add30`와 미커밋 문서는 수정하지 않았다.
- 작업공간: `C:/Users/cnkr/.codex/worktrees/a980/l2-pragmatic-translator/.tmp/learner-ux`, branch `codex/learner-free-correction-pilot-2026-09-14`.
- 정본 탐색: 앱 `docs/CANONICAL.md`와 논문 `01_정본/00_정본목록.md`의 세 정본 경로가 일치한다. 정본의 기존 reason/공통 장면 계약을 이번 실험안으로 변경하지 않았다.

## 구현

- 기존 실제 learner route `/learner/practice`와 `CanonicalMissionRunner`를 사용한다. 기존 DEV preview 진입에 `?preview=v5&pilot=free-correction`을 붙인 경우에만 수동 fixture를 선택한다. 실제 scenarioId가 있거나 production build이면 이 선택을 적용하지 않는다. Auth/RLS를 수정하지 않았다.
- `학교 × 요청`, 한국어→중국어, 중급 대표 1건. MJT1 팀플 파일 상기 → MJT2 교수 추천서 → MJT3 출석 확인 선택교정 → MJT4 리허설 시간 변경 자유교정 → MJT5 동아리 포스터 후보별 판단 → 요약 → DCT 세미나실 이용 가능 여부 문의.
- 여섯 활동의 사건·원문은 각각 독립이다. 첫 두 문항을 병렬 제시하거나 relation pair를 복원하지 않는다. 문항 번호별 오류 방향이나 후보의 대역 비율을 생성 규칙으로 만들지 않았다.
- 자유교정: 상황/원문/번역 → 초기 판단 → 출발 번역이 채워진 입력란에서 부분 수정 → 제출 → 서로 다른 참고 표현 2개와 해설 → 다음 문항. 비어 있지 않은 입력만 요구하며, 모범답안 일치·맞음/틀림 자동 판정·AI 호출은 없다. 초기 판단도 정오 판정하지 않는다.
- MJT5: 후보 4개를 각각 3대역에 독립 배치한다. 같은 대역 선택과 복수 적절을 허용하며, 제출 뒤 참고 위치·해설을 보여준다.
- DCT는 별도 새 원문에서 빈 입력란으로 시작한다. 이번 fixture에서는 작성된 참고 표현을 확인하고 최종안을 확정한다. 기존 preview의 합성 평가와 실제 모델 평가를 모두 호출하지 않으며, AI를 사용하지 않는 체험임을 표시한다.
- 의미 변경 비권장안을 사용한 경우 원문의 확인/가능 여부와 후보의 변경 요구/수락 전제를 해설에서 구분한다. 이를 실증된 한국어권 학습자 빈번 오류로 주장하지 않는다. 콘텐츠는 교육적으로 구성한 UX 초안이며 최종 품질 승인/동결본이 아니다.
- 최소 저장: 다음 단계로 넘긴 응답·단계와 DCT 최초안/최종안을 전용 `sessionStorage` 키에 보관한다. 새로고침 시 완료 단계만 이어가며, 입력 중 초안·다른 탭/기기·서버 저장·교수자 검토 lifecycle은 구현하지 않았다. 완료 화면에서 자유교정 원문 응답을 펼쳐 볼 수 있다.
- `free_correction`/`spectrum`은 화면 view model의 fixture 지원이다. `mission_v5` Zod 계약, DB migration/validation, 기존 reason 응답 필드, 생성기·critic·evaluator, 승인 콘텐츠·provenance는 변경하지 않았다.

## 변경 파일

- `src/lib/mission/canonicalMissionPreview.ts`: 로컬 화면 타입 2개와 선택적 다음 버튼 문구.
- `src/lib/mission/learnerUxPilot.ts`: 대표 1건의 수동 콘텐츠.
- `src/pages/learner/CanonicalMissionRun.tsx`: 기존 러너에 로컬 흐름·정적 DCT 참고 화면·탭 내 보관 연결.
- `src/pages/learner/CanonicalMissionRun.pilot.test.tsx`: 실제 페이지 컴포넌트의 순차 진행과 외부 호출 부재 검증.
- `scripts/serve-learner-ux-pilot.mjs`: `127.0.0.1:8081` 전용 실행. 클라이언트 초기화에 가짜 로컬 값을 쓰며 실제 `.env`/자격증명을 읽지 않는다.
- 이 dev-log.

## 검증

- `npm run typecheck` 통과.
- 관련 4파일 30 tests 통과: pilot 1, runtime 15, connections 7, canonicalMissionRuntime 7. 기존 live demo의 번역/통역·저장/피드백 흐름 회귀 포함. 실제 외부 호출은 모의 처리한다.
- 새 통합 테스트: 여섯 독립 장면/원문, 빈 자유교정 제출 차단, 참고 표현과 다른 수정안 보존, 초기 판단과 무관한 진행, 제출 전 참고 표현 비노출, 후보별 네 응답, 최소 재진입, 새 DCT 최초안/최종안 분리, 재시작 초기화. fixture 수행에서 mission fetch/AI feedback/DB save/events 호출 0을 확인했다.
- 초기 테스트는 샌드박스의 esbuild 경로 접근 제한으로 시작되지 않았다. 승인된 로컬 실행 후에는 클라이언트 초기화용 URL 미지정으로 3 suite가 수집 단계에서 실패했다. 실제 자격증명을 찾지 않고 가짜 로컬 환경값을 지정해 최종 30/30 통과했다. 앱 동작 결함을 테스트 완화로 숨긴 것이 아니다.
- 브라우저: localhost 실제 learner route에서 안내→MJT1→2→3→4→5→요약→독립 DCT→참고 표현/최종안→완료를 직접 수행했다. 자유교정에 참고 표현과 다른 `明天我下课比较晚，大家方便把彩排从七点推迟到七点半吗？`를 입력하고, 제출 후 그대로 유지되는 것을 확인했다.
- 브라우저 완료 화면 새로고침 뒤 자유교정 답안·DCT 최초안/최종안 유지 확인. 새 탭 브라우저 console error 0. 자유교정 제출 후 화면을 스크린샷으로 시각 확인했다. 전체 운영 E2E, 모바일 전수 검사, 실모델 평가, DB round-trip은 수행하지 않았다.
- `git diff --check` 통과. 기존 D 작업공간의 dirty 목록은 작업 전후 동일하다. 생성계약·research-trail은 사용자 체험/후속 결정 전 반영하지 않는다.

## 실행과 한계

- 실행: 이 작업공간에서 `node scripts/serve-learner-ux-pilot.mjs`.
- URL: `http://127.0.0.1:8081/learner/practice?preview=v5&pilot=free-correction`.
- 이 환경에서는 Vite가 의존성 경로를 읽도록 sandbox 밖 로컬 실행이 필요했다. 서버는 loopback에만 바인딩한다. 실제 Supabase 구성 복구나 Edge 배포는 하지 않았다.
- localhost 체험 범위의 남은 blocker 없음. 교수자 검토, 전체 복원, 생성·승인·편성 연결은 이번 완료 기준이 아니며 자동 후속 작업으로 만들지 않는다. UX 개선 효과·학습효과를 검증한 결과도 아니다.
- 로컬 커밋까지만 진행. push·PR·main 병합·Railway/Edge 배포 없음.

[논문 영향 3줄]
1. 수치: 관련 테스트 30/30 통과. 운영 버전·배포 상태 변화 없음.
2. 화면: localhost 대표 1건의 자유교정·후보별 판단·정적 참고 표현 체험 화면 추가.
3. 프롬프트·계약: 변경 없음. 정본 동기화·동결본 재발행 없음.

## 후속 — 사용자 체험 피드백에 따른 흐름·문항 최소 수정

- 기준 `61f86fc6`, 같은 clean 작업공간·branch에서 수행. [단독 진행 적합]. 사용자가 승인한 대표 미션 수정만 반영했으며 생성·운영 계약으로 확대하지 않았다.
- 도입에서 DCT 장면을 먼저 제시하던 페이지를 이 pilot에서 제외했다. 최초 진입·재시작은 바로 MJT1이며, pilot 진행 표시에서도 미션 안내를 제거했다. 기존 저장 미션의 도입은 유지한다. MJT1과 DCT/피드백 콘텐츠는 직전 커밋의 객체와 동일함을 대조했다.
- MJT2는 4점 전체 적절성 판단으로 변경했다. 제출 후 선택 결과에 관계없이 해설과 `가능한 수정 예시` 두 문장을 보여준다. 예시를 유일한 정답으로 표시하지 않는다. 대표 fixture에는 `조정`이라는 문구가 남지 않는다.
- MJT3는 선행 이분법 없이 바로 수정안 선택으로 시작한다. 안내는 `원문의 뜻을 유지하면서 이 상황에 맞게 고친 표현을 골라보세요.`이며, `가장`·`유일한 최적 번역`을 요구하지 않는다. 적절한 후보는 `가능한 수정안`으로 표시한다. 기존 세 후보/해설을 유지하며 별도 판단을 선택한 것처럼 응답에 기록하지 않는다.
- MJT4도 선행 이분법 없이 출발 번역이 담긴 입력란으로 시작한다. 제출→참고 표현/해설→다음 흐름과 비채점 원칙은 그대로다. 응답에는 실제 수정 텍스트만 보관한다.
- MJT5 두 번째 후보를 `学姐，能否劳烦您发一下社团宣传海报的原文件？我只改一下日期。`로 축약했다. 요청과 이유를 보존하고 과도한 사과 누적을 제거한 결과, 적절한 표현으로 다시 판정하고 해설을 함께 수정했다. 후보별 코드포인트 길이(문장부호 포함)는 28/30/30/24자다. 적절 3개·너무 직접적 1개이며 특정 대역 비율을 맞추려고 방어 가능한 표현을 오답으로 남기지 않는다. 실모델 검수나 교수자 최종 승인으로 해석하지 않는다.
- 로컬 fixture ID/탭 저장 키만 v2로 분리했다. 바뀐 문항의 이전 응답을 새 콘텐츠의 응답으로 재해석하지 않으며, v1 저장 항목은 삭제하지 않는다. production schema·DB·응답 계약 버전은 변경하지 않았다.
- 변경 파일은 화면 view model, 대표 fixture, 공통 러너의 pilot 분기, 기존 pilot 통합 테스트, 이 dev-log 5개다. 새 범용 구성요소·생성기·evaluator·교수자 화면·배포 작업 없음.
- 검증: typecheck 및 기존과 동일한 관련 4파일 30 tests 통과. 선행 질문/도입 비노출, 4점 선택지, 오판을 선택해도 예시 2개 공개, 수정 텍스트 보존·가상 판단 응답 부재, 축약 후보의 적절 참고 판정, 재시작 시 MJT1 진입을 확인했다. 기존 production runner의 번역/통역·완료 연결 회귀도 통과했다.
- 브라우저에서 바로 MJT1 진입, MJT2의 수용 대역 선택 후 가능한 예시 2개 공개, MJT3 직접 선택과 완화 문구/표시, MJT4 직접 작성·비채점 피드백, MJT5 축약/판정 및 요약→DCT 연결을 확인했다. viewport 451px에서 네 후보 카드의 clientWidth/scrollWidth가 모두 350px, 높이가 모두 165px이며 가로 넘침이 없었다. 전체 캡처의 스티칭이 비정상적으로 보인 부분은 DOM 치수로 대조했고 레이아웃 결함으로 해석하지 않았다.
- 계약 정본·research-trail은 이번에도 수정하지 않는다. 대표 UX의 후속 로컬 실험 기록이며 확정 생성계약이나 학습효과의 증거로 승격하지 않는다. 로컬 커밋만 남기고 push·PR·배포 없음.

[논문 영향 3줄]
1. 수치: 관련 30/30 tests, 운영 버전·배포 변화 없음.
2. 화면: pilot 도입·선행 이분법 제거, MJT2 가능한 예시, MJT3 안내, MJT5 후보/해설 수정.
3. 프롬프트·계약: 변경 없음. 동결본 재발행 없음.

## 후속 — 대표 미션의 상황 표시 축소

- 기준 `cacae545`를 완료 checkpoint로 유지한 후속 로컬 변경이다. 같은 작업공간·branch에서 수행했으며 [단독 진행 적합]으로 판단했다.
- 판정: 보완 후 수용. 배지와 중복 설명을 줄이되, 원문에 없는 사전 합의·관계·매체처럼 판단에 필요한 단서는 보존한다. 원문만으로 충분한지 범용 규칙으로 자동 판정하지 않고 대표 1건의 여섯 활동만 대조했다.
- `QuestScaffold`에서 정확히 `LEARNER_UX_PILOT` 객체에만 화면용 맥락 문구를 적용한다. 기존 큰 상황 카드/PDR 배지를 짧은 일반 문장으로 대체했다. MJT3는 원문에 조교·결석 표시·확인 요청이 있어 별도 맥락을 생략한다. 나머지는 원문과 중복되는 일정·용건·이유를 되풀이하지 않고 한 문장만 표시한다. DCT 참고 화면에도 같은 맥락 문구를 사용한다.
- MJT1은 조원이 하기로 한 일임을, MJT2는 수업에서만 뵌 교수님에게 첫 부탁이며 아직 수락 전임을 남긴다. MJT4는 같은 수업의 조원들, MJT5는 알고 지내는 여자 선배, DCT는 처음 연락하는 학생회관 직원이라는 관계와 각 매체를 표시한다.
- 대표 fixture 파일, scenario/PDR 원본, 원문·후보·정답·해설, 저장 키, 학습 순서, DB·schema·생성기·관리자 metadata·provenance는 변경하지 않았다. 다른 저장 미션은 기존 `ContextCard` 경로를 그대로 사용한다.
- 검증: typecheck 통과. 관련 2파일 16 tests(pilot 1 + runtime 15) 통과. 여섯 화면의 원문 보존·전체 상황문/PDR 배지 비노출·필요 맥락 표시와 기존 미션의 배지 유지 회귀를 확인했다. 마지막 MJT1 문구 명확화 후 pilot 1 test만 재실행해 통과했으며 별도 테스트 수로 중복 집계하지 않는다.
- 브라우저: MJT1~5와 DCT의 맥락·원문 영역을 DOM과 화면 캡처로 확인했다. 좁은 화면에서도 문장이 자연스럽게 줄바꿈하고 겹침·잘림이 없었다. MJT3는 원문부터 바로 시작한다. DCT 제출 이후의 완료·저장 경로는 다시 수행하지 않고 기존 증거를 재사용했다. 검증용 탭을 정리하고 첫 문항을 새 체험 탭에 열어뒀다.
- 코드와 기존 테스트, 이 dev-log만 변경한다. 이번에도 로컬 대표 UI 실험이므로 계약 정본·research-trail을 다시 열지 않는다. 로컬 커밋까지만 수행하며 운영 배포는 하지 않는다.

[논문 영향 3줄]
1. 수치: 관련 16/16 tests 통과, 운영 버전·배포 상태 변화 없음.
2. 화면: localhost 대표 미션의 PDR 배지/큰 상황 박스를 숨기고 필요한 맥락만 표시.
3. 프롬프트·계약: 변경 없음. 동결본 재발행 없음.

## 후속 — 현행 피드백·다듬기 화면 재사용

- 기준 `bcea10e2`, 같은 작업공간·branch에서 수행. [단독 진행 적합]. 사용자는 `기존 화면·흐름 재사용, 실제 AI는 미실행`으로 범위를 명시했다.
- pilot 전용 `PilotDctReferenceView`를 제거하고 현행 `DctFeedbackView`를 사용한다. 내 답안 → 우선 확인할 기준 → 그대로 확정 또는 다듬기 → 최종 확정 흐름을 재사용한다. 앞서 승인한 MJT·콘텐츠·상황 표시와 로컬 저장 범위는 유지한다.
- 현행 개발 미리보기 `evaluateDct`는 면접 일정 등 다른 미션의 표현을 검사하므로 이 fixture에서 호출하지 않는다. 새 evaluator도 만들지 않았다. 대표 미션의 기존 `feedback.action`을 작성된 확인 기준으로 표시하며 `available: false`로 취급하고, 화면에 `AI 미실행`과 비평가 안내를 명시한다. 가상 평가·등급·하이라이트·AI 생성 고지·수정 강제·AI 판정 이의 제기를 표시하지 않는다. 이 표시값은 응답의 `evaluation`으로 저장하지 않는다.
- 다듬기는 학습자의 최초안으로 시작하고, 원문·첫 번역을 펼쳐 확인할 수 있다. 이 대표 미션의 펼침에서도 짧은 맥락만 표시하고 PDR 배지와 중복 상황문은 되살리지 않는다. 기존 실제 미션의 피드백 호출·판정·이의 제기·저장 경로는 변경하지 않았다.
- 검증: typecheck, 관련 3파일 23 tests(pilot 1 + runtime 15 + connections 7), `git diff --check` 통과. 기존 수정 권고·이의 제기·그대로 확정·저장 재시도 회귀를 재사용했다. pilot에서는 AI 표시 구분, 가상 판정/외부 호출 부재, 최초안으로 다듬기 시작, 수정안 확정과 최초안·최종안 보존을 확인했다.
- 브라우저에서는 동일 learner route의 DCT 제출 → 현행 피드백 화면 → 다듬기 입력/원문 펼침을 확인했다. 캡처로 두 화면을 확인했고, 마지막에는 검증용 미확정 수정안을 새로고침으로 비우고 피드백 화면을 열어뒀다. 완료 저장은 통합 테스트로 확인했으며 운영 DB·실제 모델 검증은 수행하지 않았다.
- 변경은 공통 러너의 pilot 표시 분기, 기존 pilot 통합 테스트, 이 dev-log 3파일이다. 대표 fixture·DB/schema·생성기·평가 함수·프롬프트·계약·research-trail·운영 배포는 변경하지 않는다. 사용자 확인을 위한 로컬 UI 실험 기록이며 정본 설계나 학습효과 증거로 확대하지 않는다.

[논문 영향 3줄]
1. 수치: 관련 23/23 tests 통과, 운영 배포 변화 없음.
2. 화면: 대표 미션의 DCT 후 현행 피드백·다듬기 화면 재사용, AI 미실행 표시.
3. 프롬프트·계약: 변경 없음. 동결본 재발행 없음.

## 후속 — MJT3 선택지 1곳과 해설 보정

- 기준 `cf28a9ea`, 같은 작업공간·branch. [단독 진행 적합]. 사용자 지시를 수용하고 현행 localhost 구조를 유지한다.
- MJT3의 긴 사과 후보 1개를 `助教您好，不好意思，我上周忘了签到，能帮我查一下记录吗？`로 교체했다. 원문은 결석으로 표시된 기록의 확인을 요청하지만, 이 후보는 확인 요청과 함께 출석 체크를 잊었다는 원인을 사실로 추가한다. 사과나 존칭 자체를 오류로 보지 않는다. `valid: false`를 재확인하고 해당 후보 해설 및 MJT3 종합 해설을 수정했다.
- 후보 길이는 문장부호 포함 코드포인트 기준 기존 26/24/53자에서 26/24/28자로 바뀌었다. 길이 제한이나 새 검사 규칙을 만들지 않았다. 세 후보의 차이는 기록 변경 요구 / 표시된 사실 확인 / 원인 임의 추가다. 원문·상황·출발 번역·질문·다른 두 수정 후보와 기존 가능한 수정안은 그대로다.
- MJT5는 읽기 검토만 했다. A는 요청 뒤 목적 설명, B는 더 격식 있는 부탁, C는 목적을 먼저 밝힌 뒤 편의 확인, D는 질문 없이 파일 전달을 요구하는 표현이다. 정보 순서·격식·요청 방식의 차이를 각각 판단할 수 있다. A/B/C는 주어진 장면에서 모두 방어 가능한 표현으로 유지하며 D는 이 장면에서 전달을 지시하는 듯 들릴 수 있는 후보로 유지한다. 이를 특정 단어·문장형이나 대역 비율의 일반 규칙으로 확장하지 않는다. 네 후보·판정·해설은 모두 변경하지 않았다.
- 의미가 바뀐 후보를 이전 응답과 연결하지 않도록 로컬 fixture ID와 그에 따른 전용 sessionStorage 키만 v3로 구분했다. v2 저장 항목을 삭제·재해석하지 않으며 DB/schema/운영 응답 버전은 변경하지 않았다.
- 검증: typecheck와 기존 pilot 통합 테스트 1건 통과. 수정 후보를 선택하면 원인 추가에 관한 해설이 나오고 가능한 수정안으로 표시되지 않으며, 사실 확인 후보는 가능한 수정안으로 유지됨을 확인했다. 전체 운영 E2E·실제 모델 호출·중국어 외부 독립 감수는 하지 않았다.
- `cf28a9ea`의 fixture 객체와 대조해 MJT3 외 모든 활동, MJT3의 상황·원문·출발 번역·질문, 다른 두 후보가 동일함을 확인했다. 브라우저에서는 수정 전 길이 단서가 줄어든 세 선택지와 선택 후 해설을 확인했다. 실제 학습자가 더 어렵거나 재미있게 느낀다는 효과 검증은 아니다.
- 사용자가 요청한 논문 주장 제한을 `02_decision_log.md`의 DEC-20260914-01에 기록했다. 이유찾기를 복원하거나 판단 이유·메타화용 설명 능력을 측정했다고 과장하지 않는다. 생성계약 정본·다른 research-trail 문서·논문 원고는 수정하지 않았다.
- 변경 파일: 대표 fixture, 기존 pilot 테스트, 이 dev-log, 연구 decision log 4개. 구조·러너·schema·생성기·evaluator·운영 배포 변경 없이 로컬 커밋으로 남긴다.

[논문 영향 3줄]
1. 수치: 대표 통합 테스트 1/1 통과, 로컬 fixture v3, 운영 배포 변화 없음.
2. 화면: MJT3 후보 1개와 관련 해설 수정. MJT5 유지.
3. 프롬프트·계약: 변경 없음. 응답 자료의 연구 해석 제한만 decision log에 기록.
