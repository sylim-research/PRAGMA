# 사과 미션의 사건·책임·원문 불일치 조사 · 2026-09-10

- 요청: 운영 캡처의 초대 거절 사과가 상식에 맞지 않는 원인을 조사하고, 구버전이면 재생성할지 판단.
- 기준: main `d1a97079`와 같은 제품 코드. `[단독 진행 적합]` 읽기 전용 원인 조사.
- 수행: 운영 사과 미션 2건·각 검수 이력 조회, 기존 시드·생성·검수·실행 경로 대조, 현행 순수 규칙 재실행.
  유료 모델 호출, 새 콘텐츠 생성, 운영 데이터 수정, 재배포는 하지 않았다.

## 확인된 콘텐츠 결함

| 대상 | 저장된 사건 | 원문의 결함 |
|---|---|---|
| 번역 `35a5e1a4-150a-413b-93c7-0258464e0efd` | 화자가 이웃을 초대했고 상대가 거절했는데 화자가 사과 | 상황에 없는 `준비가 미흡`을 도입한다. 초대 자체가 어떤 피해를 주었는지 근거 없이 책임을 가정하고, 사과·반성·재발 방지·재사과를 누적한다. |
| 통역 `432ec4e7-93de-46dc-88ee-71a3a54e2932` | 운영자 A가 초대하고 참여자 B가 거절했는데 A가 사과 | A의 원문은 `초대에 응하지 못해`로 시작한다. 거절한 인물이 B에서 A로 뒤집힌다. |

두 건 모두 코어와 DCT의 상황문·원문이 각각 완전히 같다. 코어 이전/생성 단계의 오류가 DCT와
참고 번역으로 이어졌다. 오늘 UI 렌더링이 사건이나 원문을 바꾼 문제가 아니다. 번역 DCT의
어휘 힌트도 `프로젝트 지연`, `약속을 어기다`로 다른 MJT 장면에서 가져왔다.

초대를 거절하는 사람이 반드시 잘못했다거나 반드시 사과해야 한다는 일반 규칙은 만들지 않는다.
초대한 사람도 강권·일정 착오 등 실제 잘못이 있다면 사과할 수 있다. 이번 장면에는 그 사실이 없다.
P=동등·D=아는 사이만으로 말투의 유일한 정답을 정할 수도 없다. 다만 사과에서 R은 실제 침해·피해의
심각도인데, 여기에는 그 근거가 없으므로 `R=mid`와 강한 반성문을 정당화할 수 없다.

## 생성·검수 이력

- 번역 코어 생성: 2026-09-06T07:13:47Z. 통역 코어 생성: 2026-09-04T14:56:43Z.
- 두 건 모두 `core_v15_zhko_bidirectional_roles_v1` / `gpt-4.1-mini`, 미션은
  `mission_v5_mpj5_minidiscourse_v15_zhko_bidirectional_lock` / `gpt-4o`.
- 생성 계열 `pragma_zhko_bidirectional_candidate_20260904_02`.
- 당시 품질검사 `quality_v18_zhko_bidirectional_fidelity` / `gpt-4.1`은 둘 다 warning.
  지적은 MJT5 후보 해설의 비교 구체성이며 위 사건·책임 오류는 지적하지 않았다.
  요약에는 `결함이 없다` / `학습 오도 결함이 없다`고 적혀 있어 실제 내용과 맞지 않는다.
- 번역의 별도 OpenAI/Claude 검토는 null. 통역의 별도 OpenAI 검토는 fail이나,
  주장은 MJT 각 장면을 DCT와 동일한 사건으로 맞추라는 것으로 저장된 공통 Anchor A 설계와
  충돌한다. 이 fail을 이번 원인 검출의 증거로 재해석하지 않는다. Claude·재정은 null.
- 9월 6일 공개 기록은 우선 편성하고 품질 개선을 후속으로 두는 운영상 사용 결정으로 남아 있다.
  기록 자체도 교수자가 문항별 정밀 검토·직접 체험을 완료했다는 뜻이 아니라고 명시한다.
  `mission_status=reviewed`와 내용 타당성 검증 완료를 동일시하면 안 된다.
- 9월 7일 `dcf181b0`·`534350bd`에서 현실성 기준 및 `scene_plausibility` /
  `implausible_scene`가 추가됐다. 이 두 저장 미션에 새 의미 검사를 실행한 이력은 조회 결과 없다.
- `missionRelease.ts:28`의 과거 공개 계열 실행 호환 때문에 오늘도 제공된다.
  새 프롬프트 배포는 과거 콘텐츠의 재생성·재검수가 아니다.

## 현행 코드에도 남은 재발 경로

1. `src/lib/pragma/scenarioTopics.ts:221`에서 `invitation_refusal`은 지금도 refusal와 apology를
   함께 허용하며 시드는 거절 사건 하나다. `batchPlan.ts:86`은 이 허용 목록으로 시드를 고른다.
   사과의 주된 계기와 단순 거절의 부수적 유감 표현을 구별하지 않은 입력이 만들어질 수 있다.
   실제 두 행의 topic 코드·생성 item key도 이 조합이다. 저장 topic 단계부터 잘못된 역할이 있다.
2. 과거 코어의 context_spec에는 화자가 위반·피해를 인정할 책임, 상대가 피해자라는 범용 사과
   전제가 주입돼 있다. 현행 `generate-scenario/index.ts:975` 근처도 화행별 의무 문구는 유지한다.
   그러나 실제 위반이 무엇인지부터 확정하는 구조 필드는 없다. 불완전한 사건에 사과 의무를
   먼저 끼워 넣는 것이 재발 위험이다. 구체적 모델 내부 생성 원인은 저장 자료만으로 확정하지 않는다.
3. 현행 현실성 규칙과 상황·원문 대응 감사축은 이미 존재한다. 다만 일반 `runCoreCell`에서는
   규칙 검사 뒤 R26 산업 warning일 때만 AI 비평을 호출하고 industry 축만 소비한다
   (`coreBatchRun.ts:114`, `:167`, `:330`). 전체 코어 비평은 별도 파일럿 버튼이며 저장 차단과
   분리돼 있다(`coreQualityAudit.ts:140`). 따라서 현실성 검사를 모든 코어가 통과했다고 주장할 수 없다.
   뒤의 미션 품질검사에는 현실성 항목이 있으므로 의미 검사가 전혀 없다는 주장도 틀리다.
4. 원문 2~4문장·권장 길이와 서면 문어체 지시는 장황한 격식 표현을 만들 위험 요인이다.
   하지만 현행 규칙은 관계 관리에 필요한 만큼만 쓰고 사실을 발명하지 말라고도 명시한다.
   분량 강제가 이 원문의 직접 원인이라는 증거는 없다. 금지어 추가나 길이 제한 완화만으로
   사건 불일치를 해결하려 하지 않는다. 최신 생성의 실제 원문을 보고 조정할 항목이다.

## 현행 결정론 규칙으로 재실행

`scripts/manual-checks/apology-plausibility-replay.ts`로 두 저장 미션을 오프라인 검사했다.

- 코어 두 건 모두 `pass`, 위반 0개.
- 미션 두 건 모두 `warning`, fail 0개. 길이 편차(R5)·중복(R19) 신호만 있다.
  공통 Anchor A 중복 경고를 사건 결함 또는 구조 변경 근거로 확대하지 않는다.
- 두 topic 조합 모두 현행 `topicSupportsContext=true`.
- R23은 원문 계승 여부를 확인한다. 잘못된 코어가 그대로 계승돼도 이 검사는 통과한다.
- 이는 구조 검사와 의미 검토가 다르다는 실행 근거다. 새 AI 검수가 같은 오류를 놓친다는
  실험 결과나 전체 콘텐츠 오류율이 아니다. 새 AI 호출·전체 테스트·빌드는 실행하지 않았다.

## 재생성에 대한 제안 — 아직 실행·확정하지 않음

재생성은 합리적이지만 기존 나쁜 시드를 그대로 다시 넣으면 안 된다. 먼저 실제 사건 →
행위자·책임/수혜·영향 → 관계와 화행별 R → 자연스러운 원문을 확인하고 미션을 생성한다.
미션·평가 구조 MJT5+DCT1은 유지한다.

- 지금 반드시 해결: 이 사과 2건의 사건·원문 불일치, 신규 시드의 화행/사건 대응,
  재생성 묶음에서 현실성·역할·상황/원문 대응을 실제로 확인하는 통과 기준.
- 완성 전 해결 권장: 문제 사과 번역·통역 + 요청·거절 등 대표 4개를 새 시드·코어부터
  소량 생성해 사람 검토까지 완료한 뒤, 해당 교과목 20개를 새 초안 묶음으로 제작·검수한다.
  소량 통과가 전체 품질 보증은 아니며 각 새 미션도 검수 후 편성한다.
- 기존 콘텐츠·승인·학습 기록은 보존하고, 새 콘텐츠 승인 뒤 편성 교체한다.
  나쁜 원문을 기준으로 만든 후보·정답·참고 표현·피드백을 원문만 바꿔 재사용하지 않는다.
- 후속 개선: 대규모 상식 판정 시스템이나 모든 규칙 재설계. 이번 발견만으로 모델 교체,
  과목 전체 폐기, 전체 오류율 또는 논문 연구 결과를 확정하지 않는다.

앞선 9월 10일 조사는 평가어 노출과 표시 문제에 집중했고, 동일 자료에 있는 이 사건 불일치를
놓쳤다. 이전 8곳의 수정안을 전체 콘텐츠 타당성 검수 결과로 간주해서는 안 된다.

## 증거·작업 경계

- `C:/PRAGMA_THESIS_LOCAL/05_증거/앱통합검증/2026-09-10_사과개연성/live-apology-evidence.json`:
  운영 시나리오 2건과 검수 이력. 인증값·학습자 답안은 수집하지 않았다.
- 같은 폴더 `structural-replay.json`: 위 두 건의 현행 규칙 실행 결과.
- `EVD-20260910-08`에 연결. 연구 구인·계약을 변경하지 않아 결정/추적성 문서는 갱신하지 않았다.
- 논문 영향: 조회 2건·오프라인 재검사 2건, 새 생성/배포 없음. 현재 사과 화면은 적합한 완성
  콘텐츠의 예시로 쓰기 어렵다. 프롬프트·계약 변경 및 동결본 재발행은 아직 실행하지 않았다.
