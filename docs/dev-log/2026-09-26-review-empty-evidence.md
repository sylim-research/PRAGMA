# 2026-09-26 · 택배 대표 미션 검토를 막는 빈 필드 인용 처리

- 새 택배 후보 `3da0c62d-e91f-4f68-9b74-28cd9d42f044`의 Claude 검토 2회가 근거 인용 불일치로 저장 전 거부됐다. 사용자 전송 승인 후 호출했으며 성공한 검토로 기록하지 않았다.
- 현행 서버는 모델이 고른 실제 필드에서 인용을 복사한다. 빈 문자열 필드를 고르면 `quote=""`를 생성하고 곧바로 검증기가 이를 거부하는 경로를 로컬에서 재현했다. 실패 응답의 원본 모델 출력은 서비스가 보존하지 않아 실제 선택 경로까지는 확인하지 못했다.
- 최소 수정: 빈 문자열·공백 문자열은 인용문 `null`로 표현하며 실제 JSON Pointer는 유지한다. 기존 객체·누락 설명과 동일한 인용 부재 표현이다. 존재하지 않는 경로나 근거 없는 인용 허용은 바꾸지 않는다.
- 로컬 검증: contentReview.test.ts 25건 통과. 빈 필드 2종과 실제 없는 인용 거부를 확인했다. 운영 재실행 결과는 주대표 완료 기록에 연결한다.
- 새 기능·스키마·승인 우회 없음. 다른 미션 콘텐츠 변경 없음.
