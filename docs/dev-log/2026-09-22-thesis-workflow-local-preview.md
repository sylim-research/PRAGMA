# 2026-09-22 · 두 핵심 워크플로우 localhost 시안

- 요청: 논문 프레이밍에 맞춰 학습·콘텐츠 제작 검수를 강조하고 수업 운영을 경량화한 아이디어를 localhost에서 제시.
- 기준: bdb0992efd0601088bc629f6dd64c26110910b6d의 별도 managed worktree. 기존 dirty 루트와 원고는 수정하지 않음.
- 산출물: public/preview/thesis-workflows.html. 앱 루트의 Vite로 제공하는 독립 화면 구성 시안이며 운영 라우트·권한·학습계약은 변경하지 않음.
- 구성: 시작 화면의 학습/제작·검수 두 카드, 학습의 판단→산출→피드백→최종결정, 교수자의 승인/수정/보류, 편성·기록·재검토 세 카드와 접힌 관리 도구.
- 예시 데이터·미리 작성한 피드백만 사용. 화면 내 선택은 메모리 상태이며 실제 AI 요청·DB 조회/저장·승인·편성은 없음. 학습 판단 1개는 시안용 축약이며 정식 MJT 5문항 계약은 유지.
- 실행: 이 작업본에 npm ci로 자체 의존성 설치. npm run dev -- --host 127.0.0.1 --port 8099 --strictPort. HTTP 200 후 in-app browser로 화면을 열었음.
- 최소 확인: 초기 화면 시각 확인, 학습 화면 전환, 최초안 유지 이유 입력→최종 비교 화면, 제작·검수 화면 표시. 기존 테스트 전체 재실행은 하지 않음. npm run build 성공. 기존 앱의 CSS/청크 크기/Browserslist 경고가 출력됨. 런타임 Node 24와 package 요구 Node 22의 설치 경고가 있었으나 설치·빌드·Vite 실행은 성공.
- localhost: http://127.0.0.1:8099/preview/thesis-workflows.html
- 사용자 화면 채택 전의 되돌릴 수 있는 시안. 운영 배포·PR·commit·원고 편집 없음. 학습설계·기능 채택이 아니므로 research-trail 정본은 갱신하지 않음.

[논문 영향 3줄]
1. 수치: 실제 운영/연구 수치 변경 없음. 화면 자료는 예시.
2. 화면: 두 핵심 흐름의 결정권 강조와 운영 메뉴 경량화 제안. 실제 운영 UI 적용 전.
3. 프롬프트·계약: 변경 없음. 공식 동결·승인 상태를 부여하지 않음.

## 기존 앱 최소 UI 변경안 (localhost 검토)

- 배포본 기준 격리 작업공간에서 기존 AdminShell 메뉴를 재배치했다. 제작·검수와 편성·수행기록을 먼저 제시하며 생성 기준·재료·관리 도구는 기본 접힘으로 유지한다. 모든 기존 경로와 화면 폭은 보존했다.
- 기존 대시보드에 두 핵심 워크플로우 안내를 추가하고 제작·검수 화면에 현재 위치를 나타내는 공통 링크를 추가했다. 승인 상태를 표시하거나 변경하지 않는다.
- CompletionRecord에 저장된 reflected 값에 따른 최종 결정 표시와 최초안·최종안 제목을 추가했다. 피드백의 수정·유지 버튼 크기와 외형을 맞추되 활성화 조건과 저장 로직은 유지했다.
- /dev/thesis-ui는 실제 변경 구성요소를 예시 응답으로 보여주는 개발 전용 경로다. 예시는 저장하지 않는다. 실제 대시보드와 검수 화면에도 같은 구성요소가 적용된다.
- 검증: 타입 검사 성공, 기존 메뉴 테스트 4개 및 학습 완료 연결 테스트 7개 성공, 운영 빌드 성공. 첫 실행에서 바뀐 제목을 찾는 테스트 1개가 실패하여 기대 문구만 수정하고 해당 파일을 다시 통과했다. 기존 CSS minify/Browserslist/chunk 경고는 남아 있다. localhost HTTP 200, 시작 화면·학습 완료 표시 확인.
- 배포·커밋·DB 쓰기 없음. 빌드가 생성한 manifest/snapshot 변경은 이 작업공간에 남아 있다.
- research-trail은 미갱신: 표시 변경 검토안이며 학습설계·승인 규칙의 변경 또는 최종 채택이 아니다.

## 메뉴 순서 확정 반영
- 사용자 합의에 따라 콘텐츠 생성 기준 → 학습 미션 재료 → 콘텐츠 제작·검수 → 수업 운영 → 관리 도구로 재배치했다.
- 모든 그룹의 기본 펼침을 복원하고 학습자 관리를 관리 도구의 첫 항목으로 배치했다. 관련 프리뷰 안내 문구도 맞췄다.
- 기존 메뉴 테스트 4개 통과, 운영 빌드 성공. 메뉴 표시만 변경하여 별도 research-trail 갱신은 생략했다.

## 공통 단계 안내 정교화
- 보조 설명 문단 삭제. 생성 계약 → 시나리오 생성 → 학습 미션 제작 → 자동 품질 검수 → AI 검토 → 교수자 최종 승인 → 수업 편성 순서로 표시한다.
- 자동 품질 검수와 AI 검토는 시각적으로 구분하되 같은 기존 검수 화면으로 연결하며, 현재 화면 표시를 공유한다. 시나리오 배치 생성에도 같은 안내를 표시한다.
- 한 줄 배치와 좁은 화면의 가로 스크롤을 사용한다. 미션 식별자는 제작·검수·승인 링크에만 전달한다.
- 코드에서 규칙 검사에 외부 AI 호출이 없고 AI 검토가 OpenAI, 선택적 교차 검토가 Claude임을 확인했다. 저장된 생성 단계 검토 재사용 가능성을 설명했다. 유료 API 실행 없음.
- 운영 빌드 통과. 표시 변경이므로 별도 연구 기록은 갱신하지 않았다.

## 단계 안내를 대시보드로 한정
- 사용자 요청에 따라 전체 단계 안내를 AdminShell에서 제거하고 AdminDashboard 최상단에 한 번만 배치했다. 개별 관리자 화면에는 표시하지 않는다.
- 전체 흐름 안내이므로 현재 단계 강조와 미션 식별자 전달을 제거했다. 개발용 예시의 설명도 변경했다.
- 운영 빌드 통과. 표시 위치 변경이므로 research-trail 갱신은 생략했다.

## 대시보드 추가 안내 전체 제거
- 사용자 캡처에 표시된 전체 단계 안내, 학습자 수행·콘텐츠 제작 검수 카드 두 개, 수업 운영 링크 줄을 대시보드에서 제거했다.
- 기존 운영 지표와 사이드바는 유지했다. 개발 전용 미리보기 구성요소는 유지된다.
- 운영 빌드 통과. 단순 표시 제거이므로 research-trail은 갱신하지 않았다.

## 랜딩페이지 문구 반영
- 기존 두 영역의 레이아웃과 링크는 유지하고 학습자 설명은 판단·직접 통번역·AI 피드백 검토·유지 또는 수정·최종 결정으로 수정했다.
- 교수자 설명은 생성 기준·제작·자동 품질 검사·AI 검토·교수자 승인 및 수업 활용으로 수정했다. 버튼은 콘텐츠 제작·검수하기로 변경했다.
- 운영 빌드 통과. 문구 변경이므로 연구 기록은 별도 갱신하지 않았다.

## 랜딩 설명 축약 및 카드 폭 확대
- 두 카드 설명을 각각 두 문장으로 축약하고 의미 단위 줄바꿈을 넣었다. 학습자 최종 결정과 교수자 최종 승인 표현은 유지했다.
- 본문 최대 폭을 768px에서 896px로 확대해 데스크톱 카드의 가로 공간을 확보했다. 좁은 화면의 반응형 배열은 유지한다.
- 운영 빌드 통과. 문구·폭 조정이므로 연구 기록은 갱신하지 않았다.

## 카드 폭 원복 및 화용적 판단 명시
- 사용자 피드백에 따라 본문 최대 폭을 기존 768px로 원복했다.
- 학습자 설명을 두 줄로 수정: 화용적 판단을 바탕으로 통번역을 산출합니다. / AI 피드백을 검토하고 최종안을 결정합니다.
- 운영 빌드 통과. 문구·폭 수정이므로 research-trail은 갱신하지 않았다.

## 사이드바 대상 용어 정리
- 합의한 구분에 따라 2번 그룹을 시나리오 준비·생성, 3번 그룹을 학습 미션 제작·검수로 수정했다. 생성 기준·수업 운영·관리 도구와 하위 경로는 유지했다.
- 기존 메뉴 테스트 4개 및 운영 빌드 통과. 기존 대상을 명확히 하는 메뉴 문구 수정이며 학습설계 변경은 아니므로 research-trail 갱신은 생략했다.
- 후속 사용자 요청: 2번 그룹명을 시나리오 생성으로 간결화했다. 명칭만 변경했다.

## 배포 확정 범위
- 사용자 배포 요청에 따라 최종 메뉴 명칭 PRAGMA 대시보드를 반영하고 최신 origin/main 기준의 별도 clean worktree에 확정 변경만 옮겼다.
- 포함: 랜딩 문구, 관리자 메뉴 재배치·명칭·기본 펼침, 학습 완료 결정 표시와 유지/수정 버튼 표시. 제외: 폐기한 대시보드 추가 안내, 정적 시안, 개발 전용 프리뷰, 빌드가 변경한 생성 스냅샷.
- 최종 채택 근거는 DEC-20260922-01에 기록하고 관리자 구조 정본의 메뉴 용어를 맞췄다. DB·Edge·프롬프트·승인 조건은 변경하지 않는다.
