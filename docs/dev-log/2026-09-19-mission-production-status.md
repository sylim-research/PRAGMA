# 2026-09-19 「학습 미션 조립」 → 「학습 미션 제작 현황」

## 무엇을 했나
- `/admin/assembly`의 v5 「미션 조립」 버튼·모델 선택·반려·재조립을 제거하고, v6 미션의 제작 경로를 보여 주는 읽기 전용 화면으로 바꿨다.
  - 이유: v6 미션은 화면이 아니라 변환 스크립트로 등록된다(원본 미션 → 변환·집필 → 품질 점검 → 교수자 승인 → 편성). 조립 버튼이 부르는 v5 생성은 9/14 이후 호출 0회였다.
  - 오른쪽: 제작 경로 5단계(검수 이력·편성 조회는 대시보드·점검·승인 화면과 같은 기준) + 「학습 미션 설계」(MJT 문항 1~5 제목, DCT 문항).
  - 목록·상세 5:5, v6만 표시(칩 = 검토 중·승인 완료·전체), 버전 번호·서비스 이름·Trace·고급 필터 제거, 카드에는 현재 단계 한 구절.
- 대체된 옛 판 제외: 다른 행의 `supersedes_scenario_id`가 가리키는 행을 목록·숫자에서 뺀다(`excludeSupersededRows`). 제작 현황·품질 점검·최종 승인·대시보드가 같은 기준을 쓴다. DB는 그대로.
- DB 보관(박사님 실행): 대체 기록 없이 남아 있던 v6 요청 파일럿 초안 중 편성되지 않은 26건(generated 23·reviewed 3)에 `archived_at`, `archive_note = stale_v6_request_pilot_draft_unplaced_20260919`. 되돌리기 SQL과 보관 전 목록은 저장소 밖 `l2-pragmatic-translator-archive/2026-09-19-v6-stale-drafts/`.

## 확인
- 제작 현황: 검토 중 30 → 7, 승인 완료 14 → 11(DB 실측과 일치).
- 대시보드 「교수자 승인 대기」 32 = 최종 승인 화면 「결정 대기」 32.
- `npm run typecheck`, `vitest src/pages/admin src/lib/admin` 92/92.

## 남은 것
- 최종 승인 대기 32에는 v5 미션이 포함된다(v6는 7). v5를 승인 대기열에 계속 둘지는 별도 판단.
- 논문 4.3.2의 「생성 서버가 미션을 구성한다」 서술과 현행 v6 제작 경로의 차이 — 원고 정정은 연구자·Codex 몫.

## 추가 — 자동 품질 점검·AI 검토 화면
- 점검·승인 대기열과 대시보드 검수 수를 v6만으로(v5는 검수 대신 v6 전환). 교수자 승인 대기 32 → 7(세 화면 일치).
- 오른쪽을 세로 타임라인으로 재구성: 규칙 검사 → AI 검토 → 교차 검토(선택) → 의견 대조(선택) → 승인용 최종본 → 교수자 최종 승인. 단계·결과·진행을 한 줄씩, 선택 단계는 늘 표시.
- 실행 버튼: 「자동 점검 실행」(기본 단계), 「교차 검토 실행」(교차 검토 요청 + 의견 대조까지). 실행 중에는 단계 줄에 진행 표시. 상단 공통 추적 박스 제거.
- 이 화면에서 교수자 판단 UI·비용 문구·모델명·내부 코드 제거(최종 승인 화면에 유지). 승인된 미션은 이 화면에서 다시 점검하지 않는다.
- 배치 4.5:5.5.

## 추가 — 교수자 최종 승인 화면
- 의견 대조가 수용·보완한 지적은 수준과 무관하게 교수자 판단 목록에 포함(`professorReviewFindings`). 이전에는 경고 수준·교수자 확인 표시 없음이면 걸러져, AI 두 개가 수정 필요로 합의한 지적을 교수자가 보지 못한 채 승인할 수 있었다. 이미 승인된 미션에는 영향 없음.
- 화면 정리: 머리에 화행·방향·수준·과제 이름표(같은 폭), 「승인 대기 목록」 버튼, 시나리오 전문·참고 판정 일괄 보기·점검 요약 줄·세부 추적 정보·원본 수정 도구·보조 해설 제거, 감수 목록을 꼬리표(도입·MJT 1~5·중간 정리·DCT)+학습자 화면 이름으로, 「수정 필요」, 최종 승인 버튼을 승인 상자 안(브랜드 노랑)으로. 학습자 화면의 MJT1 큰 제목은 감수 화면에서만 숨김(학습자 화면 불변).
