# 15주 강의계획서와 주차 상세의 아코디언 통합

- 상태: `codex/course-mission-menu-2026-09-06`, main `b487e37` 기반 로컬 수정. 운영 배포 전.
- 사용자 결정: 평면 목록의 단조로움을 지적하고 기존 주차 상세의 남색 제목과 미션 카드를 선호했다. 두 화면의 아코디언 통합 및 “요청 화행” 형식의 주차 제목을 승인했다.
- 변경: PRAGMA 남색·노란색을 사용한 교과목 표지와 흰색 15주 목록. 열린 주차는 남색 제목 영역, Can-do 한 줄, 중심 질문 한 문장, 유인물 링크, 짧은 제목의 두 미션 카드와 시작 버튼으로 구성했다. 진행 수치·상태 배지·긴 중심 질문 안내는 제외했다.
- 동작: 첫 화행 학습 주차(표준 과목 2주)를 기본으로 펼친다. 한 주차만 열거나 모두 접을 수 있다. URL week 값과 교과목별 sessionStorage로 선택을 기억한다. 없는 주차 번호는 첫 학습 주차로 복구하고 저장소 사용 실패는 학습을 막지 않는다.
- 연결: 기존 LearnerCourseWeek 상세 화면은 같은 교과목·주차가 열린 통합 화면으로 연결한다. 기존 미션 완료 복귀·유인물 복귀 주소를 보존한다. 학습 미션의 courseId·weekNo·assignmentId와 현행 공개 필터는 유지한다.
- 접근성: 주차 제목의 native button, aria-expanded·aria-controls, 단일 열린 region, 접힌 상세의 비노출, 포커스 표시. 접힌 화면에는 미션 링크를 렌더링하지 않는다.
- 데이터: DB·미션 콘텐츠·검수·생성·평가·수행 저장 계약 변경 없음. 지난 수정의 짧은 메뉴 제목을 재사용한다.

## 검증

- 관련 24 tests (아코디언 6·교과목 투영 9·모드 정책 9) 통과. 마지막 선택의 과목별 유지, 모두 접기, 잘못된 주차 fallback, 비실행 배정 차단, 7/14 토론·13주 미션, 기존 주차 주소 연결을 검사했다.
- typecheck·운영용 build 통과. 테스트 파일의 타입/컴포넌트 동명 import를 별칭으로 바로잡았다. 기존 CSS/Browserslist/청크 크기 경고는 후속 개선으로 유지한다.
- localhost HTTP 200 확인 후 실제 Supabase 데이터로 비즈니스 과목의 15개 주차와 기본 2주 펼침을 확인했다.
- 실제 브라우저에서 4주 선택 시 2주 접힘, Enter로 접기·Space로 펼치기, 통역 미션의 장면 도입 1/3 진입, 브라우저 뒤로 이동 시 week=4 유지 확인.
- 데스크톱과 390×844 모바일에서 열린 주차·미션 두 카드의 배치 확인. 확인 후 임시 viewport는 해제했다.
- 강의 유인물은 기존 승인 후 공개 메시지를 확인했다. 이는 유인물 콘텐츠 승인 완료를 뜻하지 않는다. 미션 응답 제출·완료 저장은 이번 검사 범위에 포함하지 않았다.
- 전체 테스트를 반복하지 않았으며, 직전 전체 결과와 이번 24개 검사를 구분한다.

## 기록

- 학습자 정본의 단일 진입 경로와 주차 표시명을 갱신했다.
- 기존 DEC-20260906-05·ITER-20260906-05에 사용자 피드백에 따른 세 번째 시안을 연결했다. 논문상 관심·사용성 개선은 설계 목적이며 측정된 효과가 아니다.
- 확인 필요: 없음. 운영 반영은 별도 단계다.

[논문 영향 3줄]
1. 수치: 관련 24 tests·typecheck·build 통과. 로컬 미리보기, 운영 미배포.
2. 화면: 15주 강의계획서와 주차 상세 통합, “요청 화행” 주차 제목. 캡처 갱신 대상.
3. 프롬프트·계약: 생성·평가 계약 불변. 학습자 정보구조 정본만 갱신, 생성 동결본 재발행 불필요.

## 후속 시각 조정 · slim and slick

- 사용자 피드백: 아코디언 구성은 유지하되 투박한 인상을 줄인다.
- 표지 높이와 주차 행의 여백을 줄이고 제목 두께·테두리·모서리를 가볍게 조정했다. 교과목 부가 정보는 넓은 화면에서 제목 옆에 배치한다.
- 미션 카드의 여백과 제목 크기를 줄였다. 시작 버튼은 카드 전체 폭에서 문구에 맞는 작은 크기로 바꾸고, 카드 전체를 누르는 동작은 유지했다.
- 검증: 운영용 build 통과. 실제 localhost의 데스크톱·390×844 모바일 배치를 확인했으며 모바일 DOM에서 가로 넘침이 없음을 확인했다. 임시 viewport는 해제했다.
- 동작 변경이 없는 스타일 수정이므로 기능 테스트는 반복하지 않았다. 앞선 아코디언 기능 검증과 구분한다.
- 이번 조정은 시각 표현만 변경하여 research-trail을 추가 갱신하지 않았다. 프롬프트·계약·DB 변경 없음. 로컬 반영, 운영 미배포. 확인 필요 없음.

## 후속 색상 조정 · 네이비 면의 반복 축소

- 사용자 피드백: 상단 바·교과목 표지·열린 주차의 네이비 면이 세 번 반복되어 부담스럽다.
- 교과목 표지의 네이비 박스와 좌측 강조선을 제거하고 바탕 위의 제목·가는 구분선으로 정리했다. 열린 주차는 옅은 노랑 바탕·황금색 강조선·어두운 글자로 바꿨다. 상단 브랜드 바와 작은 미션 시작 버튼의 네이비는 유지했다.
- 검증: localhost HTTP 200 및 실제 데이터 화면에서 밝은 표지와 열린 5주차를 확인했다. 운영용 build는 샌드박스의 경로 읽기 차단으로 첫 실행이 실패했으며 같은 명령을 권한 승인 환경에서 재실행해 통과했다. 기존 CSS·Browserslist·청크 크기 경고는 유지한다.
- 색상·표지 여백만 변경하여 기능 테스트는 반복하지 않았다. 학습자 정본의 색상 설명을 맞췄으며 research-trail은 추가 갱신하지 않았다. 단독 진행 적합. 로컬 반영, 운영 미배포. 확인 필요 없음.

### 표지의 존재감 보완

- 이어진 사용자 피드백: 네이비를 제거한 평면 표지는 너무 밋밋하다.
- 표지를 흰색 커버로 구분하고 상단의 짧은 금색 선, 우측의 은은한 금색 곡선과 색조를 더했다. 제목 크기와 브랜드 행 사이 간격을 조정했다. 넓은 네이비 면은 추가하지 않았다.
- 실제 localhost 화면과 운영용 build 재검증 통과. 학습자 정본의 표지 설명을 현행 화면에 맞췄다. 시각 표현만 변경되어 기능 테스트·research-trail 추가 갱신은 생략했다.

### 표지 정보의 두 줄 통합

- 이어진 사용자 피드백: 브랜드 문구·과목명·우측 정보가 산발적으로 보인다.
- 표지를 `교과목명`과 바로 아래의 `15주 강의계획 · 수준 · 언어방향` 두 줄로 모아 왼쪽 정렬했다. 표지의 중복 PRAGMA·강의계획서 행을 제거하고 상단 브랜드 바에 로고를 집중했다. 흰색·금색 커버는 유지한다.
- 최종 localhost 화면 및 운영용 build 확인 통과. 기능·데이터·계약 변경 없음, 운영 미배포. 확인 필요 없음.

### 교과목명만 남긴 단색 표지

- 사용자 요청에 따라 `15주 강의계획 · 수준 · 언어방향` 문구와 그라데이션·곡선 장식을 제거했다. 낮은 PRAGMA 노랑 단색 헤더에 교과목명만 표시한다.
- 실제 localhost 화면 및 운영용 build 확인 통과. 기존 빌드 경고는 유지한다. 문구·스타일 수정으로 기능 테스트는 반복하지 않았다.
- 학습자 정본의 표지 설명과 이 개발 기록을 갱신했다. 단순 시각 조정으로 research-trail 추가 갱신 없음. 단독 진행 적합. 로컬 반영, 운영 미배포. 확인 필요 없음.

### 사용자 참고 화면에 따른 단순 제목 확정

- 사용자는 헤더 3개 시안을 비교한 뒤 기존 운영 화면처럼 단순한 제목을 요청했다. 제목 박스·색 면·장식·부가 문구를 제거하고 바탕 위에 교과목명만 배치했다.
- 적용 범위는 제목 영역과 다음 소제목까지의 간격이다. 앞서 승인한 주차 아코디언과 미션 카드는 유지했다.
- localhost 실제 화면 및 운영용 build 확인 통과. 스타일 수정으로 기능 테스트는 반복하지 않았다. 학습자 정본의 제목 설명을 맞췄고 research-trail은 추가 갱신하지 않았다. 로컬 반영, 운영 미배포. 확인 필요 없음.

## 사용자 확정 및 배포 전 최종 검증

- 사용자가 현행 화면의 커밋·푸시·배포를 승인했다. 최종 화면은 장식 없는 교과목 제목, 주차 아코디언, 짧은 미션 제목과 직접 시작 버튼이다.
- 최신 origin/main을 fetch해 작업 기준 `b487e37bead25e85ee60bd4395d7f41f95871d63`과 동일함을 확인했다. 이 기준에서 만든 전용 worktree의 변경만 커밋한다.
- 전체 127 files / 771 tests 통과, 3 files / 9 tests skip. typecheck, 운영 소스 정책 5 tests, 로컬 PostgreSQL 승인 경계 10 tests, 운영용 build 통과. 기존 CSS·Browserslist·청크 크기 경고는 유지한다.
- 공개 필터·배정 식별자·인증 경계와 미션·유인물 복귀를 보존한다. 원격 DB·콘텐츠·생성·평가 계약 변경은 없다. 단독 진행 적합.
- 배포는 필수 PR CI 통과 후 main 병합과 GitHub 연동 Railway만 사용한다. 기능 SHA → main 포함 → CI → Railway SHA → 운영 smoke의 완료 증거는 이 변경 PR의 최종 본문에 기록한다. 이 항목의 검증 수치를 운영 배포 성공으로 해석하지 않는다.
