# 수업 운영 메뉴 3개로 통합

- 사용자 결정: 4개 대안 검토 뒤 3개 구성 채택. 삭제 후보의 판단 기준은 개발 편의가 아닌 **연구 가치 대비 부담**으로 명확히 했다.
- 분류: `[단독 진행 적합]`. `34622eca` 이후 같은 로컬 작업공간에서 기존 기능의 진입 경로를 정리했다.
- 수업 운영은 수업 편성·강의계획서 / 주차별 수업 운영 / 학습자 관리의 세 항목이다.
  자료 생성·응답 현황은 주차 운영에서, 백업·복원은 편성에서 연다. 기존 URL·기능은 유지한다.
- 교과목을 백업 화면과 편성 복귀 링크에 넘기고, 주차의 자료·응답 링크는 교과목·주차·미션 ID를 유지한다.
  자료 저작 화면에 주차 운영 복귀를 추가하고 주차 계획 링크를 Composer의 실제 `outline` 매개변수에 맞췄다.
- 모바일·사이드바는 편입된 상세 화면에서도 상위 업무를 선택 상태로 표시한다.

## 검증

- 타입 검사 통과. 기존 관련 3파일 **21 tests 통과**. 전체 테스트·빌드는 반복하지 않았다.
- 로컬 브라우저: 세 메뉴, 제거된 독립 항목, 편성의 백업 진입과 모바일 상위 항목 표시 통과.
  화면은 `.tmp/operations-navigation-desktop.png`에 보존했다.
- 별도 브라우저에는 관리자 로그인 세션이 없어 교과목 목록을 읽지 못했다. 실제 교과목 인계 종단은
  이 브라우저에서 완료하지 않았으며, 주차·미션 링크는 위 컴포넌트 검사로 확인했다. 운영 데이터 변경 없음.
- 로컬 커밋 범위다. 이번 변경의 푸시·배포는 수행하지 않았다.

## 연구 가치 검토 — 제안 단계

- 논문 정본 목록과 `C:/PRAGMA_THESIS_LOCAL/01_정본/00_논문헌장.md`를 대조했다.
  중심은 화용적 적절성 판단과 통번역 산출의 연계, 그 구현·개선과 활용이다. 연구 문제 문구는 지도교수 협의 예정이다.
- 라운지는 이미 논문 범위 밖이다. 별도 부가 학습 모듈과 범용 멀티미디어 자료 저작 확장을 우선 축소 후보로 제안한다.
  교수자 수업 준비 자체나 미션과 연결된 토론의 연구적 의미가 없다는 판정은 아니다.
- 자동 편성은 `buildAutomaticAssignments`를 호출하는 규칙 기반 편의 기능이다. AI의 연구 기여로
  포장하지 않되, 저비용으로 반복 작업을 줄이는 기능까지 연구 독창성이 없다는 이유만으로 삭제하지 않는다.
- 후보별 사용 효과나 실제 유지 비용을 측정한 결과는 아니다. 후보 기능 삭제·학습설계·논문 정본 변경은 미실행이다.

## 기록·논문 영향

- 관리자 정본, DEC-20260910-02, EVD-20260910-03을 갱신했다. 연구 구성개념·생성 계약은 같아 traceability는 유지했다.
- 확인 필요: 연구 가치 기준의 삭제 후보 선택. 메뉴 3개 통합은 승인된 범위다.

1. 수치: 관련 21개·타입 통과. 운영 배포 변경 없음.
2. 화면: 수업 운영 6→3 메뉴, 문맥을 유지하는 상세 진입·복귀 링크.
3. 프롬프트·계약: 변경 없음. 동결본 재발행 불필요.
