# 2026-09-06 · A안 교과목 콘텐츠 후보 확보

- 요청: 논문·수업용 교과목 콘텐츠 확보. 사용자의 검증 최소화 지시에 따라 추가 전수
  AI 검토 dispatcher를 중단하고 생성에 필요한 기존 검사와 마지막 저장 확인에 집중했다.
- 기준: main `a301e7c26816195206d92973b5c24310991be85d`에서 시작한
  `codex/course-content-2026-09-06`. dirty 기본 작업공간은 수정하지 않았다.
- 범위: 3과목 × 9개 화행 주차 × 번역·통역 각 1개 = 54슬롯.
  13주 화행은 수업 중 교수자가 선택한다. 중심 질문은 유연한 수업 안내로 유지했다.
- 최초 현행 release 인벤토리: 코어 50개, 네이티브 MJT5 31개, reviewed 0개.
  과목 조건·기존 검사 결과로 완전 미션 21개를 선택하고 코어만 있는 8개·신규 코어 25개를 계획했다.
- 계획 교차검토: Claude CLI 실제 모델 `claude-opus-4-6[1m]`.
  비즈니스 과목의 일상 후보를 업무 맥락으로 교체했다. 내부 `agreement`를 동의로 해석한
  지적은 기각했다(표시 화행은 초대). 이 검토는 문헌·학습효과 검증이 아니다.
- 실행: 기존 core → MJT5+DCT1 → 규칙·AI 품질점검·bounded repair → generated 저장 경로.
  gpt-4o 생성·gpt-4.1 품질점검 등 기존 모델·프롬프트를 유지했다.
  1차 확보 46개 후 빈 8슬롯에 다른 구체적 사건의 후보를 한 번씩 마련했다.
  원래 실패본과 후보 regeneration 횟수를 지우거나 초기화하지 않았다.
- 시행착오: 대체 카탈로그가 빈 슬롯의 사전 처리 오류로 다른 코어 호출이 중단됐다.
  해당 run/item key의 미저장을 확인한 후 코어만 한 번 재개했다. 업무 불만은 대체 topic이
  없어 같은 허용 topic 안에 납기표 누락·출고 지시서 공유라는 별도 사건을 구체화했다.
- 최종 DB 조회 **2026-09-06T07:20:27.238Z**: 고유 시나리오 54개에 완전 미션 저장.
  과목별 18개, 번역 27개·통역 27개, MJT 270개·DCT 54개, 교수자 승인 0개.
  최초 수업 편성 23행은 값까지 동일하다. 학습자 편성·수행 기록은 쓰지 않았다.
- 품질 경계: 생성 품질점검 fail 7개. 저장된 후속 AI 지적까지 포함한 18개를
  `판정 검토 필요`, 나머지 36개를 `추가 검토 전`으로 표시했다. 36개는 검수 통과가 아니다.
  AI 3단계 완료는 10개이며 교수자 승인은 0개다. 응답 잘림·시간 초과·전체 판정과
  심각도 불일치는 성공으로 간주하지 않았다.
- 관찰: 요청이 초대·제안으로 바뀐 문항, 자연스러운 감사·완화 표현을 부적절로 분류한 문항,
  원문과 무관한 어휘 힌트가 확인됐다. 구조 통과·생성 critic의 warning만으로 내용 타당성을
  주장할 수 없다. 판정·해설 수정과 교수자 결정은 남아 있다.
- 산출물: 원문·MJT5·DCT·해설·저장 검토 결과·앱 검토 링크가 포함된 HTML,
  54슬롯 매핑 Markdown, DB 원본 JSON/SHA-256, 계획·실행·대체 후보·검토 이력.
  `C:/PRAGMA_THESIS_LOCAL/05_증거/교과목콘텐츠/2026-09-06_A안_초안/`에 비공개 보존했다.
- 최소 확인: DB 저장값, 고유 ID 54개, HTML 항목 54개, MJT/DCT 수량, 기존 편성 불변.
  앱 소스·DB 스키마·Edge 프롬프트 변경이 없어 전체 테스트·빌드·재배포는 하지 않았다.
- 기록: ITER-20260906-01, EVD-20260906-03. 현행 release
  `pragma_zhko_bidirectional_candidate_20260904_02`의 교수자 검토 후보이며,
  최종 LOCK 코퍼스·수업 사용 완료·학습효과의 증거가 아니다. raw 콘텐츠는 공개 Git에 넣지 않는다.

## 재현·재개

실행은 정식 의존성이 있는 작업공간과 기존 관리자 환경변수를 사용한다. 비밀 값은 기록하지 않는다.
`.tmp/course-content` 체크포인트를 초기화해서 같은 실패 후보를 자동 재호출하지 않는다.

1. `scripts/snapshot-course-content-inventory.mjs`: 읽기 전용 최초 인벤토리.
2. `scripts/prepare-course-content.ts`: 현행 과목 조건의 54슬롯 계획.
3. `scripts/run-course-content.ts generate`: 체크포인트 기반 신규 생성·저장.
4. `scripts/replace-missing-course-content.ts`: 이번 빈 슬롯의 별도 상황 후보 보완.
5. `scripts/export-course-content.ts`: DB를 읽어 교수자 검토본과 원본 내보내기.

`review`는 기존 검토 단계만 준비하며 승인하지 않는다. 이번에는 사용자 지시로 중단했으므로
재개는 필요한 항목에 한정한다. 생성·대체·검토·내보내기는 각각의 저장 상태를 확인하며 사용한다.

## 논문 영향 3줄

1. 수치: 후보 54개 / MJT 270개 / DCT 54개 / 교수자 승인 0개. 앱 배포 버전 변화 없음.
2. 화면: 운영 UI 변경 없음. 교수자용 비공개 콘텐츠 검토본 신규 생성.
3. 프롬프트·계약: 변경 없음. 최종 동결본 재발행·최종 코퍼스 확정 미수행.
