# 교과목의 주차별 미션 메뉴

- 상태: `codex/course-mission-menu-2026-09-06`, main `b487e37` 기반 로컬 구현. 운영 배포 전.
- 사용자 피드백: 완료 0/2보다 두 미션이 무엇인지 먼저 보여 주고 바로 시작하게 한다. 상태 배지·중복 설명을 없애고 “클리닉”을 바꾼다.
- 변경: 화행 학습 주차에 번역·통역 카드와 저장된 실제 상황 요약 제목을 표시한다. 카드 전체에서 장면 도입으로 진입하며 courseId·weekNo·assignmentId를 전달한다. 완료율 조회와 상태 배지는 목록에서 제거했다.
- 수업 안내: Can-do·중심 질문·유인물은 기존 주차 상세의 보조 경로에 둔다. 1·7·8·14·15주는 간결한 활동 링크로 표시한다. 7·14주는 중간·종합 메타화용 토론이며 과거 DB 제목에도 새 표시명을 적용한다.
- 범위: 기존 60개 미션의 brief_note_ko를 재사용한다. DB·콘텐츠 원문·검수 상태·평가·생성 프롬프트는 수정하지 않았다. 공통 주차 표시명 변경으로 검수 domain 번들을 재생성했다.

## 검증

- typecheck 통과. 관련 21 tests, 최종 전체 768 passed / 9 skipped (127 passed files / 3 skipped).
- 운영용 build 통과. 최초 sandbox esbuild 경로 읽기 오류는 같은 명령의 제한된 승인으로 해결했다. 주차 표시 변경에 따른 stale domain 오류는 정식 review:bundle 생성 후 해결했다.
- 빌드에는 CSS의 `-: T`, 오래된 Browserslist 데이터, 500 kB 초과 청크 경고가 남았다. 이번 미션 메뉴 동작을 막는 오류는 관찰되지 않았다. 범위 확대 없이 후속 개선으로 남긴다.
- 자체 의존성을 설치한 정식 worktree 루트에서 Vite 127.0.0.1:8099 strictPort 실행, HTTP 200 확인 후 브라우저를 열었다.
- 실제 Google 로그인과 운영 Supabase 데이터를 사용했다. 세 과목의 각 20개 미션을 확인했다. 중한·화용 과목 각각 번역 10개/통역 10개 링크, 비즈니스 과목 전체 20개 카드의 실제 제목·귀속 URL 확인.
- 비즈니스 2주 번역 카드 클릭 → 실제 장면 도입 1/3 진입 확인. 학습 응답 제출·완료 저장은 이번 검증에 포함하지 않았다.
- 데스크톱 두 열 및 390×844 모바일 한 열 배치를 화면으로 확인했다. 모바일 크기 임시 설정은 확인 후 해제했다.
- 최초 새 테스트의 접근성 이름 공백 기대값 한 건을 수정했다. 실제 화면의 주차·화행 사이에도 명시적 공백을 넣었다.

## 연구 기록

- 사용자 피드백에 따른 핵심 진입 흐름 변경으로 DEC-20260906-05, ITER-20260906-05에 기록했다.
- 현행 정본의 학습 경로와 표시 용어를 동기화했다. 참여 기대감 증폭은 설계 목적이며 효과 검증 결과가 아니다.
- 확인 필요: 없음. 운영 반영과 실제 학생 사용은 별도 단계다.

[논문 영향 3줄]
1. 수치: 768 tests 통과·9 skip. 로컬 구현, 운영 배포 전. 기존 미션 수 변경 없음.
2. 화면: 교과목 주차 목록이 두 미션 메뉴와 직접 시작 경로로 변경되어 논문 화면 캡처 갱신 대상이다.
3. 프롬프트·계약: 생성 프롬프트·MJT5+DCT1 계약 불변. 정본의 진입 흐름·토론 명칭만 동기화했으며 생성 동결본 재발행은 필요 없다.
