# 교수자 최종 승인 화면 P0 — 결정 대기 기본 보기와 기술 정보 접기

- 날짜: 2026-09-14
- 작업공간: `.worktrees/admin-review-p0-2026-09-14`, branch `claude/admin-review-p0-2026-09-14`
- 커밋: `1ab9fa96` → PR #156 → main `7ce3e6fd` (PR CI·main CI success, Railway production success 2026-09-14T13:56Z)
- 분류: 관리자 UI 정리. 승인 조건·DB·schema·migration·학습자 화면·mission_v6 계약 변경 없음. 연구 설계 변경이 아니므로 research-trail은 갱신하지 않는다.

## 확인된 문제

| 항목 | 이전 | 근거 |
| --- | --- | --- |
| 기본 목록 | 승인 전 `generated` 미션 207개 전체 | 승인 화면 기본 필터(`AdminAssembly.tsx`) |
| 실제 교수자 결정 차례 | 25개(`nextDashboardReviewStage === "professor"`) | 대시보드와 같은 규칙 |
| 규칙 검사 단계 미션을 펼치면 | 승인 화면에 규칙 검사·유료 AI 실행 버튼 노출 | 품질 점검 단계와 경쟁 |
| 같은 제목 미션 | 목록에서 구분 수단 없음 | — |

## 변경

1. 기본 보기 = 결정 대기(25). 검수 진행 중(182)·승인 완료(75)·전체(282)는 보조 칩으로 유지.
2. 목록 행: 교과목/주차(편성 없으면 `편성 전`, 여러 편성이면 가장 이른 주차 + `외 N곳`), Mission v5/v6, 검수 진행 상태, 교수자 결정 상태, 수정 시각, `Trace` 7자리. 편성 정보는 batch read-only 조회(N+1 없음).
3. 펼친 화면 순서 = ① 내용 확인 → ② 판정 → ③ 교수자 최종 승인. 원본 수정·추가 모델 검토·세부 추적 정보·고급 필터는 접기.
4. 규칙 검사 단계 미션은 승인 화면에서 실행 버튼 대신 `품질 점검 화면에서 진행 →` 링크.
5. 세부 추적 정보로 이동(삭제 없음): 미션 콘텐츠 해시·검수 버전 해시·원본 해시 전체·검수 run ID·검수 기준·승인 정책·모델명·검사 시각·콘텐츠 원본 JSON·승인 이력 hash.

## 불변

승인 조건(근거 10자 이상·확인 체크·문제 항목별 판단 저장·생성 AI 결함 근거), 승인 상태 semantics, mission_v6 승인/저장 로직, DB·schema·migration, 학습자 화면, 메뉴 구조(조립·품질 점검·교수자 최종 승인 3단계 분리 유지).

## 검증

- `npm run typecheck` 통과, 변경 파일 eslint 오류 없음.
- 신규 테스트 7개 포함 142개 파일 통과·3개 skip, 947 tests pass. production build 성공(생성 파일은 되돌림).
- 운영 데이터 read-only 로컬 브라우저 확인: 결정 대기 25·진행 중 182·승인 완료 75, 콘솔 오류 없음.

## 남은 항목(blocker 아님)

- 당시 결정 대기 25건은 모두 v5이고 모두 `편성 전`. v6 승인 대기 존재 여부는 v6 정상 경로 E2E가 먼저 검증한다.
- 같은 제목 미션의 실제 중복 여부 미조사.
- 여러 편성 시 가장 이른 주차를 대표로 표시하는 규칙은 현행 유지.
