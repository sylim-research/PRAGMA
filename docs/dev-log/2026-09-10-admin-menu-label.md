# 시나리오 개별 생성 메뉴명 복원

- 요청: 메뉴의 `시나리오 생성`을 `시나리오 개별 생성`으로 변경.
- 분류: `[단독 진행 적합]`. 배포된 main `71a9c854`에서 새 로컬 브랜치 `codex/admin-menu-label-2026-09-10`을 만들었다.
- 공용 내비게이션 원본과 관리자 구조 정본의 화면명만 수정했다. 경로·동작은 동일하다.
- 검증: localhost 8099의 실제 브라우저에서 링크 문구와 `/admin/generator` 경로를 확인했다.
  문자열 수정에 전체 테스트·빌드를 반복하지 않았다. 로컬 커밋 범위이며 이번 변경의 푸시·배포는 수행하지 않았다.
- 수업 운영 메뉴는 코드 조사만 했다. 주차 운영 화면의 자료 생성·응답 현황 연결을 활용하고,
  백업·복원을 편성의 관리 기능으로 옮겨 상시 메뉴를 6개에서 3개로 줄이는 방안을 제안한다.
  통합은 미채택·미구현이며 소스 기반 자료 저작, 응답 집계·공개, 백업·복원 기능을 삭제하는 제안이 아니다.
- 단순 명칭 복원이며 구조 변경 제안은 미채택이므로 research-trail은 갱신하지 않았다.
- 확인 필요: 수업 운영 통합안의 채택 여부. 메뉴명 변경에는 없음.

## 논문 영향 3줄

1. 수치: 변경 없음. 새 운영 배포 없음.
2. 화면: 생성 메뉴명이 `시나리오 개별 생성`으로 변경됐다.
3. 프롬프트·계약: 변경 없음. 동결본 재발행 불필요.
