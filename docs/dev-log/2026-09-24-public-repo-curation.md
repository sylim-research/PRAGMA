# 2026-09-24 · 공개 저장소 안내 정비

- 요청: 공개 저장소를 처음 보는 독자가 문서 구성과 논문 서술 대상 코드를 바로 찾을 수 있게 정리.
- 범위: `origin/main` 20aa20ec에서 worktree·branch `claude/repo-curation-2026-09-24`. 문서와 패키지 메타데이터만 변경했다. 앱 코드·DB·프롬프트·계약은 변경하지 않았다.
- 변경
  - README: CI 배지 추가. 「논문과 코드의 대응」 절을 신설해 학위논문 제4장 각 절과 부록 A·C를 해당 코드 경로에 연결했다.
  - `docs/README.md` 신설: 현행 정본 3종과 설계·개발 기록 폴더를 구분하고 폴더마다 지위(정본·기록·증거·역사 자료)를 표시했다.
  - `docs/handoff/`·`docs/prototypes/`에 「역사 자료, 현재 구현 근거 아님」 안내 README 추가.
  - `package.json`·`package-lock.json`의 패키지 이름을 초기 템플릿 기본값에서 `pragma`로, 버전을 `0.0.0`에서 `1.0.0`으로 바꿨다. 이 값을 읽는 코드는 없다(검색 확인).
- 보존: 기존 dev-log·research-trail·handoff 문서는 이동·삭제·개명하지 않았다. 논문과 증거 색인이 경로를 인용하기 때문이다.
- 원격 브랜치 정리: 미머지 원격 브랜치 4개는 로컬 태그 `archive/remote-branch/*`와 git bundle로 보존했다(저장소 밖 보존 폴더). 브랜치 삭제는 별도 실행.
- 검증: 독립 `npm ci`(잠금 파일 일치) 성공, `npm run typecheck` 성공. 코드 변경이 없어 표적 테스트는 실행하지 않았고 전체 검사는 CI에 맡긴다.

[논문 영향 3줄]
1. 수치: 변경 없음.
2. 화면: 변경 없음(배포본 화면 동일).
3. 프롬프트·계약: 변경 없음.
