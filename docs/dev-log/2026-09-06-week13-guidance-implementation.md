# 13주 선택 화행 집중 보완·중심 질문 앱 반영

- 날짜: 2026-09-06
- 기준: 최신 원격을 조회한 `origin/main b6da0b6`, clean worktree의 `codex/week13-reinforcement-2026-09-06`.
- 단독 진행 적합: 사용자가 채택한 A안·두 글자 화행명·유연한 중심 질문의 국소 구현. 새 DB schema·평가축·모델 프롬프트 변경 없음.
- 설계 근거: `C:/PRAGMA_THESIS_LOCAL/08_작업관리/reviews/2026-09-06_학술문헌기반_주차계획과13주_검토안.md`; `DEC-20260906-01`.

## 변경과 이유

- 공통 15주 표의 화행명을 요청·감사·칭찬·초대·거절·사과·제안·반대·불만으로 통일했다. 내부 화행 식별자와 의미 범위는 유지한다.
- 13주는 ‘선택 화행 집중 보완’이다. 편성 행의 ‘화행 선택’이 해당 주차 설정을 바로 열고, 앞선 정규 주차의 화행 중 하나를 고르게 한다. 선택 저장 시 기존 주차의 화행·제목·미션 수 2를 반영한다.
- 자동 편성·수동 후보·공통 저장 검사에 선택한 화행을 연결했다. 화행 미선정·기학습 범위 밖 화행·다른 화행의 배정·세 번째 미션을 제한한다. P·D·R이나 특정 전략의 추가 조건은 없다.
- 과거의 서로 다른 화행 배정은 삭제·자동 변환하지 않는다. 13주 화행 선택 전 또는 선택한 화행과 불일치하는 미션은 학습 실행 목록에서 제외하고 교강사가 기존 편성을 교체하게 한다.
- 편성·주차 설정·강의계획서·주차 자료 선택·학습자 계획과 상세·유인물에 공통 표시를 연결했다. 중심 질문은 수업에서 맥락에 맞춰 조정·교체하는 대표 질문이며 별도 저장 필드나 생성·검수 통과 조건이 아니다.
- 학습계획에서 첫 미션의 내부 진단 태그를 주차 목표로 쓰던 표시를 제거했다. 반복된 화행을 ‘경험한 화행’에서 중복 집계하지 않는다.
- 주차 자료를 구성하는 공유 Edge 번들을 공식 생성기로 동기화했다. 검수 prompt/criteria·자동 승인 경계는 바꾸지 않았다. 바뀐 주차 자료는 기존 내용 해시 기반 재검토 경로를 따른다.
- `docs/CANONICAL.md`와 생성·학습자·관리자 정본, 결정 기록·설계 추적을 동기화했다. 날짜별 동결본 재발행은 하지 않았다.

## 검증

- 대상 회귀 5 files / 45 tests 통과.
- 최종 전체 회귀 123 files / 739 tests 통과, 기존 외부 생성·원격 의존 3 files / 9 tests skip.
- 최종 typecheck 통과. 변경한 앱 소스 ESLint 오류 0; 기존 AdminTeachingMaterials effect cleanup 경고 1 유지.
- 운영용 `npm.cmd run build` 통과(1,973 modules). 기존 CSS·큰 청크·Browserslist 경고는 유지됐다.
- prebuild의 공유 검수 번들 검증 통과. 생성 프롬프트 스냅샷·pack manifest의 커밋/시각 메타데이터만 변한 파일은 이 변경에서 제외했다.
- Edge headless 브라우저: 화행 미선정 → 선택·주차 저장 → 같은 화행 미션 두 개 편성 → 저장·새로고침 → 강의계획서 → 학습자 계획·13주 상세·미션 링크 확인. 화행 수는 9이며 13주 반복으로 10이 되지 않는다.
- 390px 모바일에서 가로 넘침 없음, 페이지 오류 0. 편성·강의계획서·학습자 상세 스크린샷을 직접 확인했다.
- 재현: Vite를 프로젝트 루트에서 `VITE_SUPABASE_URL=https://pragma-week13.test`, 임의 테스트 키로 실행하고 `node scripts/manual-checks/week13-smoke.mjs`. 브라우저 요청은 메모리 내 HTTP fixture로만 처리하며 실제 DB 쓰기·로그인 검증이 아니다. 결과·캡처는 로컬 `tmp/week13-smoke/`.
- 최초 전체 검사에서 중심 질문을 유인물 맨 앞에 넣어 기존 첫 학습목표 표시 검사 두 건이 실패했다. 기존 목표 순서를 보존하고 중심 질문을 바로 다음에 두어 재검증했다.
- Windows 샌드박스가 esbuild의 경로 읽기를 차단한 실행은 해당 명령을 좁게 승인받아 재실행했다. Vite 파일 보안 설정을 완화하지 않았다.

## 적용 경계

- 운영 DB·교과목 편성·학습 수행자료·콘텐츠 승인·Edge·Railway는 이번에 변경하지 않았다.
- main 반영 전 로컬 구현·검증이다. 운영 반영에는 PR/CI/main 통합과 현재 공유 content-review Edge 번들 동기화가 필요하다.
- 실제 수업의 13주 대상 화행은 교강사가 학급 기록을 보고 정한다. 시스템이 자동 선택하지 않는다.
- 주차 내 번역·통역 각 한 건 병행은 별도 미결정 사항이다. 이번 구현은 기존 동일 모드 정책을 유지한다.
