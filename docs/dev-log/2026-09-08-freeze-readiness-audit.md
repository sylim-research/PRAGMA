# 2026-09-08 · 동결 전 교과목·모델·개발 규칙 점검

- 범위: 사용자 질문에 대한 읽기 전용 조사. 기준 소스 `8bc53880`, 작업공간 `learner-e2e-2026-09-08`.
- 기존 관리자 인증으로 콘텐츠·편성·검수 이력과 `content-review inspect`를 조회했다. 유료 모델 호출,
  콘텐츠·승인·편성·학생 기록 쓰기, 모델 전환·배포는 하지 않았다. 연구자 HWPX도 수정하지 않았다.

## 실제 교과목 상태 · 2026-09-08 13:10 KST

| 교과목 | 편성 | 번역/통역 | 고유 미션 |
|---|---:|---:|---:|
| AI 한중 화용 통번역 | 20 | 10/10 | 20 |
| AI 한중 비즈니스 통번역 | 20 | 10/10 | 20 |
| AI 중한 실전 통번역 | 20 | 10/10 | 20 |

- 총 60개 고유 미션에 MJT 5개와 `production_task`가 존재한다. 편성 주차는 2~6·9~13주이며
  13주의 선택 화행은 현재 세 과목 모두 요청이다. 7·14주 토론은 새 미션 분모에 넣지 않는다.
- 9월 6일 검토본의 54개는 모두 계획한 슬롯에 편성됐고 DB `mission_status=reviewed`다.
  따라서 당시 초안 기록의 승인 0·편성 23건을 현재 상태로 재사용하면 안 된다.
- 그러나 현재 `inspect`에서는 54개 전부 R5·R27 fail, 현재 hash와 일치하는 검수 run 0개였다.
  대표 번역·통역 2건에는 9월 6일의 실제 승인 이력이 남아 있다. 과거 승인 자체가 없다는 뜻은 아니다.
- 세 교과목의 `weekly_material` 승인 행은 0개다. 미션 편성 수만으로 수업자료 확정·최종 LOCK 완료를 주장하지 않는다.
- 9월 6일 교과목 후보 release에는 코어 행 88개, 본문을 갖춘 미션 70개가 있다. 이는 현재 생성기
  release `pragma_content_consistency_candidate_20260907_02`의 수량이나 Astra 최종 코퍼스 수량이 아니다.

## 확인한 버전 판정 문제

54개 저장 미션의 프롬프트는 `mission_v5_mpj5_minidiscourse_v15_zhko_bidirectional_lock`이다.
`missionRules.ts:520`의 현재 네이티브 판정은 단일 최신 프롬프트와의 동등 비교이고, 후보 대역
호환 예외는 v8뿐이다. 이 때문에 v15의 적정 2·조정 필요 2 후보가 BEST 1·중간 2·WORST 1
분기로 들어간다. 공통 Anchor를 공유하는 MJT2·3·4도 v4 중복 금지 분기로 들어가 R27 fail이 된다.
현재 검사 실패 54개를 모두 내용 결함 54개로 해석하거나 이를 이유로 전량 재생성하지 않는다.
기존 승인과 현재 승인 hash의 차이는 실제 콘텐츠 변화와 검수 기준 변화를 구분해 다뤄야 한다.

## 어제 최종 모델 결정

- 실제 사용자 대화에서 **Astra medium + 축약 전 기존 프롬프트 + 비동기 생성** 채택을 확인했다.
  이어 운영 기본값 전환을 나중으로 미룬 사용자 지시도 확인했다. High·축약 실험안은 미채택이다.
- 독립 검수는 Opus 기본, Fable은 어려운 쟁점에 선택적으로 사용한다는 인계 방향이다.
- 현재 운영 `inspect` 모델은 OpenAI `gpt-4.1`, Claude `claude-opus-5`다. main 소스의 기본
  코어/미션/피드백은 각각 `gpt-4.1-mini`/`gpt-4o`/`gpt-4.1-mini`; Astra 생성은 선택 경로다.
- 근거: Codex task `01a079b7-8faf-71a3-8ba7-d66f23687480`의 사용자 turn
  `01a07c56-089b-7470-a538-4761e301f7b4`와 `01a07c56-bdea-7090-97c8-6c71554ba211`.
  보존 worktree `../astra-background-generation-2026-09-07/docs/handoff/2026-09-08-session-wrap-up.md`.
  초기 모델 파일럿 문서만 읽으면 이후 채택 결정을 놓치므로 최종 대화와 함께 확인한다.

## 동결 전 조치안 · 이번에는 규칙·코드 변경 없음

1. **지금 반드시 해결:** 생성 버전 계열에 맞는 검사·러너 호환을 유지하고, MJT5의 정본·후보 데이터·
   BEST/WORST 화면 의미를 대조한다. 단순 프롬프트 갱신이 과거 문항을 다른 설계로 재해석하지 않게 한다.
2. **지금 반드시 해결:** 후보 수리 시 연결된 해설·정답·인용도 함께 갱신하고 완성본을 검사한다.
   Astra 저장본 비교에서 후보 교체 후 옛 해설이 남은 3미션·5문항이 기록됐다. 이 비교는
   최종 의미 검사·교수자 승인 전 산출물이며, 운영 최종 검수가 결함을 통과시켰다는 증거는 아니다.
3. **완성 전 해결 권장:** 확정 모델 결정과 기본값 적용 상태, 후보 수량과 최종 LOCK 수량을 따로 기록한다.
   현재 관리자 정본의 필수 5단계 요약과 본문의 선택형 `focused_v1` 절차, 대외 DCT 용어를 정렬한다.
4. **후속 개선:** 추가 High/Fable 전수 비교·프롬프트 축약 최적화는 보류한다. 기존 품질점검 재사용과
   필요한 쟁점의 선택 검수 원칙을 유지한다. 최종 생성의 수량·release와 실제 전환 시점은 별도 실행 범위다.

## 검증·기록

- 실제 DB 페이지 조회, 대상 54개 읽기 전용 inspect, 대표 2건 승인 이력, 고정 main 코드와
  어제 사용자 대화를 대조했다. 초기 집계의 `dct`/`interpreting` 필드 가정은 실제
  `production_task`/`stt_interpreting`에 맞춰 정정하고 다시 집계했다. 초기 집계는 근거로 채택하지 않는다.
- 증거: `docs/research-trail/evidence/2026-09-08-freeze-readiness.json`, EVD-20260908-02.
- 코드 변경이 없어 전체 테스트·빌드·배포를 반복하지 않았다. 개발 규칙 수정안과 LOCK은 미확정이며
  이 기록은 콘텐츠 무오류·수업 운영 완료·학습효과를 입증하지 않는다.

[논문 영향 3줄]
1. 수치: 현재 편성 60개·고유 미션 60개 확인. 기존 54개에 대한 현행 R5/R27 판정 불일치 확인.
2. 화면: 변경 없음.
3. 프롬프트·계약: 변경 없음. 채택 모델의 운영 전환·최종 LOCK 미실행.
