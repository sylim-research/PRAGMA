# 교수자 주도 주차 도입 HOOK · 로컬 구현

- 작업공간: `C:\Users\cnkr\.codex\worktrees\2eb7\l2-pragmatic-translator`
- 브랜치: `codex/weekly-class-hook-2026-09-05`, 기준 `c2afdb458c35d13db65be5fa3c91dbc8c69ac60d`.
  최초 detached `f89937f`는 오래된 작업본이었다. clean을 확인하고 전달받은 운영 완료 커밋에서
  새 브랜치를 만들었다. 다른 worktree·사용자 변경은 수정하지 않았다.
- 분류: [교차검증 권고]. UI·콘텐츠 초안의 로컬 구현이며 운영 배포·콘텐츠 최종 승인은 미수행.

## 승인된 브리프와 구현

사용자는 옛 도입 아크 전체 재활용 대신 **단서 공개 후 재판단**, **맥락과 표현 선택의 연결**을
선택했다. 매주 수업 초반 10~15분, 교과목·방향·수준·해당 주차 목표와 정렬하는 교수자 활동이다.
구현 요청 뒤 귀가를 위해 적당한 시점에 정리하도록 지시했다.

- `/admin/package`의 선택 교과목·주차 자료에 `도입 수업 화면 열기`를 연결했다.
- 2/3/4/3분의 장면→첫 판단→단서→정리. 큰 원문·목표어 초안, 의견 선택, 단서 개별 공개/접기,
  토론 후 해설 공개, 단계 자유 이동, 방향키·Escape·재시작·포커스 복귀를 구현했다.
- 의견은 화면에서 이야기할 의견을 강조하는 로컬 상태다. 투표 집계·채점·학생 기록 저장은 없다.
- authored 예시 카탈로그가 9화행·3수준·양방향·영역을 반영하며 주차 채널·모드·P/D/R·목표를 읽는다.
  통역은 A/B/C 역할로 구분한다. OT에는 짧은 발화의 맥락 해석 사례를, 화행 없는 주차에는
  교수자가 장면을 제시하는 진행안을 제공한다. 모든 예문이 검수된 gold라는 뜻은 아니다.
- 조건 누락/모드 불일치/편성 화행 불일치/동일 원문은 시작을 막는다. 자유 서술 목표의 의미와
  예문의 상세 주제·산업 적합성까지 자동 검증하지 않는다. 내용의 적합성은 교수자 확인이 필요하다.
- 기존 `public_material.opening`에 공통 도입 내용을 포함해 주차 검수 원본·해시에 함께 들어간다.
  미션 답안·교수자 전용 메모는 가져오지 않는다. Edge 검수 번들도 같은 소스에서 재생성했다.
  기존 학생 유인물·HTML 출력은 변경하지 않았다. 새 도입의 오프라인 출력은 이번 범위에 없다.

## 검증과 제한

- 도입 모델·도입 UI·기존 학습 노트·수업자료 페이지·교수자 설명: **5 files / 35 tests 통과**.
  모델 테스트 안에서 9화행×3수준×양방향×2모드의 **108조건**을 순회했다. 108개 독립 테스트나
  콘텐츠 타당성 검수로 세지 않는다.
- `npm.cmd run typecheck` 통과.
- 로컬 브라우저에서 첫 장면·첫 의견·키보드 이동·단서 공개를 확인했다. 단계 전환 시 이전
  스크롤 위치가 남는 문제를 발견해 단계별 상단 이동으로 수정했다.
- 실행기는 sandbox 상위 폴더 접근 제한이 있어 승인된 실행 권한으로 테스트·번들을 실행했다.
  현재 환경 Node 24이며 저장소 요구는 22.x다. Browserslist·React Router 기존 경고가 남았다.
- 전체 테스트·운영 인증/DB를 포함한 E2E·유료 AI·실제 수업 참여 효과 검증은 수행하지 않았다.
- 최종 검수 회귀·앱 빌드 결과는 아래 마감 결과에 기록한다.

## 다음 세션

1. `http://127.0.0.1:8106/.tmp/opening-preview.html`에서 사용자와 HOOK 내용/집중도를 먼저 본다.
   `.tmp/OpeningPreview.tsx`는 운영 데이터가 없는 로컬 시연이다. 서버 종료 시 이 worktree에서
   `npm.cmd run dev -- --host 127.0.0.1 --port 8106 --strictPort`로 다시 실행한다.
2. 일반화한 영역 예시의 주차별 Can-do·상세 주제 적합성, 입문 어휘, 두 단서의 타당성을 검토한다.
   카탈로그 초안과 검수된 콘텐츠를 혼동하지 않는다. 모바일 실화면 검증은 미완료다.
3. 운영 적용 승인이 오면 main PR/CI와 content-review Edge를 함께 반영한다. 새 `opening`이
   검수 해시에 포함되므로 과거 주차 승인으로 새 예문이 자동 승인된 것으로 보지 않는다.
   DB migration·자동 공개·lot 승인·개별 미션 체계 변경은 없다.

관련 기록: `DEC-20260905-04`, `ITER-20260905-04`.

## 마감 결과

- 기존 콘텐츠 검수 회귀 15 tests 추가 통과. 이번 실행 합계 **6 files / 50 tests 통과**.
- `npm.cmd run build` 통과: 1,976 modules, 검수 domain 최신 여부와 34종 프롬프트 지문 검사 통과.
  기존 CSS 구문 경고(`-: T`)와 큰 chunk 경고는 남아 있다. 프롬프트 본문은 변경하지 않았다.
- 운영 데이터/DB/Edge/웹앱 배포 및 콘텐츠 최종 승인 없음. 사용자에게 로컬 미리보기를 인계한다.
