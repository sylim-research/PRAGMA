# 소스 기반 수업자료·토론 저작

- 시작: `codex/teaching-materials-discussion-2026-09-08`, HEAD `b1445fcc`, clean.
- 작업: `codex/source-teaching-studio-2026-09-09`. DEC-20260908-03 후속 사용자 정정.
- 문제: `/package`의 미션 중심 생성만으로는 사용자가 요청한 소스 선택·조건 설정·근거 중심 저작이 보이지 않았다.
- 결정: `/admin/teaching-generator`에 소스 → 수업 조건 → 실제 요청 확인 → 생성·편집 → 기존 주차 검토 동선을 둔다.
  산출물은 수업자료와 토론자료 두 가지다. FAQ·활동은 그 내부 구성이다. 시험·강의계획서·범용 노트북은 추가하지 않는다.

## 구현 범위

- PDF 텍스트 추출(20MB·100쪽, 브라우저)과 직접 텍스트만 받는다. 영상·음성·캡처는 출처와 확인한 본문을
  텍스트로 넣는다. 사용자 가성비 우선 지시에 따라 이미 구현했던 OCR·음성 전사·YouTube 전용 연동과
  별도 `teaching-sources` Edge를 최종 변경에서 제거했다. 새 외부 추출 서비스·키 설정은 필요 없다.
- 소스 1~6개·합계 60,000자. 추출 결과·출처를 확인한 자료만 생성에 사용한다. 원본 파일은 저장하지 않는다.
  생성 전 목록 자동 저장은 없고, 생성한 초안에는 선택한 원문·조건·추출 안내가 버전별로 보존된다.
- 소스만으로 미션 편성 전 초안을 생성한다. DB 주차 화행·목표가 범위이며 7·14주만 이전 계획을 종합한다.
  기존 v1 생성·승인 해시는 보존한다. 새 v2 초안은 각 절의 인용 구절을 원문과 대조하며 수정에도 같은 검사를 적용한다.
- 기존 관리자 전용 저장소·revision 충돌 방지·주차 승인·공용 필드 분리를 재사용한다. 생성 가능과 공개 가능은 다르며,
  화행 학습 주차 공개에는 기존 완전 미션 두 개 등 편성 요건이 필요하다. 학습자 응답·채점·MJT5+DCT1 계약은 바꾸지 않는다.

## 검증과 한계

- 축소 전: 타입 검사 통과. 전체 Vitest 872 통과·9 skip. production build 통과(기존 chunk 경고 유지).
  새 테스트의 불완전 타입 fixture를 완전한 TeachingDraft로 고친 뒤 타입 검사를 다시 통과했다.
- 실제 migration을 적용한 격리 PostgreSQL 16건, 실제 생성 핸들러 8건, 소스 핸들러 4건,
  기존 생성 job 4건·배포 정책 5건: 합계 37건 통과. Auth/외부 HTTP 응답은 대체한다.
- `scripts/manual-checks/source-teaching-smoke.mjs`: 실제 PDF 추출, 원문 확인 전 차단, 미션 없는 생성,
  설정 변경 후 재확인, 수정·다시 불러오기·소스 복원·주차 링크, 390·768·1280px 가로 넘침 없음.
  API는 합성 응답이다. 실제 모델 품질·운영 DB 쓰기·휴대폰 실기 녹음의 증거가 아니다.
- PDF.js는 지연 로딩한다. [공식 예제](https://mozilla.github.io/pdf.js/examples/)에 따른 텍스트 추출이며,
  도표·스캔본까지 분석했다고 표시하지 않는다. 스캔본은 확인한 본문을 직접 입력한다.
- 원문에 인용이 존재하는 검사만으로 소스의 신뢰성·해설의 의미 타당성·교육효과를 보장하지 않는다.

## 독립 검토 및 운영 적용

`[독립 검토 필수]`: 새 저장 조건과 DB 함수 변경으로, 기존 `AI_CROSS_REVIEW_PROTOCOL.md`를 적용한다.
clean commit에서 관리자/학생 경계, v1 호환, stale/revision 차단만 읽기 전용 검토한다.
운영 DB·Edge·프런트 적용 및 실제 생성 결과 검증은 아직 완료하지 않았다. 관련 결과는 이 절에 후속 기록한다.

- `4b5e6ddc`에서 도구·MCP·수정이 없는 Claude 검토를 수행했다. 구체적인 권한 우회·v1 해시 회귀는
  제시되지 않았다. 보고서 일부 P1 표기는 본문의 “문제 없음”과 모순되어 심각도로 채택하지 않았다.
- 두 검증 권고는 수용: 실제 v1 초안 저장 후 v2 migration을 적용해 초안·current·검수 해시 동일성을
  확인하고, 실제 검수 도메인의 미션 두 개 부족 fail을 SQL 승인에 전달해 공개가 차단되는지 검사했다.
  빈 선택 미션 배열 자체는 의도된 소스 저작 조건이며, 공개 요건을 면제하는 뜻이 아니다.
- 기능 범위를 줄인 뒤에는 별도 추출 서버 자체가 없어 해당 보안·자막 서비스 테스트도 제거했다.
- 최종 축소본: typecheck, 전체 Vitest 871 통과·9 skip, production build 통과. 생성·승인 Node 검사
  25건(실제 SQL 17·Edge 8)과 같은 브라우저 smoke를 다시 통과했다. 기존 job·배포 정책 9건은 앞선 검증을 재사용한다.

- 정본: 생성계약 §8.2, 관리자구조 §9.3.2를 동기화했다.
- 연구 기록: DEC-20260909-01, EVD-20260909-01. 학습자 구조·학습 수행 계약은 변경하지 않았다.
