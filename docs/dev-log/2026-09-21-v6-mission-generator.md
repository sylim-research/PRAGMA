# 2026-09-21 v6 학습 미션 자동 생성기(최소 경로)

## 무엇을 했나
- `/admin/assembly`에 「시나리오만」 칩과, 미션 없는 시나리오의 **「v6 초안 생성」** 버튼을 붙였다(한 번에 한 건).
  - 흐름: 생성(`generate-scenario` 새 액션 `mission_v6`, 관리자 확인) → 고정 문구·척도·DCT 원문 계승을 서버 쪽 규칙으로 채움 → 스키마·규칙·저작 검사 → 실패 시 지적 위치만 1회 수리 → 기존 AI 점검(v6) → `save_generated_mission`(검수 대기 초안).
  - 초안은 기존 품질 점검·교수자 최종 승인을 거쳐야 편성할 수 있다. 생성기는 승인 상태를 만들지 않는다.
- 프롬프트는 `supabase/functions/_shared/missionV6Generation.ts` 한 곳에서 만든다(Edge와 로컬 파일럿이 같은 문자열). 생성 템플릿과 v6 계약을 분리했고, 9화행 제작 요령·「결함은 원문이 아니라 목표문에」 원칙·사실 변경 금지·요구 철회형 오답 금지를 담았다.
- 생성 모델 = `gpt-5.5`(`OPENAI_MODEL_ROUTES.mission_v6`). 추론 모델은 temperature를 받지 않으므로 요청에서 뺀다.

## 왜 · 무엇을 검토했나
- 설계안 v2(`Documents/pragma-v6-conversion/2026-09-21_v6생성기_설계안_v2.md`)는 Codex 교차 검토(「수정 후 진행」) 반영본이다. 연구자 결정 3건: 6화행 추가 확인 후 전체 공개 / 템플릿 정정 채택 / 사실 변경형 오답 제외.
- 이후 연구자 판단으로 범위를 줄였다: 대표 교과목 60건은 사람 집필로 완성됐고 생성기는 「자동 생성 경로가 작동한다」는 데 의미가 있으므로 DeepSeek 검토·6화행 추가 확인·DB 최소 계약 migration·배치 버튼은 하지 않는다.
- 모델 비교(로컬 파일럿, 같은 코어 3건: 요청 한→중 번역·불만 한→중 통역·제안 중→한 번역): gpt-4o는 원문을 충실히 옮긴 번역에 오답 판정을 붙이는 개념 오류·사건 중복, gpt-4.1은 요구 철회형 오답을 금지 뒤에도 반복, gpt-5.5는 철회·사실 변경 없이 사람판에 가까웠다. 비교표 = `Documents/pragma-v6-conversion/2026-09-21_v6생성기_파일럿/`.

## 확인
- `npm run typecheck`, 전체 vitest 152 files · 1044 passed, `npm run build` 통과. 프롬프트 스냅샷 재생성.
- 로컬 파일럿 러너(`scripts/v6-generate-pilot.mts`)로 3건 생성 → 스키마·규칙 pass.
- 🔴 화면 버튼의 실제 작동(Edge 배포 후 관리자 로그인)은 아직 확인하지 않았다.

## 남은 것 · 주장 경계
- Edge 배포 후 시험 코어 1건으로 「버튼 → 검수 대기 초안」 확인.
- v6 AI 점검은 여전히 v5 후보 blueprint를 참고한다(Codex F3). 초안 품질 보장은 교수자 승인에 맡긴다.
- 논문 서술 범위: 「코어에서 v6 초안을 자동 생성해 기존 검수·승인 경로로 넘기는 기능을 구현했다」까지. 생성 품질·효과는 주장하지 않는다.
