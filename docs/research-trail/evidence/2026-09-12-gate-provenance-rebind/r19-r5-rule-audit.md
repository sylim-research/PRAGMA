# R19 · R5 규칙 감사 (read-only) — 2026-09-12

목적: 교수자 판단 128건 중 107건(R19 80 · R5 27)을 만드는 두 규칙이 실제로 무엇을 검출하는지 실측하고,
유지·수정·완화·aggregate·삭제의 근거를 마련한다. 이 단계에서 코드·계약·콘텐츠·판단·승인·gate·검수는
건드리지 않았다. 원자료 = `r19-r5-findings.json`(20건 active review의 rules.findings + MJT 문항 원문).

---

## 1. 현행 계약 복원

### R19 — `checkInternalDuplicates` (`src/lib/pragma/missionRules.ts:1403-1449`)

| 항목 | 사실 |
|---|---|
| 이름 | R19 「세트 내 source/candidate 완전 중복」 |
| 최초 도입 | `be321956` 2026-07-23 생성계약 v1.4 구현(R1~R23). 계약 v1 §7-0 「중복률 2종: 완전 중복률 = R19 자동 / 근접 중복률 = 표본 검수」— **원래 목적은 배치 코퍼스의 중복률 계측용 exact-match 카운터**였다. |
| 범위 확장 | `2026-08-25-mission-rule-audit.md:24` — target만 비교하던 것을 「MPJ source와 target·교정안·MultiJudge 후보 전체」로 확장. |
| predicate | 두 그룹을 각각 검사. ① `source` 그룹 = 5문항의 `source` ② 「판정 후보」 그룹 = 각 문항 `target` + fix_choice `corrections[*].text` + multi_judge `candidates[*].text`. 비교 = `text.normalize("NFKC").trim()` **완전 일치**(threshold 없음, 유사도 없음). 먼저 본 위치를 기억해 두고 같은 문자열이 다시 나오면 finding 1개. |
| 발생 단위 | **중복 쌍 1개당 finding 1개**(문항이 아니라 쌍). A문장이 3곳에 있으면 2개. |
| severity | warning 고정. fail 분기 없음. |
| 교수자 판단 경로 | `contentReviewDomain.ts:55-60` `isSignal = level==="warning"` → `needs_professor: true` (62206a5b, 2026-09-09) → SQL `content_review_required_findings`의 `v_rules`(`needs_professor='true'`)에 포함 (20260909230000, 9997f23f) → `save_content_review_decisions`가 전 항목 결정 요구, 승인 시 `no_change`만 허용. |
| 생성·승인 영향 | 저장 차단 없음(warning). 승인은 결정 입력 전까지 잠김. |
| 테스트 | `missionRules.audit.test.ts:90-111` — items[0].source = items[1].source(X=A) → 「source 완전 중복」 발생, MJT5 후보 = fix 교정안 → 「판정 후보 완전 중복」 발생. **A-A-A 공유가 finding을 내지 않는다는 테스트는 없다.** 반대로 R27 테스트 제목이 「without rejecting Anchor A sharing」(:113)이라 R27만 앵커 공유를 알고 있다. |
| 변경 이력 | 07-23 도입 → 08-25 범위 확장 → 09-09 needs_professor=true(어댑터). 카탈로그(`qualityRuleCatalog.ts:142`) 설명문이 이미 「**의도된 Anchor 공유도 함께 잡힌다**」고 적어 둠. 08-30 다이어트 감사 「KEEP-WARNING · exact만」. 09-09 Codex 감사 「의도된 공유 target도 경고 → 정상 콘텐츠 경고 상시화 · 범위·문구 조정 권장; 통합/폐기는 후속」, Fable 판정 「보완 — 단순한 결함 탐지 건수로 해석하지 않는다」. **후속 조치 없음.** |

### R5 — `checkMultiJudgeLength` (`missionRules.ts:1181-1252`) + 구조 분기 (`:784-856`)

| 항목 | 사실 |
|---|---|
| 이름 | R5 「MultiJudge 역할·대역·중복·한 축 대비 및 길이 완전 분리」(계약 §6.5). 한 ID에 **구조 fail 6종 + 길이 warning 4종**이 섞여 있다. |
| 구조 fail(이번 20건 0건) | 4후보·적정 2·조정 2 / 후보 문장 NFKC+구두점 제거 후 중복 / 앵커 PDR 한 축 차이 (native BEST/WORST·v4 분기 별도). |
| 길이 warning 4종 | 길이 = `[...text].length`(코드포인트 수). `withinCode` 포함 후보 = ok, 이름 정규식으로 under/over 판별(`isUnderBand`/`isOverBand`). ① **완전 분리**: 길이순 정렬이 단조이고 모든 non-within 길이가 within 구간 한쪽 밖 ② **과잉안 유일 최장**: over 후보 정확히 1개이고 그것이 유일한 max ③ **과소안 전부 최단**: under 후보 전부가 `=== minLen` — **유일성 요구 없음**(②와 비대칭: within이 같은 최소 길이여도 발동) ④ 최장/최단 비율 > 3. |
| 발생 단위 | MJT5 문항 1개당 조건별 finding(최대 4). 이번 20건은 문항당 1~2. |
| severity | 길이 4종 = warning. 07-23 도입 시 ①은 fail, ②③④ warning(「v1.4 증거 기반 강등」) → **08-25 ①도 warning**(DEC-20260825-03; native 운영에서 17·18자 vs 23·24자 완전 분리로 반복 차단). 07-31 DEC-20260731-01은 「코드 불변 + 계약 §6.4 해석 명문화」. |
| 계약 위치 | §6.4: 「warning(독립 의미 검수 신호) … 길이 패턴만으로 저장을 차단하지 않는다. **AI·교수자 판정**: 필수 AI 품질점검이 실제 answer_cue인지 문장 내용과 함께 독립 판정한다.」 판정 기준 = 「학습자가 목표 화용 판단 없이 길이만으로 유효한 BEST/WORST를 식별하거나 선택지를 현저히 좁힐 수 있는가」. |
| 교수자 판단 경로 | R19와 동일(warning → needs_professor → 필수 결정). |
| 생성 영향 | warning이라 저장됨. 재시도 지시문(「초점 자원과 대역은 유지하고 … 길이 범위를 겹치게」)은 ① 메시지에만 붙는다(DEC-20260802-07). |
| 테스트 | `missionRules.audit.test.ts:36-65`(완전 분리 경계: [17,23,24,18] 발동 / [17,20,24,22] 미발동), `missionSchema.test.ts:325-353`(재시도 지시문·후보별 길이 표기), 구조 fail 회귀 다수(`missionSchema.test.ts:277,417,445,465-566`), `promoteMission.test.ts:31`(R5는 item repair로 안 보냄), `moatMigrationContracts.test.ts:151`(R5 재시도 프롬프트 게이트). |
| 필수 AI 게이트와의 관계 | `generate-scenario/index.ts:3371-3373` ③ answer_cue 「후보 길이 구간이 나뉘어도 그 사실만으로 fail하지 말고, 실제 BEST/WORST 선택을 화용 판단 없이 식별하거나 현저히 좁힐 수 있을 때만 근거와 함께 warning/fail」. **현재 20건의 v24 quality_check finding 17개는 전부 `primary_reason_ambiguity`, answer_cue 0건.** |

---

## 2. R19 전수 — 80건

**분포: 20미션 × 정확히 4건, 형태가 전부 같다.**

| 유형 | 건수 | 내용 |
|---|---|---|
| C. 문항 간 의도된 반복 | **80** | `source 문항2=문항3`(20) · `source 문항2=문항4`(20) · `문항2 target=문항3 target`(20) · `문항2 target=문항4 target`(20) |
| A. 같은 선택지 세트 안의 실제 중복 | 0 | — |
| B. 의미상 판별 기능 없는 후보 | 0 | (R19는 exact-match라 원리상 탐지 불가) |
| D. 정상 어휘 공유 | 0 | (exact-match라 발생 불가) |
| E. 기타 | 0 | — |

대표 원문(w11-0): source 「공간을 넓히자는 취지는 이해해요. 그래도 작품 설명문은…」이 MJT2·3·4에 동일, target 「我理解你想让空间更宽敞。观众理解作品需要这些说明，全部保留也可以考虑一下吧。」가 MJT2·3·4에 동일. 나머지 19건도 같은 위치·같은 구조(원자료 참조).

- 왜 반드시 4건인가: 현행 native MJT5는 MJT2(judge3)가 Anchor A 문장을 만들고 MJT3(fix_choice)·MJT4(reason)가 **같은 문장을 교정·설명**하는 설계다. 프롬프트가 「MJT2에서 Anchor A 상황을 한 번 만들고 MJT3·4는 … 정확히 복사 … 서버도 MJT2의 A를 MJT3·4에 고정」(`generate-scenario/index.ts:1869-1872`)이라 지시하고 R27이 situation_ko 동일성을 **fail로 강제**한다(`missionRules.ts:1286-1291`). 같은 문장을 세 관점에서 다루는 구조에서 source·target이 같은 것은 결함이 아니라 **설계의 정의**다. (source/target 복사를 명시한 프롬프트 문장은 이번에 찾지 못했다 — 실측 20/20 공유, 참조 샘플 `missionV4Sample.ts:321-368`도 target 공유.)
- 결과: R19의 finding 공간은 설계에 의해 **미션당 4건으로 포화**돼 있다. 실제 결함(예: MJT5 후보 = MJT3 교정안, X target = A target)은 「5번째 finding」으로 나타나야 하는데 20건 중 0건이다.
- 실제 학습 결함: 0/80. 자동 확정: 가능(「A 슬롯 쌍」은 위치로 식별 가능). 인간 판단 필요: 없음.

---

## 3. R5 전수 — 27건 (MJT5 20문항)

저장 순서는 20/20 모두 `#1 within · #2 under · #3 within · #4 over`다. 길이 = 코드포인트.

| 미션 | #1 within | #2 under | #3 within | #4 over | 발동 조건 | over 초과분(대 max-within) | 판정 |
|---|---|---|---|---|---|---|---|
| w2-0 | 34 | 32 | 40 | 48 | 최장 / 최단 | +8 (20%) 「但不知道这事该不该向你开口」 | 최장 **B** · 최단 **C**(−2자) |
| w2-1 | 41 | 37 | 47 | 46 | 최단 | over는 최장 아님(within 47) | 최단 **B**(−4 「可以吗？」= 선택권 표지 자체) |
| w3-0 | 21 | 24 | 23 | 29 | 분리 / 최장 | +6 「真是欠了你一份大人情」 | 분리 **C**(under 24 vs within 23, 1자) · 최장 **B** |
| w3-1 | 13 | 18 | 14 | 18 | 분리 | bad 18·18 vs within 13·14 | 분리 **A**(간격 4자·29%, 최단=BEST·최장=WORST 성립) |
| w4-0 | 29 | 31 | 31 | 35 | 최장 | +4 (13%) 「各方面都做得很出色」 | **B**(under 31 = within 31) |
| w4-1 | 25 | 26 | 29 | 43 | 최장 | +14 (48%) 「你的工作真是处处都让人放心」 | **A** |
| w5-0 | 69 | 71 | 71 | 89 | 최장 | +18 (25%) 「去不去不用定，之后怎么安排你看情况就行」 | **B**(under 71 = within 71) |
| w5-1 | 51 | 55 | 54 | 69 | 분리 / 최장 | +15 (28%) | 분리 **C**(54→55, 1자) · 최장 **B** |
| w6-0 | 24 | 19 | 19 | 50 | 최장 / 최단 | +26 (108%) 「我该怎么说才合适呢，直接说…又怕显得生硬」 | 최장 **A** · 최단 **C**(under 19 = within 19, 단서 없음) |
| w6-1 | 26 | 24 | 22 | 56 | 최장 | +30 (115%) | **A** |
| w9-0 | 43 | 53 | 54 | 64 | 최장 | +10 (19%) 「是我对工作太不负责」 | **B** |
| w9-1 | 52 | 55 | 55 | 56 | 최장 | **+1** | **C** |
| w10-0 | 42 | 52 | 51 | 74 | 분리 / 최장 | +23 (45%) 「也许也行…先这么随便想想吧」 | 분리 **C**(51→52, 1자) · 최장 **A** |
| w10-1 | 32 | 38 | 45 | 49 | 최장 | +4 (9%) | **C**(경계) |
| w11-0 | 36 | 38 | 47 | 58 | 최장 | +11 (23%) | **B** |
| w11-1 | 24 | 28 | 32 | 41 | 최장 | +9 (28%) | **B** |
| w12-0 | 44 | 42 | 40 | 47 | 최장 | **+3** (7%) | **C** |
| w12-1 | 38 | 43 | 40 | 52 | 분리 / 최장 | +12 (30%) | 분리 **C**(40→43, 3자) · 최장 **B** |
| w13-0 | 24 | 25 | 29 | 35 | 최장 | +6 (21%) 「您是否愿意考虑」 | **B** |
| w13-1 | 21 | 21 | 22 | 33 | 최장 / 최단 | +11 (50%) 「不知道会不会让你为难」 | 최장 **A** · 최단 **C**(under 21 = within 21) |

판정 기준(계약 §6.4 ③을 실측에 적용): **A** = 최장/최단만으로 유효 BEST/WORST가 나오고 여유가 40% 이상(또는 완전 분리 간격 ≥4자·≥20%) · **B** = 여유 10~40%이고 초과분이 초점 자원(완화·유보·과장 표현) 자체 · **C** = 여유 ≤3자 또는 <10%, 또는 under가 within과 같은 길이라 단서가 성립하지 않음.

| 분류 | 건수 | 내역 |
|---|---|---|
| A. 명백한 length leakage(표면 기준) | **6** | 최장 w4-1·w6-0·w6-1·w10-0·w13-1 · 분리 w3-1 |
| B. 경계(화용 실현에서 자연 발생) | **11** | 최장 w2-0·w3-0·w4-0·w5-0·w5-1·w9-0·w11-0·w11-1·w12-1·w13-0 · 최단 w2-1 |
| C. false positive | **10** | 최단 4건 전부(w2-0 −2자 / w6-0·w13-1 동률 / …) · 최장 w9-1(+1)·w12-0(+3)·w10-1(+4, 9%) · 분리 w3-0·w5-1·w10-0(1자)·w12-1(3자) |

- 계산 방식 확인: 절대 길이 아님. 「유일 최장」은 **순위**(여유 1자여도 발동), 「전부 최단」은 **동률 허용**(within과 같아도 발동), 「완전 분리」는 **구간 비겹침**(1자 차이여도 발동), 비율은 3배(0건). 세 조건 모두 **여유(margin)를 보지 않는다** — C 10건이 전부 여기서 나온다.
- A 6건도 초과분은 전부 초점 자원(over_elaborate의 메타 언급, too_indirect의 「不知道会不会…」, overreaching의 일반화)이다. §6.4의 「두 사실이 동시에 참일 수 있다」는 그대로이고, **필수 AI 게이트(answer_cue)는 20문항 모두 무지적**이었다. 즉 계약이 위임한 판정자가 이미 「단서 아님」으로 판단한 상태에서 R5가 같은 문항을 교수자 결정 목록에 다시 올린 것이다.
- 한 미션 안 중복 발생: 7미션이 같은 MJT5 문항에 2건(최장+분리 5, 최장+최단 2). 본질은 하나(「over가 길다」)인데 finding은 둘.
- 인간 판단 필요: A 6건은 §6.4 기준으로 「수정 대상인가」를 교수자가 볼 가치가 있다. B 11건은 설계가 허용한 배열(DEC-20260731-01 「과소–적정–과잉이 길이순으로 늘어서는 배열은 fail이 아니다」)이라 결정을 요구할 근거가 약하다. C 10건은 판단할 것이 없다.
- 범위 밖 관찰(기록만): 저장 순서가 20/20 동일(`within·under·within·over`). 학습자 화면에서 순서를 섞는지는 이번에 확인하지 않았다.

---

## 4. 정보 가치

| 질문 | R19 | R5(길이 warning) |
|---|---|---|
| 1. 객관적 자동 판정 결함인가 | 예 — exact match는 완전 자동. | 부분 — 길이 순위는 자동, 「단서인가」는 아님(계약도 그렇게 씀). |
| 2. 그런데 왜 professor decision인가 | 어댑터가 **모든** warning을 needs_professor=true로 바꿔서. R19에 고유한 이유 없음. | 같음. 계약 §6.4는 「교수자 검수에 남긴다」이지 「항목별 결정」이 아니다. |
| 3. deterministic rule로 적절한가 | exact-dup 자체는 적절. 단 **앵커 공유 슬롯을 제외하지 않은 채**로는 부적절. | 길이 기록은 적절(값 자체가 정보). 판정 요구는 부적절. |
| 4. 정상 콘텐츠 반복 flag | **100%** (80/80, 미션당 정확히 4). | C 10/27 = 37%가 판단 불가 finding. B까지 합치면 21/27이 「설계 허용 배열」. |
| 5. 같은 본질의 중복 finding | 예 — 한 앵커 공유가 **4 finding**(source 2 + target 2). | 예 — 7미션에서 같은 over 후보가 2 finding. |
| 6. 규칙이 없을 때 위험 | 문항 간 교정안/후보 exact 재사용을 놓침. 20건 실측 0. | 극단 길이 배열을 기록 없이 통과시킴. |
| 7. 그 위험은 다른 곳에서 이미 확인되나 | R5 구조 fail이 MJT5 후보 내부 중복, R4가 이유 문구 중복, R27이 상황문 중복을 **fail**로 막는다. 남는 공백 = 문항 **간** target/교정/후보 exact 재사용뿐. | **예.** 필수 AI 게이트 answer_cue(§6.4가 지정한 판정자) + Claude 독립 검토 + 교수자 최종 승인. |

---

## 5. 처분안 비교 (최종 선택 아님)

### R19

| 안 | 잡는 결함 | 예상 FP | 교수자 노동 | 계약 정합 | 테스트 영향 |
|---|---|---|---|---|---|
| 유지 | 20건 실측 0 | 100% (미션당 4) | 80 결정 | §6.5 R19 행 그대로 | 없음 |
| **predicate 수정** — Anchor A 슬롯(MJT2↔3↔4) source·target 쌍을 비교에서 제외, 그 외 exact dup은 유지 | X/Y target=A, 후보=교정안, 후보 간 교차 재사용 | ≈0 | **0** | §6.5 문구에 「앵커 공유 제외」 1구 추가 필요 | `audit.test.ts:90-97`(X=A source) 그대로 발동, `:99-111`(후보=교정안) 그대로 발동. **A-A-A 무발동 회귀 1개 추가**. |
| threshold 완화 | — | — | — | exact-match라 threshold 없음 → **해당 없음** | — |
| 미션 단위 aggregate | 위와 같음 | 여전히 100%(1건으로 압축) | 20 결정 | 문구 수정 필요 | finding 수 기대하는 테스트 없음 |
| 삭제 | 문항 간 exact 재사용 미탐(실측 0) | 0 | 0 | §6.5 R19 행 retired 처리, 「완전 중복률 = R19 자동」 계약 문구 정리 필요 | `audit.test.ts:90-111` 삭제·`qualityRuleCatalog` 키 제거 |

### R5(길이 warning만; 구조 fail 6종은 대상 아님)

| 안 | 잡는 결함 | 예상 FP | 교수자 노동 | 계약 정합 | 테스트 영향 |
|---|---|---|---|---|---|
| 유지 | A 6 | C 10 (+B 11은 설계 허용) | 27 결정 | §6.4 그대로 | 없음 |
| **predicate 수정** — ③ 최단에 유일성 요구(②와 대칭) + 세 조건에 여유 하한(예: ≥4자 및 ≥10%) | A 6 + B 대부분 | C 10 → 0 | 27 → 17 | §6.4 「과소안 최단 집중」 정의에 여유 조건 1줄 | `audit.test.ts:36-50` [17,23,24,18]은 간격 5·22%라 유지 발동, 하한 회귀 추가 |
| threshold 완화 | 비율 3은 0건이라 무의미; 실질은 위 「여유 하한」과 같음 | — | — | — | — |
| 문항 단위 aggregate(조건 여러 개 → finding 1) | 같음 | 같음 | 27 → 20 | 메시지 합치기만 | 메시지 문자열 검사 테스트 2개 손봐야 함 |
| 삭제 | 0 | 0 | 0 | DEC-20260825-03이 「교수자에게 단서가 보이지 않을 수 있어」 삭제 기각. 단 그때 warning은 **표시만**이었고 결정 요구가 없었다(needs_professor=false). 전제가 바뀌었다. | 6개 파일의 R5 warning 테스트 제거 |

### 두 규칙 밖의 공통 지렛대(§6과 연결)

R19 80 + R5 C 10 + R5 B 11 = **101/107**은 규칙이 아니라 「warning = 항목별 결정」이라는 어댑터 층에서 생긴다. 규칙을 고치지 않고 **결정론 warning을 「표시 신호」와 「판단 요구 신호」로 분리**하면 107건이 한 번에 사라지고 R9·R30·R16·R32(2026-09-09 결정의 실제 대상)만 결정 목록에 남는다. 이건 approval contract 변경이라 이번 단계 금지 사항이며, 여기서는 선택지로만 적는다.

---

## 6. 승인 workflow — 왜 warning 하나하나에 결정+10자인가

| finding 종류 | 예 | 현재 결정 필수? | 근거 코드 |
|---|---|---|---|
| blocking fail(규칙) | R2·R4·R5 구조·R27 | 결정 불가 — 승인 자체가 막힘(`rules.verdict='fail'` → base_ready 거부). 콘텐츠 수정 후 새 검수. | `20260912090000:37` |
| non-blocking deterministic warning(규칙) | **R19·R5·R9·R30·R16장면·R32** | **예** — 전부 `needs_professor=true` → 필수 목록 → `no_change`+10자 | `contentReviewDomain.ts:55-60` · `20260909230000:15-17` |
| AI semantic finding(Claude 독립 검토) | claude-N | fail 또는 needs_professor=true인 것만(warning·needs_professor=false는 제외) | `20260909230000:24-26` |
| generation gate(quality_check) | generation-N | fail만(needs_professor = severity fail) — 결정 **+** finalize의 issue_override 사유도 별도 | `20260909230000:21` · `finalize_reviewed_mission` |

**의도인가 부작용인가 — 층이 다르다.**

1. 2026-07-31 설계(DEC-20260731-02 §4): 「AI pass 미션도 자동 승인하지 않는다. **인간은 먼저 노란 규칙 경고를 훑는다.** R19 target 중복과 R29는 수정·보류 신호이고, §6.4의 R5 warning은 그 자체로 차단 사유가 아니다.」 — warning은 **훑는 것**, 항목별 결정이 아니었다.
2. 2026-08-27(DEC-20260827): 교수자 결정은 **Claude finding**마다. 규칙 finding은 대상 아님.
3. 2026-09-06 focused_v1: 필수 목록 = fail 또는 needs_professor. 규칙 finding은 어댑터가 전부 `needs_professor:false`로 넘겨 목록에 없었다.
4. 2026-09-09 `62206a5b`: R9·R30·R16 장면을 fail→warning으로 내리면서 「어댑터를 먼저 고치지 않으면 **warning 강등 = 침묵**」(decision-plan:19)이 첫 MUST가 됐고, 해결은 **개별 규칙이 아니라 `level==="warning"` 전체**에 `needs_professor:true`를 붙이는 한 줄이었다.
5. 같은 날 `9997f23f` / DEC-20260909-02: 「경고를 TS/SQL 필수 판단 목록에 포함」 — TS 표시와 SQL 승인 조건의 불일치를 맞추려는 결정. 회귀 테스트(`qualitySignalFlow.test.ts:60-80`)의 fixture는 **R9·R30·R16·R32뿐**이다. R19·R5는 어느 결정문·테스트에도 등장하지 않는다.
6. 같은 날 Codex 감사(`:108`)와 Fable 판정(`:54`)이 R19의 「정상 콘텐츠 경고 상시화」를 확인하고 「범위·문구 조정 권장; 통합/폐기는 후속」·「단순한 결함 탐지 건수로 해석하지 않는다」로 **미뤘다.**

결론: 「의미 추정 신호(R9·R30·R16·R32)는 교수자 결정을 받아야 한다」는 **의도된 결정**이다. 그것을 **모든 warning에 일괄 적용**한 것은 구현 편의였고, 그 결과 R19·R5가 결정 목록에 들어간 것은 **당일 감사에서 인지됐으나 후속으로 미뤄진 부작용**이다.

---

## 8. 최종 보고 요약

**A. R19** — 문항 source·target·교정·후보 exact-dup warning. 80건 = 20 × 4, 전부 Anchor A(MJT2↔3↔4) 공유. 실제 결함 0 · FP 100% · 인간 판단 필요 0. 규칙의 finding 공간이 설계로 포화됨.

**B. R5** — 길이 warning 3조건 27건(최장 18 · 최단 4 · 분리 5, 비율 0). A 6 · B 11 · C 10. C는 전부 「여유를 안 보는」 predicate(1자 차·동률·1자 분리)에서 나옴. A 6건도 초과분은 초점 자원이고 필수 AI 게이트 answer_cue는 20/20 무지적. 인간 판단 가치가 있는 것은 A 6건.

**C. 교수자 노동** — 128 = 규칙 warning 111(R19 80 · R5 27 · R32 3 · R27 1) + 생성 gate fail 6 + Claude 11. R19+R5 = 107(84%). 경로 = `isSignal→needs_professor:true`(62206a5b) → `v_rules`(9997f23f) → `validate_content_review_decisions(require_clear)`. 화면에 일괄 처리 없음(`ContentReviewPanel.tsx` 항목별 select+textarea).

**D. 선택지** — §5 표.

**E. 가장 유력한 안(결정 아님)**
1. **R19 predicate 수정(앵커 슬롯 제외)** — 규칙의 원래 목적(exact-dup 계측)은 살리고 설계가 만드는 4건을 없앤다. 삭제보다 나은 이유: 남는 공백(문항 간 교정안/후보 재사용)을 0비용으로 계속 잡는다. 계약 문구 한 구 + 회귀 1개.
2. **R5는 predicate 보정(최단 유일성 + 여유 하한)으로 C 10건 제거, 나머지는 「표시 신호」로 유지** — 단 27→17로 줄 뿐이며, 진짜 지렛대는 §5 끝의 신호/판단 분리(approval contract 층)다. 이건 별도 결정 사안.

---

## 7. 이번 단계에서 하지 않은 것

코드·계약·콘텐츠 수정 0 · 교수자 판단 저장 0 · 승인 0 · gate/검수 재실행 0 · 인접 규칙(R27·R32·R12) 감사 0. 관찰만 기록: MJT5 저장 순서 20/20 동일.

## 출처

- 코드: `missionRules.ts:784-856, 1181-1264, 1283-1297, 1403-1449` · `contentReviewDomain.ts:40-61` · `qualityRuleCatalog.ts:71-75, 142-146` · `generate-scenario/index.ts:1865-1876, 3371-3373` · migrations `20260906100000` `20260909230000` `20260912090000`
- 테스트: `missionRules.audit.test.ts` · `missionSchema.test.ts` · `qualitySignalFlow.test.ts` · `promoteMission.test.ts` · `moatMigrationContracts.test.ts`
- 계약: `docs/contracts/PRAGMA_생성계약_정본.md` §6.4·§6.5 · legacy v1 §7-0·§8
- 기록: DEC-20260731-01·02 · DEC-20260802-07 · DEC-20260825-03 · DEC-20260906-03 · DEC-20260909-02 · dev-log 2026-08-25 · 2026-09-09 ×5 · 2026-09-09-quality-signal-review-flow
- 커밋: `be321956` · `62206a5b` · `9997f23f` · `817e9f1a` · `818db3cf`
- 실측: `r19-r5-findings.json` · `checklist-data.json`(v24 gate 코드 집계)
